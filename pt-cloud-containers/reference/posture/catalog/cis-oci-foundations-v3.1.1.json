{
  "meta": {
    "catalog_id": "cis-oci-foundations",
    "framework": "CIS Oracle Cloud Infrastructure Foundations Benchmark",
    "framework_version": "3.1.1",
    "provider": "oci",
    "control_count": 54,
    "level_split": {
      "1": 45,
      "2": 9
    },
    "assessment_split": {
      "Automated": 44,
      "Manual": 10
    },
    "sections": {
      "1": "Identity and Access Management",
      "2": "Networking",
      "3": "Compute",
      "4": "Logging and Monitoring",
      "5": "Storage",
      "6": "Asset Management"
    },
    "source_url": "https://www.cisecurity.org/benchmark/oracle_cloud",
    "licence_note": "Control numbers, profile levels and assessment statuses are identifiers and facts taken from the named benchmark; every title, objective, procedure, evidence rule, verdict rule and remediation in this file is our own writing, derived from Oracle's public API documentation. No benchmark text is reproduced. CIS Terms of Use require written guidance from CIS Legal before using portions of Benchmark Recommendations in third-party documentation, and forbid hosting the benchmark itself anywhere outside CIS; the source PDF is gitignored. Benchmarks are free for non-commercial use only, so deliverables cite control numbers and say 'CIS-aligned' rather than claiming a certified CIS Benchmark assessment.",
    "verdicts": [
      "MET",
      "NOT_MET",
      "PARTIALLY_MET",
      "NOT_APPLICABLE",
      "REQUIRES_MANUAL_REVIEW"
    ]
  },
  "controls": [
    {
      "control_id": "1.1",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Delegate administration to per-service groups, not one blanket tenancy admin group",
      "objective": "Routine operation of individual OCI services should be performed by groups whose grants are scoped to a single service family and a single compartment subtree, so that compromise or error inside one service cannot be leveraged into tenancy-wide control. Concentrating all operational work in the tenancy Administrators group removes any blast-radius boundary.",
      "procedure": "Enumerate all ACTIVE compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all`. Note that `--include-root` is a valueless switch: writing `--include-root true` aborts the command with an unexpected-argument error, and omitting it drops the root compartment where most tenancy-scoped policies live. For each compartment OCID run `oci iam policy list --compartment-id <id> --all`, then `oci iam policy get --policy-id <id>` to read the `statements` array, and build a matrix of subject group -> verb -> resource-family -> scope. Derive the set of service families actually in use deterministically rather than by opinion: run `oci search resource structured-search --query-text \"query all resources\" --region <region>` once for every region returned by `oci iam region-subscription list`, then map each returned `resource-type` to its policy family (instance -> instance-family, bucket -> object-family, volume -> volume-family, vcn/subnet/securitylist -> virtual-network-family, autonomousdatabase -> database-family, functionsfunction -> functions-family, and so on). Cross-reference the policy subjects with `oci iam group list --compartment-id <tenancy_ocid> --all` and, per identity domain, `oci identity-domains groups list --endpoint <domain_url> --all`.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all --query 'data[].id' --raw-output",
        "oci iam policy list --compartment-id <compartment_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'",
        "oci iam group list --compartment-id <tenancy_ocid> --all",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url,type:type}'",
        "oci identity-domains groups list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",id:id}'",
        "oci search resource structured-search --query-text \"query all resources\" --region <region> --query 'data.items[].\"resource-type\"'"
      ],
      "evidence": "The `statements` string array from each `oci iam policy get` response, keyed by policy OCID and its `compartment-id`; the group name/OCID list from `oci iam group list` and per-domain `oci identity-domains groups list`; and the derived in-use family table (distinct `resource-type` values per region from `oci search resource structured-search`, with the family each maps to).",
      "verdict_rule": "PASS when, for every service family in the set derived from the per-region Resource Search inventory, at least one group other than Administrators holds a compartment-scoped manage grant on that family, and no group outside Administrators holds `manage all-resources in tenancy`. If the derived in-use set is empty for a region the run is inconclusive, not a PASS.",
      "remediation": "Create one group per service function (for example storage operators, network operators, database operators) and write policies of the form `allow group <name> to manage <service>-family in compartment <compartment>`. In a multi-domain tenancy qualify the subject as `allow group '<DomainName>'/'<GroupName>' to ...`. Remove those individuals from Administrators and keep Administrators for break-glass only. Attach `where` conditions on `request.permission` to narrow the grant further where the service supports it.",
      "caveat": "Marked Manual because no API can tell whether the observed split of duties matches the organisation's operating model — a tenancy with three service groups may still be non-compliant if one of them is a de-facto super-admin. Automation traps: (1) `--include-root` takes no value, so the common `--include-root true` form fails outright and a silent fallback to a root-only listing misses most policies, since policies are created in any compartment; (2) in multi-domain tenancies policy subjects are written as `group '<DomainName>'/'<GroupName>'`, so string matching on `group <name>` silently misses every non-Default-domain grant; (3) Resource Search is a regional, eventually-consistent, permission-filtered index — a single-region run yields a short in-use family set and the rule then passes vacuously, so loop every subscribed region and treat a caller with partial inspect permission as inconclusive rather than compliant."
    },
    {
      "control_id": "1.2",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Unbounded tenancy authority stays with the break-glass admin group alone",
      "objective": "A statement that grants manage on all-resources at tenancy scope is equivalent to full tenancy ownership: it covers IAM, keys, audit configuration and every workload. Only the built-in Administrators group should hold it, so that the set of principals with unbounded authority is small, known, and monitorable.",
      "procedure": "Walk the full compartment tree with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (`--include-root` is a valueless switch — do not pass it a value, and do not omit it, because `in tenancy` statements can only live in the root compartment). List policies in each compartment and pull `statements` via `oci iam policy get --policy-id`. Normalise each statement to lowercase and tokenise subject / verb / resource / scope rather than substring-matching. Flag any statement whose resource is `all-resources`, whose verb is `manage`, whose scope is `in tenancy`, and whose subject is anything other than the built-in Administrators group — including `any-user`, `dynamic-group`, and domain-qualified `group '<Domain>'/'<Name>'` subjects. Resource Search may be used only to accelerate discovery, and then only against the home region resolved from `oci iam region-subscription list`, because IAM objects are home-region/global while the Search endpoint is regional.",
      "api": [
        "oci iam region-subscription list --query \"data[?\\\"is-home-region\\\"].\\\"region-name\\\"\" --raw-output",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all --query 'data[].id' --raw-output",
        "oci iam policy list --compartment-id <compartment_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'",
        "oci search resource-type list --region <home_region> --query 'data.items[].name'",
        "oci search resource structured-search --query-text \"query policy resources\" --region <home_region> --query 'data.items[].{id:identifier,c:\"compartment-id\"}'",
        "oci iam group list --compartment-id <tenancy_ocid> --all --query \"data[?name=='Administrators'].id\" --raw-output"
      ],
      "evidence": "Each offending element of the `statements` array returned by `oci iam policy get --policy-id`, quoted verbatim together with the policy OCID, policy name and owning `compartment-id`; plus the Administrators group OCID from `oci iam group list` used to establish which subject is exempt.",
      "verdict_rule": "PASS when no policy statement in any compartment grants manage on all-resources in tenancy to a subject other than the built-in Administrators group (no any-user, no dynamic-group, no other named or domain-qualified group). A run whose compartment enumeration did not include the root compartment, or that relied solely on Resource Search, is inconclusive and may not be recorded as PASS.",
      "remediation": "Delete or rewrite the offending statement. Replace the tenancy-wide grant with the narrowest scope that still works: pin the verb to `use` or `read` where possible, pin the resource to a specific family such as `instance-family` or `object-family`, and pin the scope to `in compartment <name>`. If a broad grant is genuinely needed, bound it with `where` conditions on `request.permission` or `request.region`.",
      "caveat": "Only a root-compartment policy can carry `in tenancy` scope, but you must still read child-compartment policies: `manage all-resources in compartment <top-level-compartment>` over a compartment holding 95% of the estate is a near-equivalent grant that a regex looking for the literal `in tenancy` will pass. Two command-level false passes: `--include-root true` aborts the compartment walk (the flag takes no value), and running `oci search resource structured-search` without `--region <home_region>` queries a regional index that holds no home-region IAM objects, returning an empty item list that reads as 'no offending statements' on the most severe control in the section — confirm `policy` is even an indexed type with `oci search resource-type list` before leaning on it, and keep the compartment-tree walk as the authoritative enumeration. Statement text is free-form: case varies, several subjects may be comma-separated in one statement, and a trailing `where` clause may or may not constrain it, so token parsing beats substring matching. Search results are also eventually consistent and filtered by the caller's inspect permission."
    },
    {
      "control_id": "1.3",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Fence identity administrators out of the Administrators group via policy conditions",
      "objective": "Delegated identity administrators need to create users and groups, but if they can also modify membership of the tenancy Administrators group they can escalate themselves to full tenancy control at will. The grant must therefore carry an explicit condition that excludes the Administrators group as a target.",
      "procedure": "Enumerate policies across the whole compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`, with `--include-root` passed as a bare switch) and read `statements` with `oci iam policy get --policy-id`. Select every statement whose subject is not the Administrators group and whose resource is `groups`, `users`, `policies`, `all-resources`, or an identity aggregate, with verb `use` or `manage` (these resources exist only at tenancy scope, so the scope will read `in tenancy`). For each, verify the statement carries a `where` clause containing a `target.group.name` inequality naming the admin group, commonly inside `where all { ... }`. Separately, per identity domain resolved from `oci iam domain list`, enumerate administrator application-role grants with `oci identity-domains grants list --endpoint <domain_url> --all` (optionally filtered with `--filter 'app.value eq \"<idcs_admin_app_id>\"'`) and resolve the role names via `oci identity-domains app-roles list --endpoint <domain_url> --all`, to catch escalation paths that bypass IAM policy entirely.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam policy list --compartment-id <compartment_ocid> --all",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url,type:type}'",
        "oci identity-domains grants list --endpoint <domain_url> --all --query 'data.resources[].{grantee:grantee,role:\"app-role\",app:app}'",
        "oci identity-domains app-roles list --endpoint <domain_url> --all --query 'data.resources[].{id:id,name:\"display-name\"}'"
      ],
      "evidence": "The verbatim statement string from `oci iam policy get --policy-id`, showing presence or absence of a `where ... target.group.name != 'Administrators'` condition, keyed by policy OCID and compartment OCID; plus the grantee list from `oci identity-domains grants list --endpoint <domain_url>` joined to `oci identity-domains app-roles list` so each grant is named (Identity Domain Administrator, User Administrator, Security Administrator) and attributed to a group or user.",
      "verdict_rule": "PASS when every statement granting use or manage on groups, users, policies or all-resources at tenancy scope to a non-Administrators subject includes a condition excluding `target.group.name` equal to the admin group name, and no group or user outside the intended tenancy-administrator set holds a domain administrator application role in any identity domain.",
      "remediation": "Rewrite the grant as `allow group <IdentityAdmins> to manage groups in tenancy where all {target.group.name != 'Administrators'}`, and apply the same pattern to the `users` and `policies` resource types. Where a second identity domain has its own Administrators group, add an inequality for that name too (domain-qualified subjects and targets must be written explicitly). Review domain-level application role grants with `oci identity-domains grants list` and revoke Identity Domain Administrator, User Administrator and Security Administrator from any group that is not intended to be tenancy-equivalent.",
      "caveat": "The biggest false pass is checking IAM policy alone. In an identity-domain tenancy the domain's own administrator application roles (Identity Domain Administrator, User Administrator, Security Administrator) are granted through SCIM grants, not IAM policies, and confer group-management power that no `where target.group.name` condition constrains — so `grants list` is mandatory, not supplementary. The condition value is also matched on the group name exactly as written: a tenancy that renamed or duplicated the admin group, or that has an Administrators group in a second identity domain, is not protected by a condition naming only the Default-domain string. Statements granting `manage all-resources` implicitly include the groups resource type and must be selected too. Finally the compartment walk must use `--include-root` as a bare switch — `--include-root true` fails the command outright, and a root-less walk misses the tenancy-scoped statements this control is about."
    },
    {
      "control_id": "1.4",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Require at least 14 characters in every effective identity domain password policy",
      "objective": "Password length is the dominant factor in resistance to offline cracking and online guessing for locally managed accounts. Every password policy that can apply to a local user in any identity domain must enforce a minimum length of at least 14 characters, not just the policy named Default.",
      "procedure": "List identity domains across the compartment tree: `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (bare `--include-root` switch), then `oci iam domain list --compartment-id <id> --all` capturing each domain's `url`. For every domain run `oci identity-domains password-policies list --endpoint <domain_url> --all` — the identity-domains CLI uses the plural SCIM resource name for list operations, so the singular `password-policy list` does not exist — and read `min-length` on each entry of `data.resources[]`, together with `priority` and the `groups` assignment that determines who it applies to. Use the singular form only for single-resource operations (`oci identity-domains password-policy get --password-policy-id <id>`). For a legacy (pre-domain) tenancy also run `oci iam authentication-policy get --compartment-id <tenancy_ocid>`.",
      "api": [
        "oci iam region-subscription list --query 'data[].{region:\"region-name\",home:\"is-home-region\"}'",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url,id:id}'",
        "oci identity-domains password-policies list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",min:\"min-length\",priority:priority,groups:groups}'",
        "oci identity-domains password-policy get --endpoint <domain_url> --password-policy-id <policy_id>",
        "oci iam authentication-policy get --compartment-id <tenancy_ocid> --query 'data.\"password-policy\"'"
      ],
      "evidence": "The `min-length` attribute of every PasswordPolicy in `data.resources[]` returned by `oci identity-domains password-policies list --endpoint <domain_url>`, recorded with the policy `display-name`, its `priority`, its assigned `groups`, and the domain OCID; for legacy tenancies, `data.password-policy.minimum-password-length` from `oci iam authentication-policy get`.",
      "verdict_rule": "PASS when every password policy in every identity domain of the tenancy reports min-length >= 14, and, where the legacy authentication policy exists, password-policy.minimum-password-length >= 14. A domain enumeration that did not walk the compartment subtree is inconclusive, not a PASS.",
      "remediation": "Raise the minimum on each policy with a SCIM patch: `oci identity-domains password-policy patch --endpoint <domain_url> --password-policy-id <id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations '[{\"op\":\"replace\",\"path\":\"minLength\",\"value\":14}]'` (patch paths use camelCase SCIM attribute names; `password-policy put` also works but replaces the whole object and therefore needs every required attribute restated). Do this for every policy in the domain, not only the default one, and repeat per identity domain. Where a lower-length policy exists solely for a legacy application, retire the application rather than the length requirement.",
      "caveat": "Four false-pass traps. (1) A domain can hold several password policies ordered by `priority` and bound to groups; the policy that actually applies to a given user is the highest-priority matching one, so inspecting only the policy named Default can pass a tenancy whose real users are governed by a weaker one. (2) Identity domains are compartment-scoped resources — a domain created in a child compartment is invisible to `oci iam domain list -c <tenancy_ocid>` alone, so the subtree walk is mandatory, and that walk requires `--include-root` written as a bare switch because `--include-root true` aborts the command and can leave an automated collector with an empty domain list that scores as compliant. (3) The list verb is plural (`password-policies list`); the singular form errors out, and a script that swallows the non-zero exit reads 'no policies' as 'no violations'. (4) Federated users authenticate at the external IdP and are not governed by the domain password policy at all; their length requirement must be evidenced at the IdP and is out of scope for this API check."
    },
    {
      "control_id": "1.5",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Force periodic password rotation with an expiry no longer than one year",
      "objective": "A local account password that never expires stays valid indefinitely after a breach, a device compromise or an employee departure that HR offboarding missed. Bounding the lifetime of every locally managed password limits how long a silently stolen credential remains usable.",
      "procedure": "Walk the compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`, with `--include-root` as a valueless switch), collect every identity domain URL with `oci iam domain list --compartment-id <id> --all`, then for each domain run `oci identity-domains password-policies list --endpoint <domain_url> --all` (plural for list operations) and read `password-expires-after` (days) on each entry of `data.resources[]` alongside `priority` and its group assignment. Determine, for each population of local users, which policy wins on priority. For legacy tenancies check `oci iam authentication-policy get --compartment-id <tenancy_ocid>`, which exposes no expiry field and therefore cannot satisfy this control by itself.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url}'",
        "oci identity-domains password-policies list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",expires:\"password-expires-after\",priority:priority,groups:groups}'",
        "oci identity-domains password-policy get --endpoint <domain_url> --password-policy-id <policy_id>",
        "oci iam authentication-policy get --compartment-id <tenancy_ocid>"
      ],
      "evidence": "The `password-expires-after` value of each PasswordPolicy in `data.resources[]` from `oci identity-domains password-policies list --endpoint <domain_url>`, captured with the policy `display-name`, `priority` and the domain OCID, plus the list of groups the policy is scoped to.",
      "verdict_rule": "PASS when every password policy that can apply to a local (non-federated) user sets password-expires-after to a value greater than 0 and less than or equal to 365 days; a null, absent or 0 value (never expires) is a FAIL. A policy assigned to no groups is recorded as ineffective and cannot be used to satisfy the rule for any user population.",
      "remediation": "Set an expiry on each policy in each domain: `oci identity-domains password-policy patch --endpoint <domain_url> --password-policy-id <id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations '[{\"op\":\"replace\",\"path\":\"passwordExpiresAfter\",\"value\":365},{\"op\":\"replace\",\"path\":\"passwordExpireWarning\",\"value\":15}]'`. Pair the expiry with a warning window so users rotate before lockout, and confirm that service accounts using passwords are migrated to API keys or instance principals so that expiry does not break automation.",
      "caveat": "Marked Manual: the API returns the setting on each policy but not which policy governs which human. A person must decide whether every population of local accounts is actually covered — including accounts in secondary identity domains, accounts exempted by a higher-priority policy bound to a group, and break-glass accounts that teams often deliberately exclude. Command-level traps that produce a silent empty result set (and therefore a vacuous pass): `--include-root` must be a bare switch, and the list verb must be the plural `password-policies list`. The API also cannot settle the federated case: for IdP-managed users the OCI value is irrelevant and the effective expiry lives in the external directory, which must be evidenced separately. Finally, a policy with a compliant expiry that is assigned to no groups changes nothing and still reads as compliant field-by-field."
    },
    {
      "control_id": "1.6",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Block reuse of recent passwords by keeping a password history in each domain policy",
      "objective": "Expiry only helps if users cannot cycle straight back to the password an attacker already holds. Retaining a history of previous passwords and refusing to accept them forces genuinely new secrets at each rotation, which is what makes the expiry control in 1.5 meaningful.",
      "procedure": "For every identity domain found by walking the compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`, bare `--include-root`) and calling `oci iam domain list --compartment-id <id> --all`, run `oci identity-domains password-policies list --endpoint <domain_url> --all` and read `num-passwords-in-history` on each entry of `data.resources[]`, together with `priority` and the groups it is assigned to. Confirm the value on every policy, not just the highest-priority one, since a lower-priority policy still governs the users it matches. Legacy tenancies expose no equivalent field on `oci iam authentication-policy get`.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url}'",
        "oci identity-domains password-policies list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",history:\"num-passwords-in-history\",age:\"min-password-age\",priority:priority,groups:groups}'",
        "oci identity-domains password-policy get --endpoint <domain_url> --password-policy-id <policy_id>"
      ],
      "evidence": "The `num-passwords-in-history` attribute of every PasswordPolicy in `data.resources[]` returned by `oci identity-domains password-policies list --endpoint <domain_url>`, recorded per policy `display-name`, `priority`, assigned groups and domain OCID.",
      "verdict_rule": "PASS when every password policy that can apply to a local user reports num-passwords-in-history >= 24; a null, absent, or 0 value is a FAIL, and any value between 1 and 23 is a FAIL for this rule. A policy bound to no groups is recorded as ineffective and does not satisfy the rule for any population.",
      "remediation": "Raise the history depth on each policy: `oci identity-domains password-policy patch --endpoint <domain_url> --password-policy-id <id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations '[{\"op\":\"replace\",\"path\":\"numPasswordsInHistory\",\"value\":24}]'`, repeating for every policy in every identity domain. Combine with a minimum password age (`minPasswordAge`) so a user cannot burn through the history in a single sitting to return to a favoured password.",
      "caveat": "Marked Manual: the assessor must judge whether the configured depth is meaningful for the population it governs, and whether every population is governed at all. Automated traps: policies in secondary identity domains living in child compartments are missed unless the compartment tree is walked with `--include-root` as a valueless switch (passing it a value aborts the command); the list verb is the plural `password-policies list`, and the singular form errors out so a script that ignores the exit status sees zero policies and reports no violations; a policy carrying a compliant history value but assigned to zero groups is cosmetic; and a high-priority weaker policy bound to a broad group overrides compliant lower-priority ones for most users. Federated users never touch this setting — their reuse control sits in the external IdP and cannot be evidenced from OCI. History depth also does not prevent trivially incremented passwords."
    },
    {
      "control_id": "1.7",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Require a second authentication factor for every account that can sign in to the Console",
      "objective": "A Console password alone is phishable and reusable. Every account capable of interactive sign-in must present a second factor, so that a stolen or guessed password does not by itself yield an authenticated session with whatever OCI authority that account holds.",
      "procedure": "Determine the home region with `oci iam region-subscription list`. Walk the compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`, bare `--include-root`) and collect every domain URL via `oci iam domain list --compartment-id <id> --all`. For each domain run `oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all` and read, per user in `data.resources[]`: `active`, the capabilities extension flag for console password use, the user extension flag indicating a federated account, and the MFA extension `mfaStatus`. Then enumerate sign-on policies and rules with the plural list verbs `oci identity-domains policies list --endpoint <domain_url> --all` and `oci identity-domains rules list --endpoint <domain_url> --all`. Order the SignOn rules by `priority`, take the first enabled rule whose condition group evaluates true for the all-users case, and inspect its `return`/action values to establish whether a second factor is demanded.",
      "api": [
        "oci iam region-subscription list --query 'data[].{region:\"region-name\",home:\"is-home-region\"}'",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url}'",
        "oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all",
        "oci identity-domains policies list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",type:\"policy-type\",active:active,rules:rules}'",
        "oci identity-domains rules list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",priority:priority,active:active,cond:condition,ret:return}'"
      ],
      "evidence": "Per user from `oci identity-domains users list`: `active`, `urn:ietf:params:scim:schemas:oracle:idcs:extension:capabilities:User.canUseConsolePassword`, `...:extension:user:User.isFederatedUser` and `...:extension:mfa:User.mfaStatus` (the CLI kebab-cases sub-attributes in output, e.g. `\"...:extension:mfa:User\".\"mfa-status\"`). Plus, from `oci identity-domains policies list` and `oci identity-domains rules list`, the SignOn policy name, each rule's `priority`, `active` flag, `condition`/condition-group and its `return` action values, and the app the policy is attached to.",
      "verdict_rule": "PASS when both conjuncts hold. (a) Every ACTIVE non-federated user whose canUseConsolePassword capability is true reports mfaStatus ENROLLED. (b) In each identity domain, walking that domain's SignOn policy rules in ascending `priority` order, the first enabled rule whose condition group matches the all-users case has an action requiring a second factor; if that first matching rule allows authentication without a factor, the control FAILs regardless of enrolment. A domain whose SignOn policy is not attached to the Console application is a FAIL for (b).",
      "remediation": "Create or edit the domain's sign-on policy so the highest-priority rule matching all users has an action requiring multi-factor authentication, confirm the policy is attached to the Console application, and have each enrolled user register a factor. For accounts that exist only for API use, clear the console-password capability instead (`oci iam user update-user-capabilities --user-id <ocid> --can-use-console-password false`, or the domain-side capabilities patch) so they cannot sign in interactively at all.",
      "caveat": "The legacy `is-mfa-activated` field on `oci iam user list` is a false-pass and false-fail generator: it only reflects the Default identity domain, ignores every secondary domain entirely, and historically tracks only the legacy TOTP factor, so a user secured with a FIDO security key can report false — do not use it as evidence. The inverse trap is worse: mfaStatus ENROLLED proves a factor was registered, not that it is ever demanded, so the rule check is not optional, and it must be resolved by `priority` order with the first matching enabled rule winning rather than by 'some rule somewhere requires MFA' — an MFA rule sitting below a broader allow-without-MFA rule never fires. A sign-on policy that requires MFA but is not attached to the Console application likewise protects nothing. The list verbs are plural (`policies list`, `rules list`); the singular forms do not exist and their error exit can be mistaken for an empty, compliant result. Federated users are challenged by the external IdP, so their OCI MFA fields are meaningless and must be evidenced at the IdP."
    },
    {
      "control_id": "1.8",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "No API signing key stays in service past a 90-day lifetime",
      "objective": "An API signing key is a long-lived bearer credential: anyone holding the private half can call OCI as that user from anywhere, with no second factor and no session expiry. Bounding key lifetime bounds how long an undetected key theft remains exploitable.",
      "procedure": "Work against the home region resolved from `oci iam region-subscription list`. Enumerate users in every identity domain: walk the compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`), resolve each domain URL with `oci iam domain list --compartment-id <id> --all`, then `oci identity-domains users list --endpoint <domain_url> --all`; for the Default domain also `oci iam user list --compartment-id <tenancy_ocid> --all`. For each user OCID run `oci iam user api-key list --user-id <ocid>`, or list a whole domain in one call with the plural verb `oci identity-domains api-keys list --endpoint <domain_url> --all`. Read `fingerprint` as the key identifier — `key-id` is the composite `<tenancy_ocid>/<user_ocid>/<fingerprint>` string, not the fingerprint — and compute each key's age from its creation timestamp against the assessment date.",
      "api": [
        "oci iam region-subscription list --query \"data[?\\\"is-home-region\\\"].\\\"region-name\\\"\" --raw-output",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam user api-key list --user-id <user_ocid> --query 'data[].{fp:fingerprint,keyid:\"key-id\",created:\"time-created\",state:\"lifecycle-state\"}'",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains api-keys list --endpoint <domain_url> --all --query 'data.resources[].{fp:fingerprint,user:user,created:\"meta\".\"created\"}'"
      ],
      "evidence": "For each key: `fingerprint` (the identifier the delete command consumes), optionally `key-id` for the composite form, `time-created` (legacy) or `meta.created` (domain SCIM), `lifecycle-state`, and the owning user OCID, as returned by `oci iam user api-key list --user-id` or `oci identity-domains api-keys list`.",
      "verdict_rule": "PASS when every API key in ACTIVE state, across every user in every identity domain, has a creation timestamp no more than 90 days before the assessment timestamp. A run that enumerated users only through legacy `oci iam user list` is inconclusive, not a PASS, because it covers the Default domain only.",
      "remediation": "Generate a fresh key pair, upload the new public key with `oci iam user api-key upload --user-id <ocid> --key-file <new_public.pem>`, roll the consumers over to the new fingerprint, then delete the old one with `oci iam user api-key delete --user-id <ocid> --fingerprint <old_fingerprint>` (domain equivalent: `oci identity-domains api-key delete --endpoint <domain_url> --api-key-id <id>`). Automate the cycle on a schedule under 90 days, and prefer instance or resource principals for workloads so no key exists to rotate.",
      "caveat": "There is no rotation timestamp in OCI — rotation is delete-and-recreate, so creation time is the only truth, and a key re-uploaded with the same fingerprint still reads as new. Three collection traps. (1) `key-id` is not the fingerprint: it is `<tenancy>/<user>/<fingerprint>`, so keying evidence on it produces an identifier that `api-key delete --fingerprint` will not accept and that will not join to audit records. (2) Scope: `oci iam user list -c <tenancy_ocid>` returns only Default-domain principals, so every key held by a user in a secondary identity domain goes uncounted unless you enumerate domains across the whole compartment subtree (with `--include-root` as a bare switch) and query each domain endpoint with the plural `api-keys list`. (3) Users are global/home-region objects, so do not loop regions, but do run the read against the home region. Keys on deprovisioned users still authenticate until deleted."
    },
    {
      "control_id": "1.9",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Replace S3-compatible customer secret keys at least every 90 days",
      "objective": "Customer secret keys are static access-key/secret pairs used by the Object Storage S3-compatibility and Swift endpoints. They are frequently embedded in backup jobs and third-party appliances, where they leak into config files and images, so their useful lifetime must be capped.",
      "procedure": "Against the home region, enumerate every user in every identity domain: walk compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (`--include-root` takes no value), get domain URLs with `oci iam domain list --compartment-id <id> --all`, list users per domain with `oci identity-domains users list --endpoint <domain_url> --all`, plus `oci iam user list --compartment-id <tenancy_ocid> --all` for the Default domain. For each user run `oci iam customer-secret-key list --user-id <ocid>`, or take the whole domain at once with the plural verb `oci identity-domains customer-secret-keys list --endpoint <domain_url> --all`. Age each key from its creation timestamp against the assessment date.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam customer-secret-key list --user-id <user_ocid> --query 'data[].{id:id,name:\"display-name\",created:\"time-created\",state:\"lifecycle-state\"}'",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains customer-secret-keys list --endpoint <domain_url> --all --query 'data.resources[].{id:id,user:user,created:\"meta\".\"created\"}'",
        "oci iam region-subscription list --query \"data[?\\\"is-home-region\\\"].\\\"region-name\\\"\" --raw-output"
      ],
      "evidence": "The `time-created` / `meta.created`, `time-expires` where present, and `lifecycle-state` of each customer secret key, keyed by secret-key OCID (or SCIM id), `display-name` and owning user OCID, from `oci iam customer-secret-key list --user-id` or `oci identity-domains customer-secret-keys list`.",
      "verdict_rule": "PASS when every customer secret key in ACTIVE state, across every user in every identity domain, has a creation timestamp no more than 90 days before the assessment timestamp. A run whose user inventory came only from legacy `oci iam user list` is inconclusive, not a PASS.",
      "remediation": "Create the replacement first with `oci iam customer-secret-key create --user-id <ocid> --display-name <name>`, capture the secret shown once, repoint the S3/Swift clients, then delete the superseded key with `oci iam customer-secret-key delete --user-id <ocid> --customer-secret-key-id <id>`. Where the consumer runs on OCI compute, migrate it to native Object Storage APIs with instance principals so no static pair is needed.",
      "caveat": "The API never returns the secret half and exposes no last-used field, so age is the only signal available here — proving a stale key is unused requires correlating Object Storage access logs, which is control 1.16's job, not this one, and note that S3-compatible traffic is data-plane, so it does not appear in the audit stream unless bucket read/write logs are enabled. As with API keys, enumerating users only through legacy `oci iam user list` silently omits every secondary identity domain and yields a false pass, and the domain-side list verb is the plural `customer-secret-keys list` — the singular form errors, which an unchecked script reads as 'no keys'. The compartment walk needs `--include-root` written as a bare switch; `--include-root true` aborts it. Finally, these keys authenticate against the regional S3-compatibility endpoint of the Object Storage namespace, so a key created for one region works tenancy-wide: deleting it once is sufficient, but scoping the assessment per-region is not."
    },
    {
      "control_id": "1.10",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Auth tokens are reissued before they reach 90 days old",
      "objective": "Auth tokens are Oracle-generated passwords used by services that cannot perform request signing — Container Registry logins, Git clients for DevOps repositories, and Swift/RMAN backups to Object Storage. They are pasted into CI configuration and developer machines, so a fixed maximum lifetime limits exposure from leaked pipeline definitions. Note that Email Delivery uses a different object entirely (SMTP credentials, listed with `oci iam smtp-credential list --user-id <ocid>` or the domain-side `smtp-credentials list`), which this control does not enumerate and which must be aged separately.",
      "procedure": "Against the home region, build the full user inventory: walk the compartment tree with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (valueless `--include-root`), resolve each identity domain URL with `oci iam domain list --compartment-id <id> --all`, list users per domain with `oci identity-domains users list --endpoint <domain_url> --all`, and include `oci iam user list --compartment-id <tenancy_ocid> --all`. For each user run `oci iam auth-token list --user-id <ocid>`, or enumerate the whole domain with the plural verb `oci identity-domains auth-tokens list --endpoint <domain_url> --all`. Compare each token's creation timestamp to the assessment date.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam auth-token list --user-id <user_ocid> --query 'data[].{id:id,desc:description,created:\"time-created\",state:\"lifecycle-state\"}'",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains auth-tokens list --endpoint <domain_url> --all --query 'data.resources[].{id:id,user:user,created:\"meta\".\"created\"}'",
        "oci iam smtp-credential list --user-id <user_ocid> --query 'data[].{id:id,created:\"time-created\"}'  # separate credential type, assessed alongside"
      ],
      "evidence": "The `time-created` / `meta.created`, `lifecycle-state` and description of each auth token, keyed by token OCID (or SCIM id) and owning user OCID, from `oci iam auth-token list --user-id` or `oci identity-domains auth-tokens list`.",
      "verdict_rule": "PASS when every auth token in ACTIVE state, across every user in every identity domain, has a creation timestamp no more than 90 days before the assessment timestamp. Every token on a user is assessed, not just the most recent one; a run limited to legacy `oci iam user list` is inconclusive, not a PASS.",
      "remediation": "Issue the replacement with `oci iam auth-token create --user-id <ocid> --description <purpose>`, store the value shown once in a Vault secret rather than in pipeline files, update the consumers, then delete the old token with `oci iam auth-token delete --user-id <ocid> --auth-token-id <id>`. Each user is capped at two tokens, which is exactly enough for a make-before-break rotation, so the deletion must be the final scripted step rather than a manual follow-up.",
      "caveat": "Because a user may hold at most two auth tokens, teams often keep a permanent spare that never gets deleted after a rotation — it reads as a valid credential and is the most common finding here, so assess every token, not the newest one. Enumerating users only via legacy `oci iam user list` misses secondary identity domains entirely, and the domain-side list verb is the plural `auth-tokens list`; the singular form does not exist and its error can be swallowed as an empty, compliant result. The compartment walk needs `--include-root` as a bare switch. Do not assume this control covers Email Delivery: SMTP credentials are a distinct object that none of these commands return, so a tenancy with ancient SMTP credentials passes this control cleanly. Tokens also survive deactivation of the console password: disabling interactive login does not revoke them, so an offboarded user whose password was removed may still be able to push to Container Registry until the token itself is deleted."
    },
    {
      "control_id": "1.11",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Replace IAM database credentials at least every 90 days",
      "objective": "IAM database passwords let an OCI principal authenticate directly to Autonomous Database and other managed databases, bridging cloud identity into data-plane access. A stale one is a long-lived path to customer data that never appears in database-side user administration, so its lifetime must be bounded.",
      "procedure": "Against the home region, inventory users across all identity domains: `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (`--include-root` is valueless), then `oci iam domain list --compartment-id <id> --all`, then `oci identity-domains users list --endpoint <domain_url> --all` per domain, plus `oci iam user list --compartment-id <tenancy_ocid> --all`. For each user run `oci iam db-credential list --user-id <ocid>`, or take the domain in one call with the plural verb `oci identity-domains user-db-credentials list --endpoint <domain_url> --all`. Record each credential's creation timestamp and its expiry, then age it against the assessment date.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam db-credential list --user-id <user_ocid> --query 'data[].{id:id,created:\"time-created\",expires:\"time-expires\",state:\"lifecycle-state\"}'",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains user-db-credentials list --endpoint <domain_url> --all --query 'data.resources[].{id:id,user:user,created:\"meta\".\"created\",expires:\"expires-on\"}'",
        "oci iam db-credential delete --user-id <user_ocid> --db-credential-id <db_credential_ocid>"
      ],
      "evidence": "The `time-created`, `time-expires` and `lifecycle-state` of each DB credential from `oci iam db-credential list --user-id <ocid>`, keyed by credential OCID and owning user OCID (SCIM equivalents from `oci identity-domains user-db-credentials list`: `meta.created`, `expires-on`).",
      "verdict_rule": "PASS when every DB credential in ACTIVE state, across every user in every identity domain, has a creation timestamp no more than 90 days before the assessment timestamp and is not already past its expiry while still ACTIVE. A user inventory drawn only from legacy `oci iam user list` is inconclusive, not a PASS.",
      "remediation": "Rotate by delete-and-recreate on a schedule under 90 days: create the replacement with `oci iam db-credential create --user-id <ocid> --password <value> --description <purpose>`, repoint the database clients, then delete the superseded credential with `oci iam db-credential delete --user-id <ocid> --db-credential-id <id>`. Do not attempt to set a lifetime on the legacy create call — it has no expiry parameter and `time-expires` is service-populated. In an identity-domain tenancy the expiry is writable on the SCIM object instead: create or replace the UserDbCredential through `oci identity-domains user-db-credentials create` / `oci identity-domains user-db-credential put` with `expiresOn` set. Prefer database resource principals or Vault-brokered secrets for application connections.",
      "caveat": "Marked Manual because IAM only shows that a credential object exists — it cannot show whether any database actually trusts it. The IAM-to-database mapping is configured on the database side (the global/shared schema mapping in Autonomous Database), so a human must confirm, per database, whether an aged credential still grants a working login and what schema privileges it lands on; an unmapped credential is dormant while a mapped one is a direct data path. Second trap: an ACTIVE credential whose `time-expires` has already passed is refused by the database but still listed by IAM, so counting objects overstates the finding unless expiry is evaluated too. Third: the remediation must not promise an expiry the legacy API cannot set — `db-credential create` exposes no expiry parameter, and only the identity-domains UserDbCredential object accepts `expiresOn`, so a runbook that tells an operator to 'set a 90-day expiry' on the legacy path silently leaves the default in place. Secondary-domain users are missed by legacy user enumeration, and these credentials produce no OCI control-plane audit events, so idleness for them must come from database-side audit (see 1.16)."
    },
    {
      "control_id": "1.12",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Deny long-lived signing keys to accounts holding tenancy administrator rights",
      "objective": "Administrator accounts should only be usable interactively, behind MFA. An API signing key attached to such an account is an unattended, non-MFA, non-expiring path to full tenancy control that survives password rotation and is invisible to sign-on policy, which is precisely the combination an attacker wants.",
      "procedure": "Resolve the Administrators group OCID from `oci iam group list --compartment-id <tenancy_ocid> --all`, then list its members with `oci iam group list-users --group-id <ocid> --all`. In every identity domain, repeat with the domain's own Administrators group: `oci identity-domains groups list --endpoint <domain_url> --all` to find it, then `oci identity-domains group get --endpoint <domain_url> --group-id <id> --attributes members`, because membership is not returned by default and `--attribute-sets all` alone does not populate it. Extend the set with members of any group that policy analysis (control 1.2) showed holds `manage all-resources in tenancy`, and with holders of domain administrator application roles from `oci identity-domains grants list --endpoint <domain_url> --all`. For every user in that combined set run `oci iam user api-key list --user-id <ocid>` (or the domain-side `oci identity-domains api-keys list --endpoint <domain_url> --all` filtered to that user) and confirm the result is empty.",
      "api": [
        "oci iam group list --compartment-id <tenancy_ocid> --all --query \"data[?name=='Administrators'].id\" --raw-output",
        "oci iam group list-users --group-id <administrators_group_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci iam user api-key list --user-id <user_ocid> --query 'data[].{fp:fingerprint,created:\"time-created\",state:\"lifecycle-state\"}'",
        "oci identity-domains groups list --endpoint <domain_url> --all --query 'data.resources[].{id:id,name:\"display-name\"}'",
        "oci identity-domains group get --endpoint <domain_url> --group-id <group_id> --attributes members",
        "oci identity-domains grants list --endpoint <domain_url> --all",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'"
      ],
      "evidence": "An empty (or non-empty) `data` array from `oci iam user api-key list --user-id <admin_user_ocid>`, with each key identified by its `fingerprint` and `time-created`; paired with the membership listing that establishes the user is an administrator — `oci iam group list-users --group-id <administrators_group_ocid>`, the `members` attribute from `oci identity-domains group get --attributes members`, the qualifying statement text from `oci iam policy get`, and the admin role grants from `oci identity-domains grants list`.",
      "verdict_rule": "PASS when every user who is a member of any Administrators group in any identity domain, of any group granted manage all-resources in tenancy, or holding a domain administrator application role, returns zero ACTIVE API keys. A membership enumeration that did not request the `members` attribute, or that covered only the Default domain, is inconclusive and may not be recorded as PASS.",
      "remediation": "Delete the key with `oci iam user api-key delete --user-id <ocid> --fingerprint <fingerprint>` (domain equivalent: `oci identity-domains api-key delete --endpoint <domain_url> --api-key-id <id>`). Move the automation that used it to a dedicated non-admin service user whose policy grants only the permissions the job needs, or better, to an instance/resource principal via a dynamic group. Keep human administrators to Console access with MFA, and give break-glass accounts no programmatic credentials at all.",
      "caveat": "Checking only the group literally named Administrators is the classic false pass. Effective tenancy administrators also include any group a policy grants manage all-resources in tenancy (control 1.2 output feeds this check), dynamic groups with equivalent grants, and identity-domain principals holding the Identity Domain Administrator or Security Administrator application role, none of which appear in the Default-domain Administrators membership list. Each identity domain has its own Administrators group, so a multi-domain tenancy has several sets to enumerate — and the SCIM `members` attribute is not returned unless explicitly requested, so a collector that reads `groups list` output alone sees an empty membership and reports zero admins with keys. Use `fingerprint`, not `key-id`, as the key identifier: `key-id` is the composite tenancy/user/fingerprint string and will not be accepted by the delete command. Finally, a key on a federated admin is equally dangerous and equally invisible to legacy user listing."
    },
    {
      "control_id": "1.13",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Attach a verified, monitored mailbox to every locally managed IAM account",
      "objective": "Password resets, MFA enrolment, expiry warnings and security notifications are all delivered by email. An account whose address is missing, unverified, or points at a departed employee or a shared alias nobody reads cannot be recovered safely and cannot be warned when it is abused.",
      "procedure": "Walk the compartment tree with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all` (`--include-root` is a valueless switch; passing it a value aborts the command), resolve every identity domain URL with `oci iam domain list --compartment-id <id> --all`, then run `oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all` and inspect each user's `emails` array for an entry with `primary` true, checking its `value` and `verified` flags plus `active` and the federated-user flag. For the Default domain the legacy equivalent is `oci iam user list --compartment-id <tenancy_ocid> --all`, exposing `email` and `email-verified`. Then review each address against the current employee/contractor roster.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].{name:\"display-name\",url:url}'",
        "oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all --query 'data.resources[].{name:\"user-name\",active:active,emails:emails}'",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].{name:name,email:email,verified:\"email-verified\"}'",
        "oci iam user get --user-id <user_ocid>",
        "oci identity-domains user get --endpoint <domain_url> --user-id <user_id> --attribute-sets all"
      ],
      "evidence": "Per user: the `emails[?primary]` entry's `value` and `verified` attributes from `oci identity-domains users list --endpoint <domain_url> --attribute-sets all` (within `data.resources[]`), or `data[].email` / `data[].email-verified` from `oci iam user list`, keyed by user OCID and user name, alongside `active` and the federated flag.",
      "verdict_rule": "PASS when every ACTIVE non-federated user has a primary email present and `verified` true, and the assessor has confirmed each address resolves to a mailbox owned by a current, identifiable owner. A user set drawn only from legacy `oci iam user list`, or a compartment walk that failed on the `--include-root` flag, is inconclusive rather than a PASS.",
      "remediation": "Set or correct the address with `oci iam user update --user-id <ocid> --email <address>`, or in an identity domain with `oci identity-domains user patch --endpoint <domain_url> --user-id <id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations '[{\"op\":\"replace\",\"path\":\"emails\",\"value\":[{\"value\":\"<address>\",\"type\":\"recovery\",\"primary\":true}]}]'`, then drive the verification mail to completion. Point service and shared accounts at a monitored team distribution list rather than an individual, and add an offboarding step that reassigns or deactivates any account whose owner leaves.",
      "caveat": "Marked Manual because validity is a human fact, not an API field: the platform can prove an address was verified at some past moment, never that the mailbox is still monitored, still belongs to the named person, or is not a former employee's forwarded alias. A verified flag set two years ago is the most common false pass. Automated traps: legacy `oci iam user list` covers only the Default identity domain, so secondary-domain users are unassessed, and the domain discovery that would fix that depends on a compartment walk whose `--include-root` must be written without a value — the valued form errors out and an unchecked collector then reports zero domains and zero exceptions. Federated users often carry no OCI-side address at all because the attribute is mastered at the IdP; treating that null as a failure produces noise, while treating it as a pass hides genuinely unmanaged local accounts, so classify on the federated flag rather than on the null itself."
    },
    {
      "control_id": "1.14",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Manual",
      "service": "iam",
      "title": "Have workloads call OCI with instance or resource principals rather than embedded user keys",
      "objective": "When compute instances, Functions and managed databases authenticate as an IAM user with a stored private key, that key sits on disk or in an environment variable, survives image cloning and backup, and cannot be revoked by terminating the host. Principal-based authentication issues short-lived, automatically rotated credentials bound to the resource itself.",
      "procedure": "List dynamic groups with `oci iam dynamic-group list --compartment-id <tenancy_ocid> --all`, or per domain with the plural verb `oci identity-domains dynamic-resource-groups list --endpoint <domain_url> --all`, and record each `matching-rule`. Pull all policy statements across the compartment tree and select those whose subject is a dynamic group (remember the domain-qualified form `dynamic-group '<Domain>'/'<Name>'`). Inventory the workloads that call OCI with `oci search resource structured-search --query-text \"query instance, functionsfunction, autonomousdatabase resources\" --region <region>` for every subscribed region — the type name for a Function is `functionsfunction`, not `function`; the literal `function` is not an indexed Resource Search type, so a query using it either errors or silently returns no Functions at all. Confirm the exact type names with `oci search resource-type list` before pinning the query, and add `functionsapplication` if you want the parent objects. Map each workload to a matching rule, and separately flag non-human IAM users holding API keys.",
      "api": [
        "oci iam region-subscription list --query 'data[].\"region-name\"' --raw-output",
        "oci iam dynamic-group list --compartment-id <tenancy_ocid> --all --query 'data[].{name:name,rule:\"matching-rule\"}'",
        "oci identity-domains dynamic-resource-groups list --endpoint <domain_url> --all --query 'data.resources[].{name:\"display-name\",rule:\"matching-rule\"}'",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'",
        "oci search resource-type list --region <region> --query 'data.items[].name'",
        "oci search resource structured-search --query-text \"query instance, functionsfunction, autonomousdatabase resources\" --region <region>",
        "oci iam user api-key list --user-id <service_user_ocid> --query 'data[].{fp:fingerprint,created:\"time-created\"}'"
      ],
      "evidence": "The `matching-rule` of each dynamic group from `oci iam dynamic-group list` / `oci identity-domains dynamic-resource-groups list`, the dynamic-group-subject statements from `oci iam policy get --policy-id`, the per-region workload inventory (`identifier`, `resource-type`, `compartment-id`) from `oci search resource structured-search`, and the API-key listing (`fingerprint`, `time-created`) for each suspected service user from `oci iam user api-key list --user-id`.",
      "verdict_rule": "PASS when every workload in the per-region inventory that calls OCI APIs is covered by a dynamic group matching rule plus a policy granting it the access it needs, and no IAM user representing that workload holds an API key. An inventory built from a query containing an invalid resource type, or run in only one region, is inconclusive and may not be recorded as PASS.",
      "remediation": "Create a dynamic group whose matching rule selects the workload (for example by instance OCID, compartment OCID, or resource type) and grant it access with `allow dynamic-group <name> to <verb> <resource> in compartment <name>`, using the domain-qualified subject form where the dynamic group lives outside the Default domain. Change the application to use the SDK's instance-principal or resource-principal provider, then delete the user's API key with `oci iam user api-key delete --user-id <ocid> --fingerprint <fp>` and, where the user existed only for that workload, delete the user.",
      "caveat": "Marked Manual because no OCI API can see inside the workload. The control plane shows that a dynamic group and policy exist; it cannot show whether the application actually uses the instance-principal provider or is still reading a private key from ~/.oci/config, a Kubernetes secret, or a Function configuration variable. Proving compliance requires host or source inspection. Two enumeration traps make the inventory silently short: the Resource Search type for Functions is `functionsfunction` (a query written with `function` returns no Functions, so the very resource class this control names disappears from the denominator and the check passes), and compute, Functions and databases are regional, so the inventory must loop every subscribed region while dynamic groups themselves are global. The domain-side list verb is the plural `dynamic-resource-groups list`. The mirror-image false pass: a service user holding an API key may be legitimate if the caller runs outside OCI, where instance principals are unavailable."
    },
    {
      "control_id": "1.15",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 2,
      "assessment": "Manual",
      "service": "iam",
      "title": "Withhold delete permissions from delegated storage administrator groups",
      "objective": "A storage administrator needs to create and configure buckets, volumes and file systems, but granting the full manage verb also grants destruction of the data and of its backups. Excluding the delete permissions from the grant means a compromised or mistaken storage admin cannot destroy the estate they operate, and keeps deletion as a separate, deliberately privileged act.",
      "procedure": "Enumerate all ACTIVE compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` is a valueless switch — the valued form aborts the walk and leaves child-compartment policies unread). List policies in each compartment and read `statements` via `oci iam policy get --policy-id`. Select statements granting manage on `object-family`, `volume-family`, `file-family` (or the individual types buckets, objects, volumes, volume-backups, boot-volumes, file-systems, mount-targets) to any subject other than Administrators. For each, verify a `where all { ... }` clause excludes the family's delete permissions by name, and record the exact permission strings excluded. Enumerate the full permission list for each family from the service's policy reference before judging completeness, then compute the effective union of grants per principal by joining group membership (`oci iam group list-users --group-id <ocid> --all`) to every statement naming those groups.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all --query 'data[].id' --raw-output",
        "oci iam policy list --compartment-id <compartment_ocid> --all",
        "oci iam policy get --policy-id <policy_ocid> --query 'data.statements'",
        "oci iam group list-users --group-id <group_ocid> --all --query 'data[].{id:id,name:name}'",
        "oci os bucket get --namespace <namespace> --bucket-name <bucket> --fields approximateCount,approximateSize",
        "oci os retention-rule list --namespace <namespace> --bucket-name <bucket>",
        "oci os bucket list --namespace <namespace> --compartment-id <compartment_ocid> --all --region <region>"
      ],
      "evidence": "The verbatim statement from `oci iam policy get --policy-id`, showing whether it carries conditions such as `request.permission != 'BUCKET_DELETE'`, `!= 'OBJECT_DELETE'`, `!= 'OBJECT_VERSION_DELETE'`, `!= 'VOLUME_DELETE'`, `!= 'VOLUME_BACKUP_DELETE'`, `!= 'BOOT_VOLUME_DELETE'`, `!= 'FILE_SYSTEM_DELETE'`, `!= 'MOUNT_TARGET_DELETE'`, keyed by policy OCID and compartment OCID; plus the group membership listing used to build the effective per-principal union, and bucket retention-rule state as compensating evidence.",
      "verdict_rule": "PASS when, for every non-Administrators principal, the union of all statements granting manage on a storage family carries conditions excluding every delete permission in that family — including the object-version and backup delete permissions, not only the parent object's — and no second statement or group membership restores deletion. A statement-by-statement evaluation that does not compute the per-principal union may not be recorded as PASS.",
      "remediation": "Rewrite the grant with explicit exclusions, for example `allow group StorageAdmins to manage object-family in compartment X where all {request.permission != 'BUCKET_DELETE', request.permission != 'OBJECT_DELETE', request.permission != 'OBJECT_VERSION_DELETE'}`, with the volume equivalents (`VOLUME_DELETE`, `VOLUME_BACKUP_DELETE`, `BOOT_VOLUME_DELETE`, `BOOT_VOLUME_BACKUP_DELETE`, `VOLUME_GROUP_DELETE`, `VOLUME_GROUP_BACKUP_DELETE`) and the file-storage equivalents (`FILE_SYSTEM_DELETE`, `MOUNT_TARGET_DELETE`). Back this with bucket versioning plus retention rules and volume backup policies so that even an authorised delete is recoverable.",
      "caveat": "Marked Manual because an assessor must judge completeness of the exclusion list, and an incomplete list is a silent false pass: excluding BUCKET_DELETE while leaving OBJECT_DELETE and OBJECT_VERSION_DELETE permitted still allows the contents to be destroyed, and excluding VOLUME_DELETE while leaving VOLUME_BACKUP_DELETE or BOOT_VOLUME_DELETE permitted destroys recoverability. Excluding the delete permissions is also not the same as preventing data loss — OBJECT_OVERWRITE (and unversioned buckets generally) lets the same principal replace object contents with no delete permission at all, so an exclusion list that stops at the DELETE strings should be paired with versioning and retention rules before it is called effective. The other trap is additive policy: OCI unions all grants, so a storage group that is also a member of a broader group, or is named in a second statement without conditions, regains delete — evaluate the effective union per principal, not each statement in isolation. Policies in child compartments must be walked, which requires `--include-root` written without a value."
    },
    {
      "control_id": "1.16",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Dormant credentials are switched off rather than left latent",
      "objective": "Dormant accounts and unused programmatic credentials expand the attack surface without serving anyone. They are rarely monitored, frequently belong to departed staff or retired automation, and are the credentials most likely to still be sitting in an old laptop image or repository. Deactivating them on a fixed idleness threshold shrinks that surface continuously.",
      "procedure": "Per identity domain (compartment walk with `--include-root` as a valueless switch, then `oci iam domain list --compartment-id <id> --all`), run `oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all` and read the userState extension's last successful login date plus `active`. For non-console credentials, query the audit stream in every subscribed region against the subtree pseudo-log, not the root-only one: `oci logging-search search-logs --region <region> --search-query 'search \"<tenancy_ocid>/_Audit_Include_Subcompartment\" | summarize count() by data.identity.principalName, data.identity.credentials' --time-start <T> --time-end <T+chunk> --limit 1000`. Logging Search caps a single query's time range and returns a bounded page count, so split the 45-day window into chunks and paginate with `--limit`/`--page` inside each chunk before aggregating.",
      "api": [
        "oci iam region-subscription list --query 'data[].\"region-name\"' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains users list --endpoint <domain_url> --all --attribute-sets all",
        "oci logging-search search-logs --region <region> --search-query 'search \"<tenancy_ocid>/_Audit_Include_Subcompartment\" | summarize count() by data.identity.principalName, data.identity.credentials' --time-start <iso8601> --time-end <iso8601> --limit 1000",
        "oci audit event list --compartment-id <compartment_ocid> --start-time <iso8601> --end-time <iso8601> --all --region <region>",
        "oci iam user api-key list --user-id <user_ocid> --query 'data[].{fp:fingerprint,created:\"time-created\"}'",
        "oci identity-domains user patch --endpoint <domain_url> --user-id <user_id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations file://deactivate.json"
      ],
      "evidence": "`urn:ietf:params:scim:schemas:oracle:idcs:extension:userState:User.lastSuccessfulLoginDate` and `active` per user from `oci identity-domains users list`; and the per-credential `data.identity.credentials` occurrence counts from `oci logging-search search-logs` over the `_Audit_Include_Subcompartment` stream of each subscribed region for the last 45 days, joined to the credential inventories from `oci iam user api-key list` (by `fingerprint`), `oci iam auth-token list` and `oci iam customer-secret-key list`.",
      "verdict_rule": "PASS when (a) every ACTIVE user holding the console-password capability has a last successful login within 45 days — a null last-login on an ACTIVE account is a FAIL, not an exemption — and (b) every ACTIVE API key, auth token and customer secret key appears at least once in the `_Audit_Include_Subcompartment` stream of at least one subscribed region within the same window. IAM database credentials are excluded from test (b): they authenticate to the database listener and emit no OCI control-plane event, so their idleness is evidenced from database-side audit (Autonomous Database unified audit / DBA_AUDIT_TRAIL) or declared not evidenceable from OCI and routed to the manual procedure in 1.11.",
      "remediation": "Deactivate an idle account by patching its `active` attribute to false in the identity domain: `oci identity-domains user patch --endpoint <domain_url> --user-id <id> --schemas '[\"urn:ietf:params:scim:api:messages:2.0:PatchOp\"]' --operations '[{\"op\":\"replace\",\"path\":\"active\",\"value\":false}]'` (for a legacy Default-domain user, remove credentials and group memberships instead). Reserve `oci iam user update-user-state --user-id <ocid> --blocked false` for clearing a lockout — it unblocks, it cannot deactivate. Then delete the idle credentials with the api-key, auth-token, customer-secret-key and db-credential delete commands. Automate this as a scheduled job driven by the audit query, and add an offboarding step that removes credentials on the day access ends.",
      "caveat": "Four false results dominate. (1) `search \"<tenancy_ocid>/_Audit\"` returns root-compartment events only, so every credential used exclusively against resources in child compartments — nearly all of them — shows zero hits and is scored idle, while a genuinely idle credential in a sub-compartment-only tenancy is indistinguishable from a used one; the subtree pseudo-log `_Audit_Include_Subcompartment` is mandatory. (2) The audit stream is regional: a key used only from one region produces no events elsewhere, so loop `oci iam region-subscription list` rather than trusting the home region. (3) A never-used account returns a null last-login date; treating null as absence-of-evidence rather than a fail passes brand-new and long-abandoned accounts alike. (4) Only control-plane activity lands in `_Audit` — Object Storage S3/Swift data-plane traffic appears only if bucket read/write logs are enabled, and IAM database credentials never appear at all, so scoring them by audit silence manufactures FAILs on credentials in daily use and destroys confidence in the whole check. Logging Search also bounds both the query time range and the records per request, so chunk the 45 days and paginate or you will under-count usage. Finally, `update-user-state --blocked false` only clears a lockout; using it as the deactivation step leaves the account fully active."
    },
    {
      "control_id": "1.17",
      "section": "1",
      "section_name": "Identity and Access Management",
      "level": 1,
      "assessment": "Automated",
      "service": "iam",
      "title": "Allow no more than one active signing key per IAM principal",
      "objective": "Each additional API key on a user is an independent, equally powerful credential that must be tracked, rotated and revoked. A second key that outlives a rotation is the copy nobody remembers — it keeps working after the 'current' key is replaced and is the credential most likely to remain valid on a decommissioned host.",
      "procedure": "Against the home region, enumerate identity domains across the compartment tree (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all`, bare `--include-root`; then `oci iam domain list --compartment-id <id> --all`) and, for each domain, list every signing key in one call with the plural verb `oci identity-domains api-keys list --endpoint <domain_url> --all`, grouping by the `user` reference on each SCIM ApiKey. For the Default domain the legacy path is `oci iam user list --compartment-id <tenancy_ocid> --all` followed by `oci iam user api-key list --user-id <ocid>` per user, grouping by `user-id`. Count keys in ACTIVE state per user and, for any user with more than one, record each key's `fingerprint` and creation timestamp — `key-id` is the composite `<tenancy_ocid>/<user_ocid>/<fingerprint>` string, not the fingerprint — so that a rotation in flight can be distinguished from a permanent second key.",
      "api": [
        "oci iam region-subscription list --query \"data[?\\\"is-home-region\\\"].\\\"region-name\\\"\" --raw-output",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --all --query 'data[].id' --raw-output",
        "oci iam domain list --compartment-id <compartment_ocid> --all --query 'data[].url' --raw-output",
        "oci identity-domains api-keys list --endpoint <domain_url> --all --query 'data.resources[].{fp:fingerprint,user:user,created:\"meta\".\"created\"}'",
        "oci iam user list --compartment-id <tenancy_ocid> --all --query 'data[].id' --raw-output",
        "oci iam user api-key list --user-id <user_ocid> --query 'data[].{fp:fingerprint,keyid:\"key-id\",created:\"time-created\",state:\"lifecycle-state\"}'",
        "oci raw-request --http-method GET --target-uri https://identity.<home_region>.<realm_domain>/20160918/users/<user_ocid>/apiKeys"
      ],
      "evidence": "A per-user count of ACTIVE API keys with each key's `fingerprint` and `time-created` (SCIM: `meta.created`), derived from `oci identity-domains api-keys list --endpoint <domain_url>` grouped by the `user` reference, or from `oci iam user api-key list --user-id <ocid>` grouped by `user-id`; `key-id` recorded only where the composite form is needed for cross-referencing.",
      "verdict_rule": "FAIL when any user holds more than one ACTIVE API key and the older key's creation timestamp is more than 24 hours before the newest key's creation timestamp; otherwise PASS. Counting is done per identity domain, over ACTIVE keys only. A run whose user set came solely from legacy `oci iam user list` is inconclusive rather than PASS, because it omits every secondary domain.",
      "remediation": "Decide which fingerprint is current, repoint every consumer to it, and delete the rest with `oci iam user api-key delete --user-id <ocid> --fingerprint <fp>` (domain equivalent: `oci identity-domains api-key delete --endpoint <domain_url> --api-key-id <id>`). If two keys are genuinely required for a make-before-break rotation, bound the overlap to the 24-hour tolerance the verdict allows and script the deletion of the superseded key as the final rotation step rather than leaving it for manual cleanup.",
      "caveat": "A momentary second key during rotation is legitimate, which is why the verdict carries an explicit 24-hour overlap tolerance rather than a bare count — a rule that says 'more than one key is a FAIL' while the guidance excuses rotations makes the outcome depend on which text the assessor follows. Report the `fingerprint`, never `key-id`: the latter is the composite tenancy/user/fingerprint string, so grouping or deleting by it fails and cross-referencing to audit records breaks. Counting must be done per domain — the same human present in two identity domains is two distinct principals each entitled to one key, and merging them mis-reports in either direction — and the domain-side list verb is the plural `api-keys list`. Legacy `oci iam user list` returns only Default-domain users, so secondary-domain principals with several keys go entirely uncounted, and the compartment walk that finds those domains needs `--include-root` written without a value. Count only ACTIVE keys, and note the raw-request host differs by realm (oraclecloud.com, oraclegovcloud.com, and so on)."
    },
    {
      "control_id": "2.1",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Automated",
      "service": "vcn",
      "title": "Block internet-wide SSH reachability in VCN security list ingress rules",
      "objective": "No VCN security list should let the whole IPv4 internet reach TCP/22, because a single permissive stateful rule exposes every instance in every subnet that list is attached to, turning SSH into a directly brute-forceable perimeter service.",
      "procedure": "Resolve the region set with `oci iam region-subscription list --tenancy-id <tenancy_ocid>` and repeat everything below per region. Enumerate security lists tenancy-wide with `oci search resource structured-search --query-text \"query SecurityList resources\" --region <region> --all`, having first confirmed the type name is indexed in this tenancy with `oci search resource-type list --region <region>`. Because Search returns only what the caller can inspect, reconcile the count against the authoritative fallback walk: `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` is a valueless flag — passing it a value aborts the command; `--access-level ANY` rather than ACCESSIBLE so compartments the principal only sees indirectly are not dropped) then `oci network security-list list --compartment-id <c> --all --region <region>` per compartment. For each identifier run `oci network security-list get --security-list-id <ocid>` and evaluate ingress-security-rules. A rule fails when source-type=CIDR_BLOCK AND source=0.0.0.0/0 AND protocol is \"6\" or \"all\" AND any of: tcp-options is absent; tcp-options is present but destination-port-range is null (a tcp-options object carrying only a source-port-range leaves every destination port open, 22 included); or destination-port-range.min <= 22 <= destination-port-range.max evaluated numerically.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource-type list --region <region>",
        "oci search resource structured-search --query-text \"query SecurityList resources\" --region <region> --all",
        "oci network security-list get --security-list-id <security_list_ocid> --region <region>",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all  # fallback walk; --include-root takes no value",
        "oci network security-list list --compartment-id <compartment_ocid> --all --region <region>  # fallback when Search is unavailable or the caller is under-privileged",
        "oci network security-list update --security-list-id <security_list_ocid> --ingress-security-rules file://rules.json --force"
      ],
      "evidence": "The full ingress-security-rules array of each `oci network security-list get` response, keyed by security list OCID, vcn-id and compartment-id, quoting the offending rule object verbatim: source-type, source, protocol, is-stateless, and the whole tcp-options object including the explicit null/absent state of destination-port-range (recording \"tcp-options present, destination-port-range null\" distinctly from \"tcp-options absent\"). Record alongside it the per-region Search result count and the per-region compartment-walk count so a permission-truncated enumeration is visible in the evidence itself.",
      "verdict_rule": "PASS when, across every subscribed region, no security list has an ingress rule where source-type=CIDR_BLOCK AND source=\"0.0.0.0/0\" AND protocol in {\"6\",\"all\"} AND (tcp-options is absent OR tcp-options.destination-port-range is null OR destination-port-range.min <= 22 <= destination-port-range.max by numeric comparison). An enumeration that Search truncated relative to the compartment walk is not a PASS — it is an inconclusive result reported as a finding about the assessing principal's permissions.",
      "remediation": "Rewrite the rule set with `oci network security-list update --security-list-id <ocid> --ingress-security-rules file://rules.json --force` (the update replaces the entire array and `--force` suppresses the overwrite prompt), replacing 0.0.0.0/0 with the operator CIDR and giving the rule an explicit tcp-options.destination-port-range rather than leaving it null, or delete the rule entirely and reach hosts through an OCI Bastion session or an on-prem-attached DRG/VPN path instead of a public SSH port.",
      "caveat": "Five false-pass traps. (1) Resource Search is a regional endpoint with an eventually-consistent index — a single-region run, or a run seconds after a change, silently misses lists; and Search returns only resources the caller holds inspect on, so an under-privileged auditor gets an artificially clean set unless the compartment-walk reconciliation above is performed. (2) A tcp-options object that carries only a source-port-range has a null destination-port-range and therefore opens ALL destination ports; code that treats \"tcp-options present\" as \"a port range was specified\" passes the most permissive rule in the tenancy. (3) protocol \"all\" carries no tcp-options at all, so code that requires a destination-port-range object skips it. (4) The control text names only 0.0.0.0/0, but an ingress rule sourced from ::/0 is equally open — report it as an accompanying observation. (5) A security list attached to zero subnets still fails — do not filter on attachment; conversely a clean security list says nothing about the NSGs bound to the same VNICs, which is control 2.3."
    },
    {
      "control_id": "2.2",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Automated",
      "service": "vcn",
      "title": "Block internet-wide RDP reachability in VCN security list ingress rules",
      "objective": "No VCN security list should let the whole IPv4 internet reach TCP/3389, since publicly reachable Remote Desktop is a standing credential-stuffing and pre-auth-CVE target for every Windows instance in the attached subnets.",
      "procedure": "Same traversal as 2.1 with the port changed. Per subscribed region (`oci iam region-subscription list --tenancy-id <tenancy_ocid>`), enumerate security lists via `oci search resource structured-search --query-text \"query SecurityList resources\" --region <region> --all`, and independently enumerate them through the fallback path — `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` then `oci network security-list list --compartment-id <c> --all --region <region>` — reconciling the two counts before trusting an empty finding set. Then `oci network security-list get --security-list-id <ocid>` for each. In ingress-security-rules flag any entry with source-type CIDR_BLOCK, source 0.0.0.0/0, protocol \"6\" or \"all\", and tcp-options absent, or tcp-options present with a null destination-port-range (all destination ports open), or a destination-port-range numerically spanning 3389. Record compartment-id and vcn-id from the same response for reporting.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource-type list --region <region>",
        "oci search resource structured-search --query-text \"query SecurityList resources\" --region <region> --all",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all  # fallback enumeration; --include-root takes no value",
        "oci network security-list list --compartment-id <compartment_ocid> --all --region <region>  # fallback enumeration, reconcile against Search",
        "oci network security-list get --security-list-id <security_list_ocid> --region <region>",
        "oci network security-list update --security-list-id <security_list_ocid> --ingress-security-rules file://rules.json --force"
      ],
      "evidence": "The offending ingress rule object inside `oci network security-list get` output — source-type, source, protocol, is-stateless and the complete tcp-options object with destination-port-range min/max or its explicit null — plus the security list OCID, its vcn-id and compartment-id, and the per-region Search-count versus compartment-walk-count pair that proves the enumeration was complete.",
      "verdict_rule": "PASS when no security list in any subscribed region has an ingress rule with source-type=CIDR_BLOCK AND source=\"0.0.0.0/0\" AND protocol in {\"6\",\"all\"} AND (tcp-options is absent OR tcp-options.destination-port-range is null OR destination-port-range.min <= 3389 <= destination-port-range.max by numeric comparison). A Search result count lower than the compartment-walk count makes the result inconclusive rather than PASS.",
      "remediation": "Rewrite the rule set with `oci network security-list update --security-list-id <ocid> --ingress-security-rules file://rules.json --force` scoped to jump-host or corporate CIDRs and with an explicit destination-port-range, or drop the rule and front RDP with OCI Bastion or a DRG-attached private path.",
      "caveat": "Beyond the 2.1 traps (regional and eventually-consistent Search, ::/0, protocol \"all\", unattached lists): a wide port range such as 1024-65535 or 3000-4000 contains 3389 without ever naming it, so a string match on \"3389\" produces a false pass — the comparison must be numeric containment. A tcp-options object whose destination-port-range is null is wider still and must fail. Search returns only resources the caller holds inspect on, so an under-privileged auditor gets an artificially clean result; the compartment-walk reconciliation above is the fix, and a Search count below the walk count is itself a finding about the assessing principal's permissions rather than a clean tenancy. Note also that RDP has a UDP transport on the same port number: a udp-options rule spanning 3389 from 0.0.0.0/0 is real exposure and should be reported as an accompanying observation even though the control names TCP."
    },
    {
      "control_id": "2.3",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Automated",
      "service": "vcn",
      "title": "No security group exposes remote shell access to the whole internet",
      "objective": "NSGs are applied directly to VNICs and are evaluated in addition to subnet security lists, so an NSG rule opening SSH to the internet exposes the attached instances even when every security list in the VCN is tight.",
      "procedure": "Per subscribed region (`oci iam region-subscription list --tenancy-id <tenancy_ocid>`), get NSG OCIDs via `oci search resource structured-search --query-text \"query NetworkSecurityGroup resources\" --region <region> --all`, with the fallback walk `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` is valueless; ANY rather than ACCESSIBLE so indirectly-visible compartments are not dropped) then `oci network nsg list --compartment-id <c> --all --region <region>`. Rules live outside the NSG object: call `oci network nsg rules list --nsg-id <ocid> --all --region <region>` and flag direction=INGRESS with source-type=CIDR_BLOCK, source=0.0.0.0/0, protocol \"6\" or \"all\", and tcp-options absent, or tcp-options present with a null destination-port-range (every destination port open), or a destination-port-range numerically covering 22.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource structured-search --query-text \"query NetworkSecurityGroup resources\" --region <region> --all",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all  # fallback walk; --include-root takes no value",
        "oci network nsg list --compartment-id <compartment_ocid> --all --region <region>",
        "oci network nsg rules list --nsg-id <nsg_ocid> --all --region <region>",
        "oci network nsg rules remove --nsg-id <nsg_ocid> --security-rule-ids '[\"<rule_id>\"]' --region <region>  # rule ids are opaque short strings, not OCIDs",
        "oci network nsg rules update --nsg-id <nsg_ocid> --security-rules file://rules.json --region <region>"
      ],
      "evidence": "The rule object from `oci network nsg rules list --nsg-id <ocid> --all` showing id (the opaque rule identifier, not an OCID), direction=INGRESS, source-type, source, protocol, is-stateless and the complete tcp-options object including a null destination-port-range where present, together with the NSG OCID, its vcn-id and compartment-id, and the region.",
      "verdict_rule": "PASS when, across all subscribed regions, no NSG rule has direction=INGRESS AND source-type=CIDR_BLOCK AND source=\"0.0.0.0/0\" AND protocol in {\"6\",\"all\"} AND (tcp-options is absent OR tcp-options.destination-port-range is null OR destination-port-range.min <= 22 <= destination-port-range.max by numeric comparison). The verdict is only valid if `nsg rules list` was actually called with --all for every enumerated NSG; an NSG whose rules were never fetched is inconclusive, not a pass.",
      "remediation": "Remove or narrow the rule: `oci network nsg rules remove --nsg-id <ocid> --security-rule-ids '[\"<rule_id>\"]'` using the id string returned by `nsg rules list`, or `oci network nsg rules update --nsg-id <ocid> --security-rules file://rules.json` with source set to an administrative CIDR and an explicit destination-port-range, or better with source-type NETWORK_SECURITY_GROUP referencing the bastion NSG.",
      "caveat": "The dominant false pass is checking the NSG object alone — `oci network nsg get` and Resource Search (including `return allAdditionalFields`) never carry the rule bodies, so an assessor who never calls `nsg rules list` sees zero findings. The second is a tcp-options object with a null destination-port-range, which opens all ports while looking like a scoped rule. Also: source-type may be NETWORK_SECURITY_GROUP or SERVICE_CIDR_BLOCK, neither of which is internet exposure and both of which must be excluded before flagging; the security-rule id is an opaque short string, not an OCID, so tooling that validates it as an OCID before calling `rules remove` will refuse to remediate; rules are paginated, so omitting --all truncates the list and yields a false pass on large NSGs; an NSG with no attached VNICs still fails the control; and NSGs are regional and per-VCN, so the region loop is mandatory."
    },
    {
      "control_id": "2.4",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Automated",
      "service": "vcn",
      "title": "No security group exposes remote desktop access to the whole internet",
      "objective": "An NSG ingress rule admitting the internet to RDP places Windows hosts one credential guess away from compromise regardless of subnet-level security lists, because NSG and security list rules are unioned, not intersected.",
      "procedure": "Identical traversal to 2.3 with the port changed. Resolve the region set with `oci iam region-subscription list --tenancy-id <tenancy_ocid>` and drive `--region` from its region-name values. Per region: enumerate NSGs via `oci search resource structured-search --query-text \"query NetworkSecurityGroup resources\" --region <region> --all`, with the fallback `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` plus `oci network nsg list --compartment-id <c> --all --region <region>`. Then `oci network nsg rules list --nsg-id <ocid> --all --region <region>`. Flag direction INGRESS, source-type CIDR_BLOCK, source 0.0.0.0/0, protocol \"6\" or \"all\", and tcp-options absent, or tcp-options present with destination-port-range null, or a destination-port-range whose min <= 3389 <= max.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource structured-search --query-text \"query NetworkSecurityGroup resources\" --region <region> --all",
        "oci network nsg list --compartment-id <compartment_ocid> --all --region <region>",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all  # --include-root takes no value",
        "oci network nsg rules list --nsg-id <nsg_ocid> --all --region <region>",
        "oci network nsg rules remove --nsg-id <nsg_ocid> --security-rule-ids '[\"<rule_id>\"]' --region <region>  # rule ids are opaque strings, not OCIDs",
        "oci network nsg rules update --nsg-id <nsg_ocid> --security-rules file://rules.json --region <region>"
      ],
      "evidence": "The INGRESS rule entry returned by `oci network nsg rules list --all` (rule id, source-type, source, protocol, is-stateless, and the whole tcp-options object with destination-port-range or its explicit null) plus the owning NSG OCID, its vcn-id, compartment-id and region, and the list of subscribed regions actually swept.",
      "verdict_rule": "PASS when no NSG rule in any subscribed region has direction=INGRESS AND source-type=CIDR_BLOCK AND source=\"0.0.0.0/0\" AND protocol in {\"6\",\"all\"} AND (tcp-options is absent OR tcp-options.destination-port-range is null OR destination-port-range.min <= 3389 <= destination-port-range.max by numeric comparison). A run whose region set was not derived from `oci iam region-subscription list` is inconclusive rather than PASS.",
      "remediation": "Delete the rule with `oci network nsg rules remove --nsg-id <ocid> --security-rule-ids '[\"<rule_id>\"]'` or re-scope it via `oci network nsg rules update --nsg-id <ocid> --security-rules file://rules.json` to the jump-host NSG or corporate CIDRs with an explicit destination-port-range, and expose administrative access through OCI Bastion or a site-to-site VPN instead.",
      "caveat": "Same rule-body trap as 2.3 (NSG metadata carries no rules) plus three more. Range trap: catch-all ranges like 1-65535 or 3000-4000 include 3389 without the literal string, so containment must be numeric, and a tcp-options object whose destination-port-range is null is wider than any range and must fail. Region trap: the verdict quantifies over subscribed regions, so `oci iam region-subscription list --tenancy-id <tenancy_ocid>` must be the first call and the source of every `--region` value — an implementer who omits it runs against the CLI profile's default region only and reports a single-region tenancy as clean. Pagination trap: omitting --all truncates the rule list and yields a false pass on large NSGs. Finally, this control and 2.2 are independent: a clean NSG result does not clear the subnet security lists, and both must pass before administrative exposure is genuinely closed. A udp-options rule spanning 3389 from 0.0.0.0/0 is real RDP exposure and should be reported as an accompanying observation."
    },
    {
      "control_id": "2.5",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Automated",
      "service": "vcn",
      "title": "A VCN's auto-created list is never the thing that lets traffic in",
      "objective": "Each VCN is born with a default security list that OCI attaches to any subnet created without an explicit list, and its shipped rules admit SSH from anywhere and permit all egress to anywhere; leaving it as-is silently grants internet ingress to future subnets and unrestricted outbound from them. Only intra-VCN ICMP path-MTU traffic should survive on the ingress side, and egress should not remain wide open.",
      "procedure": "Per subscribed region (`oci iam region-subscription list --tenancy-id <tenancy_ocid>`) list VCNs with `oci search resource structured-search --query-text \"query Vcn resources\" --region <region> --all` (fallback: `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` then `oci network vcn list --compartment-id <c> --all --region <region>`). For each VCN run `oci network vcn get --vcn-id <ocid>` and capture default-security-list-id together with the VCN's complete prefix set: cidr-blocks (IPv4), ipv6-cidr-blocks and ipv6-private-cidr-blocks (IPv6). Fetch the list with `oci network security-list get --security-list-id <default_id>` and evaluate BOTH arrays. Ingress: every entry must be protocol \"1\" (ICMP) with a source contained in one of the IPv4 prefixes, or protocol \"58\" (ICMPv6) with a source contained in one of the IPv6 prefixes — match each protocol against the address family it belongs to, never IPv6 sources against cidr-blocks. Any TCP/UDP entry, or any source outside the VCN's own prefixes, fails. Egress: every entry must be destination-type CIDR_BLOCK with a destination contained in the VCN's own IPv4 or IPv6 prefixes, or destination-type SERVICE_CIDR_BLOCK naming a service-gateway destination the organisation has explicitly approved; a 0.0.0.0/0 or ::/0 egress rule fails.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource structured-search --query-text \"query Vcn resources\" --region <region> --all",
        "oci network vcn get --vcn-id <vcn_ocid> --region <region>",
        "oci network security-list get --security-list-id <default_security_list_ocid> --region <region>",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all  # fallback walk; --include-root takes no value",
        "oci network vcn list --compartment-id <compartment_ocid> --all --region <region>",
        "oci network security-list update --security-list-id <default_security_list_ocid> --ingress-security-rules file://icmp_only.json --egress-security-rules file://egress.json --force"
      ],
      "evidence": "From `oci network vcn get`: default-security-list-id plus the full prefix set (cidr-blocks, ipv6-cidr-blocks, ipv6-private-cidr-blocks). From `oci network security-list get` for exactly that OCID: the complete ingress-security-rules AND egress-security-rules arrays. For every rule record which prefix list its source or destination matched (IPv4 cidr-blocks, ipv6-cidr-blocks, ipv6-private-cidr-blocks, or none), so an IPv6 rule is never recorded as unmatched merely because it was compared against the IPv4 list.",
      "verdict_rule": "PASS when, for every VCN in every subscribed region, the security list at default-security-list-id satisfies both of: (a) ingress-security-rules is empty or contains only rules with protocol \"1\" whose source CIDR is contained in the union of that VCN's cidr-blocks, or protocol \"58\" whose source CIDR is contained in the union of that VCN's ipv6-cidr-blocks and ipv6-private-cidr-blocks; AND (b) egress-security-rules is empty or contains only rules whose destination-type is CIDR_BLOCK with a destination contained in that VCN's own IPv4/IPv6 prefixes, or destination-type SERVICE_CIDR_BLOCK for an explicitly approved service destination. Any TCP/UDP ingress entry, any 0.0.0.0/0 or ::/0 source or destination, or an ICMPv6 rule sourced outside the VCN's IPv6 prefixes is a FAIL.",
      "remediation": "Strip the shipped rules on both sides in one call, since an update that supplies only one array leaves the other exactly as shipped: `oci network security-list update --security-list-id <default_id> --ingress-security-rules '[{\"protocol\":\"1\",\"source\":\"<vcn_ipv4_cidr>\",\"sourceType\":\"CIDR_BLOCK\",\"icmpOptions\":{\"type\":3,\"code\":4}}]' --egress-security-rules '[]' --force` (add a protocol \"58\" entry sourced from the VCN's IPv6 prefix on dual-stack VCNs), and attach purpose-built security lists or NSGs to each subnet so nothing depends on the default list.",
      "caveat": "Do not identify the default list by display name — \"Default Security List for <vcn>\" is fully editable, and a renamed default or a custom list coincidentally named that way breaks name-based matching in both directions; the only authoritative pointer is vcn.default-security-list-id. Second trap: auditing only lists currently attached to subnets skips the common case of an untouched default list sitting unused, which is precisely the risk this control addresses. Third: the shipped ICMP 3/4 rule is sourced 0.0.0.0/0 by default and must be re-sourced to the VCN CIDR — it looks harmless and is routinely left in place. Fourth, and newly load-bearing: the VCN's IPv6 prefixes never appear in cidr-blocks, so validating an ICMPv6 source against cidr-blocks alone scores a correctly-scoped dual-stack rule as FAIL and, worse, invites an implementer to \"fix\" it by skipping protocol 58 entirely — build the allowed-source set as the union of cidr-blocks, ipv6-cidr-blocks and ipv6-private-cidr-blocks and match per address family. Fifth: the default list also ships an all-protocols egress rule to 0.0.0.0/0, so checking ingress only lets a completely untouched default list score PASS; both arrays are in scope here, and a single-array `security-list update` silently preserves the other."
    },
    {
      "control_id": "2.6",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Manual",
      "service": "vcn",
      "title": "Confine Integration Cloud instances to a private endpoint or a narrow source allowlist",
      "objective": "An OIC instance published with an unrestricted public endpoint exposes its design-time console and REST integration endpoints to the entire internet, so reachability must be cut to a private endpoint or an explicit list of approved source networks.",
      "procedure": "Walk compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` is a valueless flag; use ANY rather than ACCESSIBLE so compartments visible only indirectly are not dropped) and, per subscribed region from `oci iam region-subscription list --tenancy-id <tenancy_ocid>`, run `oci integration integration-instance list --compartment-id <c> --all --region <region>`. For each instance call `oci integration integration-instance get --integration-instance-id <ocid>` and read network-endpoint-details. Do not assume the discriminator can be PRIVATE_ENDPOINT: confirm the enum against the live schema first — `oci integration integration-instance get --integration-instance-id <ocid> --query 'data.\"network-endpoint-details\"'` against a known-restricted instance, or the Integration API specification — because PUBLIC appears to be the only value this discriminator takes, OIC private connectivity being expressed through separate private-endpoint constructs rather than through this enum. Within a PUBLIC endpoint read allowlisted-http-ips, allowlisted-http-vcns (each entry carrying id and allowlisted-ip-cidrs, which must be inspected individually) and is-integration-vcn-allowlisted. A null network-endpoint-details is fully open, not unconfigured.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci integration integration-instance list --compartment-id <compartment_ocid> --all --region <region>",
        "oci integration integration-instance get --integration-instance-id <integration_instance_ocid> --region <region>",
        "oci integration integration-instance get --integration-instance-id <integration_instance_ocid> --query 'data.\"network-endpoint-details\"'  # confirm the live discriminator values before pinning the verdict",
        "oci search resource-type list --region <region>  # confirm IntegrationInstance is an indexed type before relying on Search",
        "oci search resource structured-search --query-text \"query IntegrationInstance resources\" --region <region> --all",
        "oci integration integration-instance change-network-endpoint --integration-instance-id <integration_instance_ocid> --network-endpoint-details file://endpoint.json --region <region>"
      ],
      "evidence": "The complete network-endpoint-details block of `oci integration integration-instance get` — network-endpoint-type, allowlisted-http-ips, allowlisted-http-vcns (recording each entry's id AND its nested allowlisted-ip-cidrs individually) and is-integration-vcn-allowlisted (the documented attribute name; a collector keyed on is-integration-vcn-only-allowlist reads null and records \"not configured\" for an instance that is in fact restricted to the integration VCN) — recorded against the instance OCID, lifecycle-state, compartment-id and region, with the explicit null state of network-endpoint-details captured as its own evidence value.",
      "verdict_rule": "PASS when every integration instance whose lifecycle-state is not DELETED or DELETING has a non-null network-endpoint-details whose network-endpoint-type equals the schema-confirmed public value (PUBLIC) AND at least one of allowlisted-http-ips non-empty, allowlisted-http-vcns non-empty, or is-integration-vcn-allowlisted == true, AND, after normalising every allowlist entry — top-level allowlisted-http-ips plus each VCN entry's nested allowlisted-ip-cidrs — to a CIDR, no single entry has prefix length 0 and the interval-merged union of all entries does not cover the entire 32-bit IPv4 address space (nor the entire IPv6 space if IPv6 entries are present). A null or absent network-endpoint-details is a FAIL. An instance exempt because inbound access is genuinely private must be evidenced from its private-endpoint configuration, never from an unverified PRIVATE_ENDPOINT enum value. The residual judgement — whether the surviving, strictly narrower ranges are organisationally approved — is the Manual overlay and is stated as such.",
      "remediation": "Apply an allowlist with `oci integration integration-instance change-network-endpoint --integration-instance-id <ocid> --network-endpoint-details file://endpoint.json` specifying allowlistedHttpIps for the approved CIDRs, allowlistedHttpVcns (each with its own narrow allowlistedIpCidrs) for internal callers, and isIntegrationVcnAllowlisted where the integration VCN is the intended caller. Where inbound access should not traverse the internet at all, place the instance behind the service's private-endpoint configuration rather than relying on an allowlist.",
      "caveat": "Marked Manual because the API can prove an allowlist exists but not that it is meaningful — only a human who knows which networks are approved can adjudicate the surviving ranges. Four mechanical traps must not be left to that judgement, though. (1) 0.0.0.0/1 plus 128.0.0.0/1, or a set of /8s, is exactly equivalent to a /0 while matching no single 0.0.0.0/0 entry; the test must be prefix-length-0 detection plus an interval-merge union check over the address space, not string equality. (2) The attribute is is-integration-vcn-allowlisted, not is-integration-vcn-only-allowlist; the wrong key reads null and mislabels a restricted instance as unconfigured. (3) A null network-endpoint-details reads like \"nothing configured\" but means unrestricted public access, so it is a FAIL. (4) The PRIVATE_ENDPOINT branch may be dead code — PUBLIC appears to be the only documented value of this discriminator — so an assessor who sees no PRIVATE_ENDPOINT instances may believe the check is exercised when only one branch has ever run; confirm the enum against the live schema and, if it is PUBLIC-only, drop the branch rather than leaving it as an untested exemption. Also: a listed VCN whose nested allowlisted-ip-cidrs is wide is not a restriction; and OIC instances are regional and compartment-scoped, so a root-compartment-only or home-region-only sweep misses whole estates. Confirm the Search resource type name with `oci search resource-type list` before relying on it."
    },
    {
      "control_id": "2.7",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Manual",
      "service": "vcn",
      "title": "Confine Analytics Cloud instances to a VCN endpoint or an explicit IP allowlist",
      "objective": "An OAC instance with an open public endpoint puts a full BI console and its data connections on the internet; access should terminate on a private VCN endpoint or be limited to enumerated source IPs and VCNs.",
      "procedure": "Enumerate compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` is valueless; ANY rather than ACCESSIBLE so no compartment is silently dropped) and loop subscribed regions from `oci iam region-subscription list --tenancy-id <tenancy_ocid>` with `oci analytics analytics-instance list --compartment-id <c> --all --region <region>`. For each, run `oci analytics analytics-instance get --analytics-instance-id <ocid>` and read network-endpoint-details: network-endpoint-type PRIVATE carries vcn-id, subnet-id and network-security-group-ids; PUBLIC carries whitelisted-ips and whitelisted-vcns, each VCN entry carrying its own id and nested whitelisted-ips which must be inspected individually. Treat a null or absent network-endpoint-details as an unrestricted public endpoint, not as unconfigured. Where the endpoint is PRIVATE, follow network-security-group-ids into `oci network nsg rules list --nsg-id <ocid> --all` so a private endpoint fronted by an internet-open NSG is not scored as restricted.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci analytics analytics-instance list --compartment-id <compartment_ocid> --all --region <region>",
        "oci analytics analytics-instance get --analytics-instance-id <analytics_instance_ocid> --region <region>",
        "oci search resource-type list --region <region>  # confirm AnalyticsInstance is an indexed type before relying on Search",
        "oci search resource structured-search --query-text \"query AnalyticsInstance resources\" --region <region> --all",
        "oci network nsg rules list --nsg-id <nsg_ocid> --all --region <region>  # for PRIVATE endpoints, prove the fronting NSGs are not internet-open",
        "oci analytics analytics-instance change-network-endpoint --analytics-instance-id <analytics_instance_ocid> --network-endpoint-details file://endpoint.json --region <region>"
      ],
      "evidence": "The complete network-endpoint-details object from `oci analytics analytics-instance get` — network-endpoint-type, and either vcn-id/subnet-id/network-security-group-ids or the whitelisted-ips array and the whitelisted-vcns array with each entry's id AND its nested whitelisted-ips — captured with the instance OCID, lifecycle-state, compartment-id and region, recording an absent or null network-endpoint-details as its own explicit evidence value. For PRIVATE instances, also the ingress rule set of each referenced NSG.",
      "verdict_rule": "PASS when every analytics instance whose lifecycle-state is not DELETED or DELETING has a non-null network-endpoint-details and either (a) network-endpoint-type=PRIVATE with a non-null subnet-id, and no NSG in network-security-group-ids carries an INGRESS rule from 0.0.0.0/0 or ::/0; or (b) network-endpoint-type=PUBLIC with whitelisted-ips non-empty or whitelisted-vcns non-empty, and, after normalising every entry — top-level whitelisted-ips plus each VCN entry's nested whitelisted-ips — to a CIDR, no single entry has prefix length 0 and the interval-merged union of all entries does not cover the entire IPv4 (or IPv6) address space. A null or absent network-endpoint-details is a FAIL. Whether the surviving narrower ranges are the organisation's approved networks is the Manual overlay, stated separately from this mechanical rule.",
      "remediation": "Restrict the endpoint with `oci analytics analytics-instance change-network-endpoint --analytics-instance-id <ocid> --network-endpoint-details file://endpoint.json`, listing only approved CIDRs under whitelistedIps and internal VCNs under whitelistedVcns with narrow nested whitelistedIps, or migrate the instance to a PRIVATE endpoint bound to a subnet and to NSGs whose ingress rules name specific sources.",
      "caveat": "Manual because judging whether the listed IPs and VCNs are the organisation's own approved networks is a business decision no API answers — a long whitelist of partner or ISP ranges passes the mechanical rule while leaving the console broadly reachable. Five traps. (1) \"No entry equal to 0.0.0.0/0\" is not a sufficient test: 0.0.0.0/1 plus 128.0.0.0/1, or a handful of /8s, covers the whole internet without matching any /0 string, so the check must be prefix-length-0 detection plus an interval-merge union over the address space. (2) A whitelisted-vcns entry carries its own nested whitelisted-ips, so a permissive sub-list inside an otherwise internal-looking VCN entry is real exposure and nested entries must be walked. (3) A null or absent network-endpoint-details is unrestricted access, not \"unset\", and must fail rather than skip. (4) A stopped (INACTIVE) instance still holds its endpoint configuration and counts. (5) A PRIVATE instance still depends on the NSGs in network-security-group-ids, so a private endpoint fronted by an internet-open NSG is only nominally restricted and the NSG rules must be fetched — NSG metadata alone never carries them. OAC instances are regional and compartment-scoped, so both loops are mandatory."
    },
    {
      "control_id": "2.8",
      "section": "2",
      "section_name": "Networking",
      "level": 1,
      "assessment": "Manual",
      "service": "vcn",
      "title": "Bind serverless Autonomous Databases to a private endpoint or an enforced access ACL",
      "objective": "Serverless Autonomous Database endpoints are internet-facing by default, so every such database must either sit behind a VCN private endpoint or enforce a network access control list that names the permitted sources.",
      "procedure": "For every compartment (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all`; `--include-root` is a valueless flag and ANY rather than ACCESSIBLE keeps indirectly-visible compartments in the tree) and every subscribed region from `oci iam region-subscription list --tenancy-id <tenancy_ocid>`, run `oci db autonomous-database list --compartment-id <c> --all --region <region>`; or resolve tenancy-wide via `oci search resource structured-search --query-text \"query AutonomousDatabase resources\" --region <region> --all` then `oci db autonomous-database get`. Select the in-scope serverless population with the boolean `is-dedicated == false` — do NOT filter on infrastructure-type, whose documented values are CLOUD and CLOUD_AT_CUSTOMER, never SHARED/DEDICATED; a SHARED filter matches nothing and evaluates zero databases. Use infrastructure-type only as an additional filter if Exadata Cloud@Customer estates are to be excluded. For each selected database read subnet-id, private-endpoint-ip, nsg-ids, is-access-control-enabled and the whitelisted-ips array.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci db autonomous-database list --compartment-id <compartment_ocid> --all --region <region>",
        "oci db autonomous-database get --autonomous-database-id <autonomous_database_ocid> --region <region>",
        "oci db autonomous-database get --autonomous-database-id <ocid> --query 'data.{ded:\"is-dedicated\",infra:\"infrastructure-type\",state:\"lifecycle-state\",subnet:\"subnet-id\",acl:\"is-access-control-enabled\",ips:\"whitelisted-ips\"}'",
        "oci search resource-type list --region <region>  # confirm AutonomousDatabase is an indexed type before relying on Search",
        "oci search resource structured-search --query-text \"query AutonomousDatabase resources\" --region <region> --all",
        "oci db autonomous-database update --autonomous-database-id <ocid> --is-access-control-enabled true --whitelisted-ips '[\"<cidr>\",\"<vcn_ocid>;<cidr>\"]' --force"
      ],
      "evidence": "Per database OCID: is-dedicated (the attribute that actually separates serverless from dedicated), infrastructure-type, lifecycle-state, subnet-id and private-endpoint-ip, nsg-ids, is-access-control-enabled and the whitelisted-ips array, all from `oci db autonomous-database get`, with compartment-id and region. Record the count of databases returned by the list call and the count that survived the is-dedicated filter, so a filter that matched nothing is visible as an enumeration failure rather than as a clean tenancy.",
      "verdict_rule": "PASS when every autonomous database with is-dedicated == false and lifecycle-state not in {TERMINATED, TERMINATING, UNAVAILABLE} either has a non-null subnet-id (private endpoint), or has is-access-control-enabled == true together with a non-empty whitelisted-ips containing no entry that is 0.0.0.0/0 or otherwise of prefix length 0, and whose entries do not union to cover the whole address space. A database whose whitelisted-ips is populated while is-access-control-enabled is false is a FAIL. A run in which the serverless filter selected zero databases while the list call returned some is inconclusive, not a PASS.",
      "remediation": "Apply an ACL with `oci db autonomous-database update --autonomous-database-id <ocid> --is-access-control-enabled true --whitelisted-ips '[\"<cidr>\",\"<vcn_ocid>;<cidr>\"]' --force` (the boolean must be set in the same change, since the list alone has no effect; `--force` suppresses the list-overwrite prompt), or move the database onto a private endpoint by supplying --subnet-id and --nsg-ids so it is reachable only from the VCN.",
      "caveat": "Manual because whether the listed CIDRs are legitimate corporate sources cannot be settled by API. The catastrophic false pass is scoping the population by the wrong attribute: infrastructure-type on an Autonomous Database takes CLOUD or CLOUD_AT_CUSTOMER, not SHARED, so a SHARED filter silently selects zero databases and the entire tenancy scores PASS with no database ever examined — filter on is-dedicated == false and assert a non-zero selected count wherever the list call returned rows. The second sharpest is a populated whitelisted-ips with is-access-control-enabled == false: the list persists as stale configuration while the database stays open, so the boolean must be checked, not the array alone. Also: entries may be raw CIDRs, bare IPs, or VCN-scoped forms like \"<vcn_ocid>;<cidr>\", and each shape needs its own parser before any prefix-length or union test can run; a set of narrow-looking entries can still union to the whole internet, so test coverage of the address space rather than string-matching 0.0.0.0/0; dedicated Exadata databases (is-dedicated == true) are outside the control; mTLS being required (is-mtls-connection-required) is authentication, not network scope, and never substitutes for the ACL; a private endpoint is only as tight as the NSGs in nsg-ids; and ADBs are regional and compartment-scoped, so a home-region-only or root-only sweep under-reports."
    },
    {
      "control_id": "3.1",
      "section": "3",
      "section_name": "Compute",
      "level": 1,
      "assessment": "Automated",
      "service": "compute",
      "title": "Force instances onto the authenticated v2 instance-metadata endpoint",
      "objective": "The unauthenticated v1 metadata endpoint (http://169.254.169.254/opc/v1/) hands instance metadata, and any instance-principal credentials reachable through it, to anything that can make the instance issue an outbound HTTP request. Requiring v2, which demands an Authorization: Bearer Oracle header, turns a plain SSRF on a workload into a non-event instead of a credential leak.",
      "procedure": "Loop over every subscribed region from `oci iam region-subscription list --tenancy-id <tenancy_ocid>`. Per region, structured-search instance resources for all OCIDs tenancy-wide (`oci search resource structured-search --query-text \"query instance resources where lifecycleState != 'TERMINATED'\" --region <region> --all`; note the attribute is lifecycleState with a lower-case c — a misspelling silently matches nothing). If Search is denied or under-privileged, list compartments with `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all` (`--include-root` takes no value) and run `oci compute instance list --compartment-id <c> --all --region <region>` in each. For every non-terminated instance run `oci compute instance get --instance-id <ocid>` and read instance-options.are-legacy-imds-endpoints-disabled together with lifecycle-state and shape in the same projection. Then, over the same compartment set and the same region set, enumerate `oci compute-management instance-configuration list --compartment-id <c> --all --region <region>` and read instance-details.launch-details.instance-options.are-legacy-imds-endpoints-disabled on each, because these are what instance pools launch from.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource structured-search --query-text \"query instance resources where lifecycleState != 'TERMINATED'\" --region <region> --all",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci compute instance list --compartment-id <compartment_ocid> --all --region <region>",
        "oci compute instance get --instance-id <instance_ocid> --region <region> --query 'data.{state:\"lifecycle-state\",shape:shape,io:\"instance-options\"}'",
        "oci compute-management instance-configuration list --compartment-id <compartment_ocid> --all --region <region>",
        "oci compute-management instance-configuration get --instance-configuration-id <instance_configuration_ocid> --region <region>",
        "oci compute instance update --instance-id <instance_ocid> --instance-options '{\"areLegacyImdsEndpointsDisabled\": true}' --force"
      ],
      "evidence": "instanceOptions.areLegacyImdsEndpointsDisabled from each GetInstance response, keyed by instance OCID, lifecycleState, compartment OCID and region, with an absent or null instanceOptions recorded explicitly as null rather than omitted; plus instanceDetails.launchDetails.instanceOptions.areLegacyImdsEndpointsDisabled from every GetInstanceConfiguration response, keyed by instance configuration OCID, compartment and region. Record the per-region instance count and instance-configuration count so an empty sweep is distinguishable from a compliant one.",
      "verdict_rule": "PASS iff BOTH hold. (a) For every instance in every subscribed region whose lifecycleState is not TERMINATED or TERMINATING, instanceOptions.areLegacyImdsEndpointsDisabled == true. (b) For every instance configuration returned by `oci compute-management instance-configuration list` across every compartment in the subtree plus the tenancy root and every subscribed region, instanceDetails.launchDetails.instanceOptions.areLegacyImdsEndpointsDisabled == true. A null or absent instanceOptions block on either object is a FAIL, not a skip. Evidence is collected from instance configurations precisely because they are load-bearing — a tenancy whose running instances are all compliant but whose configurations still launch legacy IMDS reverts on the next scale or heal event and must not score PASS.",
      "remediation": "Per instance: `oci compute instance update --instance-id <ocid> --instance-options '{\"areLegacyImdsEndpointsDisabled\": true}' --force` (no reboot needed; --force suppresses the complex-parameter overwrite prompt). Confirm the workload reads metadata via /opc/v2/ with header 'Authorization: Bearer Oracle' first, because flipping the flag breaks any code still calling v1. Then set the same field under instanceDetails.launchDetails.instanceOptions in every instance configuration — creating a replacement configuration and repointing the instance pool at it, since instance configurations are effectively immutable — so pool scale-out does not reintroduce v1.",
      "caveat": "Four false-pass traps. (1) Search and the compute APIs are regional: a single-region run silently reports 100% pass for a tenancy with instances elsewhere, and `oci iam region-subscription list --tenancy-id <ocid>` must be the source of every --region value. (2) Instances launched before the option existed return instanceOptions as null, which JSON-path tooling reads as 'not false' and scores as pass; null means v1 is live and is a FAIL. (3) Fixing running instances is not durable if the backing instance configuration still launches with v1 enabled, so an instance pool re-creates non-compliant instances after any scale or heal event — which is why the verdict quantifies over instance configurations as well as instances, rather than collecting configuration evidence and then ignoring it. (4) Structured search returns only summary attributes and never exposes instanceOptions, so the per-instance GetInstance call is mandatory; Search also returns only what the caller can inspect, so reconcile its count against the compartment walk before trusting a small result set. The query attribute is lifecycleState, not lifeCycleState — the wrong casing errors or matches nothing, which reads as a clean tenancy."
    },
    {
      "control_id": "3.2",
      "section": "3",
      "section_name": "Compute",
      "level": 2,
      "assessment": "Automated",
      "service": "compute",
      "title": "Require UEFI Secure Boot on shielded-capable virtual machines",
      "objective": "Secure Boot makes the instance firmware refuse boot components that are not signed by a trusted authority, which blocks bootkit and unsigned-kernel-module persistence surviving a reinstall of the OS. On OCI this is a shielded-instance platform setting fixed at launch, so it is either designed in or absent.",
      "procedure": "Per subscribed region from `oci iam region-subscription list --tenancy-id <tenancy_ocid>`, collect all instance OCIDs — `oci search resource structured-search --query-text \"query instance resources where lifecycleState != 'TERMINATED'\" --region <region> --all` (lower-case c in lifecycleState), or `oci compute instance list --compartment-id <c> --all --region <region>` over every compartment from `oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all`. For each non-terminated instance run `oci compute instance get`: platform-config must exist and platform-config.is-secure-boot-enabled must be true. In the same projection capture shape and launch-options.firmware (UEFI_64 versus BIOS), since a BIOS instance cannot carry Secure Boot. Establish applicability per instance from the shape rather than from bare-metal-versus-VM: `oci compute shape list --compartment-id <c> --region <region>` returns platform-config-options, whose secure-boot-options block tells you whether that specific shape supports the setting. Check the same platform-config field under instance-details.launch-details in every instance configuration returned by `oci compute-management instance-configuration list --compartment-id <c> --all --region <region>`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci search resource structured-search --query-text \"query instance resources where lifecycleState != 'TERMINATED'\" --region <region> --all",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci compute instance list --compartment-id <compartment_ocid> --all --region <region>",
        "oci compute instance get --instance-id <instance_ocid> --region <region> --query 'data.{state:\"lifecycle-state\",shape:shape,fw:\"launch-options\".firmware,pc:\"platform-config\"}'",
        "oci compute shape list --compartment-id <compartment_ocid> --region <region>  # read platform-config-options.secure-boot-options to decide per-shape applicability",
        "oci compute-management instance-configuration list --compartment-id <compartment_ocid> --all --region <region>",
        "oci compute-management instance-configuration get --instance-configuration-id <instance_configuration_ocid> --region <region>"
      ],
      "evidence": "platformConfig.isSecureBootEnabled == true in the GetInstance response for each instance OCID, recorded with lifecycleState, shape, launchOptions.firmware and region, and with a null platformConfig captured explicitly as null; the shape's platformConfigOptions.secureBootOptions from ListShapes as the applicability record for any instance claimed to be incapable; and instanceDetails.launchDetails.platformConfig.isSecureBootEnabled for each instance configuration, keyed by configuration OCID, compartment and region.",
      "verdict_rule": "PASS iff BOTH hold. (a) Every non-terminated instance in every subscribed region returns a non-null platformConfig with isSecureBootEnabled == true. (b) Every instance configuration across every compartment and subscribed region sets instanceDetails.launchDetails.platformConfig.isSecureBootEnabled == true, so that pool scale-out cannot launch unshielded instances. platformConfig == null is a FAIL on either object and must not be auto-classified as not-applicable; where the shape genuinely does not support Secure Boot, the finding is recorded as a FAIL carrying the shape's platformConfigOptions.secureBootOptions as the exception evidence and a shape-migration note, never converted into a pass.",
      "remediation": "Secure Boot is immutable after launch. Re-launch from a UEFI image on a shielded-capable shape: `oci compute instance launch ... --platform-config '{\"type\":\"AMD_VM\",\"isSecureBootEnabled\":true,\"isMeasuredBootEnabled\":true,\"isTrustedPlatformModuleEnabled\":true}'` where type matches the shape family (AMD_VM / INTEL_VM for virtual machines, the corresponding BM platform type for bare metal shapes that support shielded instances), migrate data, then terminate the old instance. Set the same block under instanceDetails.launchDetails.platformConfig in a replacement instance configuration and repoint the instance pool at it.",
      "caveat": "platformConfig is null on shapes and legacy VMs with no shielded-instance support, and the tempting reading of null as not-applicable converts the whole estate into a false pass; the honest result is a FAIL with a shape-migration note. Applicability is shape-dependent across BOTH families — shielded instances (Secure Boot, Measured Boot, TPM) are supported on selected bare metal shapes as well as VM shapes, and several BM shapes are in fact where Measured Boot and TPM are offered — so do not write bare metal off as categorically incapable, which turns real, remediable failures into permanent exceptions. The genuine exclusions are GPU, HPC and legacy shape generations, and the image must be UEFI rather than BIOS (visible as launchOptions.firmware). Decide applicability per instance from the shape's platformConfigOptions.secureBootOptions and cite the current Oracle shielded-instance supported-shape list in any exception note. Secure Boot cannot be toggled on a live instance, so remediation is a rebuild and any check that expects `oci compute instance update` to fix it will report perpetual drift. Finally, Secure Boot blocks unsigned third-party kernel modules, so enabling it estate-wide needs a functional test, not just an API flip — and remediating instances while leaving instance configurations unshielded reverts on the next pool event, which is why the verdict covers both."
    },
    {
      "control_id": "3.3",
      "section": "3",
      "section_name": "Compute",
      "level": 1,
      "assessment": "Automated",
      "service": "compute",
      "title": "Encrypt the host-to-storage data path for boot and block volume attachments",
      "objective": "Block Volume data is always encrypted at rest, but the iSCSI/paravirtualized traffic between the compute host and the storage service is only encrypted when in-transit encryption is switched on for the attachment. Without it, volume traffic crosses the substrate network unencrypted, so at-rest encryption alone does not cover the read/write path.",
      "procedure": "Per subscribed region (`oci iam region-subscription list --tenancy-id <tenancy_ocid>`) and per compartment (`oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all`; `--include-root` takes no value), list instances and, for each non-terminated one, run `oci compute instance get` and read launch-options.is-pv-encryption-in-transit-enabled. Do not stop there: enumerate attachments compartment by compartment rather than trying to reach them through an instance filter, because `oci compute volume-attachment list` requires --compartment-id and --instance-id is only a filter applied inside that compartment. Run `oci compute volume-attachment list --compartment-id <c> --all --region <region>` over every ACTIVE compartment in the subtree plus the tenancy root, then join the returned attachments to instances by instance-id; read is-pv-encryption-in-transit-enabled, attachment-type and lifecycle-state on every attachment in state ATTACHED. Evidence the boot volume as a first-class object too: loop the ADs from `oci iam availability-domain list --compartment-id <tenancy_ocid> --region <region>` and run `oci compute boot-volume-attachment list --compartment-id <c> --availability-domain <ad> --region <region>` (both parameters are required), asserting is-pv-encryption-in-transit-enabled == true on each BootVolumeAttachment in state ATTACHED. Finally check launch-options.is-pv-encryption-in-transit-enabled under instance-details.launch-details in every instance configuration.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy_ocid>",
        "oci iam compartment list --compartment-id <tenancy_ocid> --compartment-id-in-subtree true --include-root --access-level ANY --lifecycle-state ACTIVE --all",
        "oci iam availability-domain list --compartment-id <tenancy_ocid> --region <region>",
        "oci compute instance list --compartment-id <compartment_ocid> --all --region <region>",
        "oci compute instance get --instance-id <instance_ocid> --region <region> --query 'data.{state:\"lifecycle-state\",shape:shape,lo:\"launch-options\"}'",
        "oci compute volume-attachment list --compartment-id <compartment_ocid> --all --region <region>  # iterate every compartment; --instance-id is only an in-compartment filter",
        "oci compute boot-volume-attachment list --compartment-id <compartment_ocid> --availability-domain <availability_domain> --region <region>  # both parameters required",
        "oci compute-management instance-configuration list --compartment-id <compartment_ocid> --all --region <region>",
        "oci search resource structured-search --query-text \"query instance resources where lifecycleState != 'TERMINATED'\" --region <region> --all",
        "oci compute instance update --instance-id <instance_ocid> --launch-options '{\"isPvEncryptionInTransitEnabled\":true}' --force"
      ],
      "evidence": "launchOptions.isPvEncryptionInTransitEnabled from GetInstance per instance OCID (with shape and lifecycleState); isPvEncryptionInTransitEnabled, attachmentType and lifecycleState from every VolumeAttachment returned by ListVolumeAttachments across all compartments, each recorded with the instanceId it joins to and the compartment it was found in (which may differ from the instance's compartment); isPvEncryptionInTransitEnabled and lifecycleState from every BootVolumeAttachment returned by ListBootVolumeAttachments per compartment and availability domain; and instanceDetails.launchDetails.launchOptions.isPvEncryptionInTransitEnabled per instance configuration. All keyed by region, compartment and availability domain, with the compartment and AD lists actually swept recorded alongside so an incomplete enumeration is visible.",
      "verdict_rule": "PASS iff all of the following hold. (a) For every non-terminated instance, launchOptions.isPvEncryptionInTransitEnabled == true. (b) Every VolumeAttachment in lifecycleState ATTACHED, joined by instance-id to a non-terminated instance, has isPvEncryptionInTransitEnabled == true. (c) Every BootVolumeAttachment in lifecycleState ATTACHED has isPvEncryptionInTransitEnabled == true. (d) Every instance configuration sets instanceDetails.launchDetails.launchOptions.isPvEncryptionInTransitEnabled == true, so pool scale-out does not launch unencrypted paths. Any false or null value on any of these objects is a FAIL. A result computed without sweeping every ACTIVE compartment (for volume attachments) and every availability domain (for boot volume attachments) is inconclusive, not a PASS.",
      "remediation": "Stop the instance, then `oci compute instance update --instance-id <ocid> --launch-options '{\"isPvEncryptionInTransitEnabled\":true}' --force` and start it. For each data volume, detach and re-attach encrypted: `oci compute volume-attachment attach --type paravirtualized --instance-id <ocid> --volume-id <ocid> --is-pv-encryption-in-transit-enabled true`. Boot volumes take the setting from the instance launch, so a non-compliant BootVolumeAttachment is fixed by the stop/update/start above or by relaunching the instance. Set the same flag in the launch details of every instance configuration and repoint the pool at the replacement configuration.",
      "caveat": "Checking only the instance launchOptions is the classic false pass: that flag governs the boot volume path, while each attached block volume carries its own isPvEncryptionInTransitEnabled and can be unencrypted on an otherwise compliant instance. The stated fix of 'run the attachment query with the instance filter' does not work — `oci compute volume-attachment list` requires --compartment-id and --instance-id is only a filter applied within that compartment, so an attachment created in a compartment other than the one being queried is still missed; the enumeration must iterate every ACTIVE compartment in the subtree plus the tenancy root and join attachments to instances by instance-id afterwards. The boot volume must also be evidenced directly through ListBootVolumeAttachments, which requires BOTH --compartment-id and --availability-domain, so the AD loop is mandatory and a missing AD silently drops every boot volume in it. In-transit encryption requires paravirtualized attachment on supported shapes with Oracle-provided or UEFI-capable images; shapes that cannot set it should be reported as evidenced exceptions rather than silently skipped. The instance-level change needs a stop/start, so an update that returns 200 while the instance keeps running is not yet in effect and re-reading the API can show true before the data path actually changes. Finally, remediating instances while the backing instance configurations still launch with the flag off reverts on the next scale or heal event, which is why instance configurations are in the verdict and not merely collected."
    },
    {
      "control_id": "4.1",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "logging",
      "title": "Require a default tag to be stamped automatically onto newly created resources",
      "objective": "Every resource created in the tenancy should inherit an automatically applied defined tag so that ownership, cost and incident attribution work without relying on operators to tag by hand.",
      "procedure": "Work against the tenancy home region, because tag namespaces, tag definitions and tag defaults are IAM (home-region) objects. Resolve the tenancy OCID, then enumerate compartments with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` (`--include-root` is a valueless CLI flag; passing it a value aborts the call, and `--access-level ACCESSIBLE` silently truncates the tree for an auditor principal). Run `oci iam tag-default list --compartment-id <ocid> --all` for the tenancy root first, then for each child compartment. For every returned tag default read `tag-definition-id`, `is-required`, `value` and `lifecycle-state`, then confirm the owning namespace and definition are live: `oci iam tag-namespace get --tag-namespace-id <ocid>` and `oci iam tag get --tag-namespace-id <ocid> --tag-name <name>` must both return `is-retired: false`.",
      "api": [
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci iam tag-default list --compartment-id <compartment-ocid> --all",
        "oci iam tag-namespace list --compartment-id <tenancy-ocid> --include-subcompartments true --all",
        "oci iam tag-namespace get --tag-namespace-id <ocid>",
        "oci iam tag get --tag-namespace-id <ocid> --tag-name <name>"
      ],
      "evidence": "The tag-default list response for the tenancy root compartment: at least one item with lifecycle-state ACTIVE, `is-required: true`, a non-empty `value`, and a `tag-definition-id` whose tag definition and owning namespace both report `is-retired: false`. Record the child-compartment tag-default listings as well, plus the compartment count returned by the subtree walk so a truncated tree is visible in the evidence.",
      "verdict_rule": "PASS when `oci iam tag-default list --compartment-id <tenancy-ocid> --all` returns >=1 entry with lifecycle-state ACTIVE and `is-required: true` whose tag definition and tag namespace both have `is-retired: false`. An empty list, only `is-required: false` entries, or an entry whose namespace or definition is retired is a FAIL. A tag default set on the root is inherited by descendant compartments, so root-level evidence is sufficient for this verdict; if the organisation requires per-compartment values instead, tighten the rule to demand a qualifying ACTIVE, is-required tag default in every ACTIVE compartment returned by the subtree listing.",
      "remediation": "Create a defined tag namespace and key in the root compartment (`oci iam tag-namespace create`, `oci iam tag create`), then bind it as a default: `oci iam tag-default create --compartment-id <tenancy-ocid> --tag-definition-id <ocid> --value <default-value> --is-required true`. Add equivalent defaults in child compartments only where a different enforced value is required, since the root default already propagates down the tree. Never retire a namespace or definition that a live tag default references.",
      "caveat": "A retired namespace or a retired tag definition leaves the tag default present in the API response but completely inert, which is the primary false pass here: the record must be resolved through `tag-namespace get` and `tag get` rather than trusted from the tag-default listing alone. Tag defaults never retro-tag resources that already exist, so a passing verdict says nothing about the existing estate. These are IAM objects served from the home region, so issuing the calls against another region returns nothing and reads as an empty result rather than an error."
    },
    {
      "control_id": "4.2",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "An alert destination exists and a subscriber has confirmed receipt",
      "objective": "Detective controls are worthless if nothing delivers the alert. The tenancy needs a live Notifications topic with a subscription that has actually been confirmed, so Events and Alarms have somewhere to publish.",
      "procedure": "Loop over subscribed regions from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`. Build the compartment set once with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all`. In each region, for each compartment, run `oci ons topic list --compartment-id <ocid> --all --region <r>` and `oci ons subscription list --compartment-id <ocid> --all --region <r>`. For each subscription read `topic-id`, `protocol`, `endpoint` and `lifecycle-state`, and join it back to a topic whose `lifecycle-state` is ACTIVE in the same region.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci ons topic list --compartment-id <compartment-ocid> --all --region <region>",
        "oci ons subscription list --compartment-id <compartment-ocid> --all --region <region>",
        "oci ons subscription get --subscription-id <ocid>"
      ],
      "evidence": "A topic object with lifecycle-state ACTIVE plus at least one subscription object whose `topic-id` matches it and whose `lifecycle-state` is ACTIVE (not PENDING); record topic OCID, topic compartment OCID, subscription OCID, protocol, endpoint and region for each pair, together with the list of regions enumerated.",
      "verdict_rule": "PASS when at least one ONS topic with lifecycle-state ACTIVE exists in the tenancy and at least one subscription referencing that topic, in the same region, has lifecycle-state ACTIVE. A topic with zero subscriptions, or with only PENDING subscriptions, is a FAIL; so is a run that enumerated fewer regions than `region-subscription list` returned.",
      "remediation": "`oci ons topic create --name sec-alerts --compartment-id <ocid> --region <r>` then `oci ons subscription create --topic-id <ocid> --protocol EMAIL --subscription-endpoint <mailbox> --compartment-id <ocid> --region <r>`, and have the recipient click the confirmation link so the state moves from PENDING to ACTIVE. Create one topic per subscribed region that carries Events rules, since a rule can only publish to a topic in its own region. Prefer a distribution list or a chat/paging endpoint over one person's mailbox.",
      "caveat": "Two false-pass traps. First, EMAIL subscriptions sit in PENDING until the recipient confirms, and a PENDING subscription silently drops every message, so counting subscriptions instead of testing `lifecycle-state == ACTIVE` passes a dead channel. Second, ONS is regional and compartment-scoped: a topic in the home region does nothing for Events rules created in another region, and a root-compartment-only listing (or a compartment walk run with `--access-level ACCESSIBLE` under a partially privileged principal) misses topics parked in a child compartment. Deliverability itself (mailbox exists, filters do not bin it, the paging integration is live) cannot be proven from the API and needs an out-of-band test message."
    },
    {
      "control_id": "4.3",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert when federation providers are added, altered, or removed",
      "objective": "Adding or editing an identity provider changes who can obtain a session in the tenancy. Those events must reach a human in near-real time, not surface weeks later in an audit export.",
      "procedure": "Work in the tenancy home region (resolve it from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, where `is-home-region` is true). List rules across the compartment tree: `oci events rule list --compartment-id <ocid> --all --region <home>` for the tenancy root and each ACTIVE compartment. For each rule run `oci events rule get --rule-id <ocid>` and parse the `condition` JSON for eventType entries `com.oraclecloud.identitycontrolplane.createidentityprovider`, `.updateidentityprovider`, `.deleteidentityprovider`. Record the rule's own `compartment-id`, confirm `is-enabled` is true, and confirm at least one action of type ONS with `is-enabled: true` whose topic passes the 4.2 delivery check.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>",
        "oci iam identity-provider list --compartment-id <tenancy-ocid> --protocol SAML2 --all",
        "oci iam domain list --compartment-id <tenancy-ocid> --all"
      ],
      "evidence": "The rule OCID and its `compartment-id`, the `condition` string listing the three identityprovider eventTypes, `is-enabled: true`, and an `actions.actions[]` entry with action-type ONS, `is-enabled: true`, and a topic-id resolving to an ACTIVE topic with a subscription in lifecycle-state ACTIVE.",
      "verdict_rule": "PASS when an Events rule whose `compartment-id` equals the tenancy OCID (root compartment) exists in the home region with `is-enabled: true`, a condition matching all three of createidentityprovider, updateidentityprovider and deleteidentityprovider, and an ONS action whose own `is-enabled` is true pointing at a topic with >=1 subscription in lifecycle-state ACTIVE. A matching rule anchored in a child compartment does not satisfy this control, because an Events rule only observes its own compartment and that compartment's descendants.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name idp-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitycontrolplane.createidentityprovider\",\"com.oraclecloud.identitycontrolplane.updateidentityprovider\",\"com.oraclecloud.identitycontrolplane.deleteidentityprovider\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`",
      "caveat": "Identity control-plane events are emitted only in the tenancy home region, so a correct rule built in any other region never fires while looking valid. Test the whole chain: a rule can be enabled while its ONS action has `isEnabled: false`, or point at a topic whose only subscription is PENDING, and a rule created below the root observes only its own subtree. In tenancies migrated to identity domains, IdP objects live inside a domain (enumerate with `oci iam domain list` and the domain's own admin surface) and may emit domain-scoped event types, so a rule matching only the legacy identitycontrolplane types can be a false pass; corroborate against the domain's event configuration."
    },
    {
      "control_id": "4.4",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert when federated-group-to-IAM-group mappings are altered",
      "objective": "Group mappings decide which federated users land in which privileged IAM groups. Silently repointing a mapping is a stealthy privilege grant, so every create, update and delete must raise a notification.",
      "procedure": "In the home region, list Events rules for the tenancy root and every ACTIVE compartment (`oci events rule list --compartment-id <ocid> --all --region <home>`), fetch each with `oci events rule get --rule-id <ocid>`, and search the `condition` JSON for `com.oraclecloud.identitycontrolplane.createidpgroupmapping`, `.updateidpgroupmapping` and `.deleteidpgroupmapping`. Record each rule's own `compartment-id` and `is-enabled`, then follow each ONS action (which must itself have `is-enabled: true`) through `oci ons topic get --topic-id <ocid>` and `oci ons subscription list` to prove a live delivery path. Enumerate current mappings for context with `oci iam identity-provider list` followed by `oci iam idp-group-mapping list --identity-provider-id <ocid> --all`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci iam identity-provider list --compartment-id <tenancy-ocid> --protocol SAML2 --all",
        "oci iam idp-group-mapping list --identity-provider-id <ocid> --all",
        "oci iam domain list --compartment-id <tenancy-ocid> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>"
      ],
      "evidence": "Rule OCID and its `compartment-id`, the `condition` containing the three idpgroupmapping eventTypes, `is-enabled: true` on the rule, and an ONS action with `is-enabled: true` whose topic has a subscription in lifecycle-state ACTIVE.",
      "verdict_rule": "PASS when an Events rule whose `compartment-id` equals the tenancy OCID (root compartment) exists in the home region, is enabled, matches createidpgroupmapping, updateidpgroupmapping and deleteidpgroupmapping, and routes through an enabled ONS action to a topic with at least one ACTIVE subscription. A rule in a child compartment, or an enabled rule whose ONS action is disabled, FAILs.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name idp-group-mapping-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitycontrolplane.createidpgroupmapping\",\"com.oraclecloud.identitycontrolplane.updateidpgroupmapping\",\"com.oraclecloud.identitycontrolplane.deleteidpgroupmapping\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`",
      "caveat": "Home-region-only emission applies here as well, and a rule anchored below the tenancy root sees only its own subtree. The bigger trap is identity domains: mappings for a domain-based IdP are managed through the domain's own admin/SCIM surface and do not necessarily produce the legacy identitycontrolplane idpgroupmapping events, so a migrated tenancy can hold a syntactically perfect, enabled rule that never fires. Where domains are in use (enumerate them with `oci iam domain list`), corroborate against the domain's event configuration instead of trusting the rule alone."
    },
    {
      "control_id": "4.5",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Group membership changes reach an alerting channel",
      "objective": "Because authorization in OCI is granted to groups, creating or deleting a group is a change to the permission model itself and should generate an alert carrying the actor's identity.",
      "procedure": "Home region only. For the tenancy root and each ACTIVE compartment run `oci events rule list --compartment-id <ocid> --all --region <home>`, then `oci events rule get --rule-id <ocid>` and inspect `condition` for `com.oraclecloud.identitycontrolplane.creategroup`, `.updategroup` and `.deletegroup`. Record the rule's `compartment-id`; require `is-enabled: true`, an ONS action whose own `is-enabled` is true, and a topic that resolves to an ACTIVE topic with an ACTIVE subscription. Where the tenancy's realm exposes them, treat the membership event types (`...addusertogroup`, `...removeuserfromgroup`) as an additional strengthening — verify which virtualnetwork/identity event types exist before pinning them into a condition. Baseline the group set with `oci iam group list --compartment-id <tenancy-ocid> --all`, plus `oci identity-domains groups list --endpoint <domain-url> --all` for each domain returned by `oci iam domain list`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci iam group list --compartment-id <tenancy-ocid> --all",
        "oci iam domain list --compartment-id <tenancy-ocid> --all",
        "oci identity-domains groups list --endpoint <domain-url> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>"
      ],
      "evidence": "Rule OCID with its `compartment-id`, the `condition` JSON listing the three group eventTypes, `is-enabled: true` on the rule, and the ONS action (`is-enabled: true`) topic-id with its subscription lifecycle-state ACTIVE.",
      "verdict_rule": "PASS when an enabled Events rule whose `compartment-id` equals the tenancy OCID exists in the home region, matches creategroup, updategroup and deletegroup, and delivers through an enabled ONS action to a topic having >=1 subscription in lifecycle-state ACTIVE. A child-compartment rule, a disabled ONS action, or a PENDING-only subscription FAILs.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name iam-group-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitycontrolplane.creategroup\",\"com.oraclecloud.identitycontrolplane.updategroup\",\"com.oraclecloud.identitycontrolplane.deletegroup\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. Add the membership event types to the same condition where the realm emits them, and pair the rule with a scheduled Logging Search over the audit subtree pseudo-log for group-membership API calls.",
      "caveat": "Matching group create/update/delete does not cover adding a user to an existing privileged group, which is a distinct event type; treating this control as full group-membership monitoring is the classic false sense of coverage, so pair it with an audit query over `search \"<tenancy-ocid>/_Audit_Include_Subcompartment\"` for the membership API calls. Identity events are home-region only and a rule anchored in a child compartment gives no tenancy-wide coverage. In identity-domain tenancies groups are domain objects (listed with the plural `oci identity-domains groups list`, not the singular form) and the legacy event types may not be raised, so verify against a real change if you need certainty."
    },
    {
      "control_id": "4.6",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert whenever authorization policies are written, edited, or deleted",
      "objective": "An IAM policy statement is the only thing between a principal and a resource. Any change to the policy corpus must be visible immediately so an over-broad grant cannot sit unnoticed.",
      "procedure": "In the home region, enumerate Events rules for the tenancy root and every ACTIVE compartment with `oci events rule list --compartment-id <ocid> --all --region <home>` and read each via `oci events rule get --rule-id <ocid>`. Look in `condition` for `com.oraclecloud.identitycontrolplane.createpolicy`, `.updatepolicy` and `.deletepolicy`. Record the rule's `compartment-id`, verify `is-enabled: true`, and verify the ONS action's own `is-enabled` is true and its topic-id passes the 4.2 delivery check. Baseline the policy corpus per compartment with `oci iam policy list --compartment-id <ocid> --all`, driving the compartment set from `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci iam policy list --compartment-id <compartment-ocid> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>"
      ],
      "evidence": "The rule OCID and `compartment-id`, its `condition` containing createpolicy, updatepolicy and deletepolicy, `is-enabled: true`, and an ONS action with `is-enabled: true` pointing at a topic with an ACTIVE subscription.",
      "verdict_rule": "PASS when an enabled Events rule whose `compartment-id` equals the tenancy OCID exists in the home region, matches all three policy eventTypes, and routes through an enabled ONS action to a topic with >=1 subscription in lifecycle-state ACTIVE. A rule found only in a child compartment FAILs, since it observes just that subtree.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name iam-policy-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitycontrolplane.createpolicy\",\"com.oraclecloud.identitycontrolplane.updatepolicy\",\"com.oraclecloud.identitycontrolplane.deletepolicy\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`",
      "caveat": "Policies can be attached in any compartment but the event surfaces in the home region, so build and check the rule there and require it at the root: a rule created in a leaf compartment only observes that compartment and its children. Confirm the ONS action itself is enabled, since a disabled action inside an enabled rule is the most common silent failure. Running the compartment walk with `--access-level ACCESSIBLE` under a partially privileged auditor principal can drop compartments from the policy baseline without any error."
    },
    {
      "control_id": "4.7",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert on IAM principal lifecycle and capability changes",
      "objective": "Creating a user, re-enabling a disabled one, or granting API-key and auth-token capabilities are all persistence moves, so they must generate an alert while the account is still fresh.",
      "procedure": "Home region. Walk the tenancy root and each ACTIVE compartment with `oci events rule list --compartment-id <ocid> --all --region <home>` and `oci events rule get --rule-id <ocid>`; parse `condition` for `com.oraclecloud.identitycontrolplane.createuser`, `.updateuser`, `.deleteuser`, and ideally `.updateusercapabilities` and `.updateuserstate`. Record the rule's `compartment-id`, require `is-enabled: true` on both the rule and its ONS action, and resolve the topic. Baseline the user population with `oci iam user list --compartment-id <tenancy-ocid> --all` for legacy IAM, and for identity-domain tenancies resolve each domain's `url` from `oci iam domain list --compartment-id <ocid> --all` (repeat across compartments) and enumerate with the plural list form `oci identity-domains users list --endpoint <domain-url> --all`; the singular `oci identity-domains user` noun carries only single-user get/create/put/patch/delete and fails with a usage error on `list`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci iam user list --compartment-id <tenancy-ocid> --all",
        "oci iam domain list --compartment-id <tenancy-ocid> --all",
        "oci identity-domains users list --endpoint <domain-url> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>"
      ],
      "evidence": "Rule OCID and `compartment-id`, the `condition` listing the user eventTypes (createuser, updateuser, deleteuser at minimum), `is-enabled: true`, the ONS action's `is-enabled: true` and topic-id with a confirmed (ACTIVE) subscription, plus the per-domain user counts from `oci identity-domains users list`.",
      "verdict_rule": "PASS when an enabled Events rule whose `compartment-id` equals the tenancy OCID exists in the home region, matches createuser, updateuser and deleteuser, and delivers through an ONS action with `is-enabled: true` to a topic with >=1 ACTIVE subscription; capability and state event types are a strengthening, not a requirement. A child-compartment rule or a disabled ONS action FAILs.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name iam-user-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitycontrolplane.createuser\",\"com.oraclecloud.identitycontrolplane.updateuser\",\"com.oraclecloud.identitycontrolplane.deleteuser\",\"com.oraclecloud.identitycontrolplane.updateusercapabilities\",\"com.oraclecloud.identitycontrolplane.updateuserstate\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. In identity-domain tenancies additionally configure the domain's own notification/event delivery for user lifecycle changes.",
      "caveat": "The state that matters may not be where you are looking. In an identity-domain tenancy users are domain objects reachable only through the domain endpoint via the plural `oci identity-domains users list --endpoint <domain-url> --all`, and legacy identitycontrolplane user events may not be emitted for them, so a rule that passes this check can still be blind to every real account creation. Identity events are home-region only, and a rule below the tenancy root covers only its own subtree. `updateuser` also fires on benign profile edits, so teams often disable the ONS action while leaving the rule enabled; check both flags."
    },
    {
      "control_id": "4.8",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert on creation, modification, or deletion of virtual cloud networks",
      "objective": "A new or altered VCN can introduce an entirely new ingress path into the tenancy, so network topology changes should notify the security team rather than being discovered at the next review.",
      "procedure": "VCN events are regional. For every region returned by `oci iam region-subscription list --tenancy-id <tenancy-ocid>` — not merely the regions that currently hold networking — run `oci events rule list --compartment-id <ocid> --all --region <r>` for the tenancy root and each ACTIVE compartment, then `oci events rule get --rule-id <ocid>`, matching `condition` on `com.oraclecloud.virtualnetwork.createvcn`, `.updatevcn` and `.deletevcn`. Record each rule's `compartment-id`, require `is-enabled: true` on the rule and on its ONS action, and confirm the referenced topic lives in the same region with an ACTIVE subscription. Use `oci network vcn list --compartment-id <ocid> --all --region <r>` only as reporting context for which regions are currently populated.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <region>",
        "oci events rule get --rule-id <ocid>",
        "oci network vcn list --compartment-id <compartment-ocid> --all --region <region>",
        "oci search resource structured-search --query-text \"query vcn resources where lifecycleState = 'AVAILABLE'\" --region <region> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <region>"
      ],
      "evidence": "Per subscribed region: a rule OCID with `compartment-id` equal to the tenancy OCID whose `condition` lists createvcn, updatevcn and deletevcn, with `is-enabled: true` and an ONS action (`is-enabled: true`) whose topic resolves in that same region and has a subscription in lifecycle-state ACTIVE. Also record the region list itself and the current VCN inventory per region as context.",
      "verdict_rule": "PASS when, for every region returned by `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, an Events rule in that region's root (tenancy) compartment has `is-enabled: true`, matches createvcn, updatevcn and deletevcn, and routes to an ONS action with `is-enabled: true` whose topic is in the same region and has >=1 ACTIVE subscription. Current VCN inventory must never be used to excuse a region: an unmonitored empty region is exactly where a `createvcn` needs to be caught.",
      "remediation": "In each subscribed region: `oci events rule create --region <r> --compartment-id <tenancy-ocid> --display-name vcn-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.virtualnetwork.createvcn\",\"com.oraclecloud.virtualnetwork.updatevcn\",\"com.oraclecloud.virtualnetwork.deletevcn\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<regional-topic-ocid>\"}]}'`, creating a same-region ONS topic first. Bake the rule into the region-enablement runbook so a newly subscribed region is never left uncovered.",
      "caveat": "This is where single-region checking produces the loudest false pass: Events rules and ONS topics are both regional, so a perfect rule in one region gives zero coverage for a VCN built in another, and scoping the verdict to regions that already contain VCNs makes the result flip over time while leaving the empty regions — the interesting ones — unwatched. The rule's ONS action must target a topic in the same region as the rule and must itself be enabled. A rule created in a child compartment only observes that subtree, so tenancy-wide coverage requires the rule in the root compartment of each subscribed region. Note that `oci search` is itself a regional call and must be looped; its query attribute is `lifecycleState`, not `lifeCycleState`."
    },
    {
      "control_id": "4.9",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert on routing-table modifications inside VCNs",
      "objective": "Route table edits can silently redirect traffic through an attacker-controlled appliance or open a path to the internet through a NAT or DRG, so these changes must be announced when they happen.",
      "procedure": "For each region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>` and for the tenancy root plus each ACTIVE compartment, list and fetch Events rules (`oci events rule list --compartment-id <ocid> --all --region <r>`, `oci events rule get --rule-id <ocid>`) and match `condition` against `com.oraclecloud.virtualnetwork.createroutetable`, `.updateroutetable`, `.deleteroutetable` and, where used, `.changeroutetablecompartment`. Record the rule's `compartment-id`, confirm `is-enabled: true` on the rule and on its ONS action, and confirm the topic is in the same region with an ACTIVE subscription. Inventory route tables for context with `oci network route-table list --compartment-id <ocid> --vcn-id <ocid> --all --region <r>`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <region>",
        "oci events rule get --rule-id <ocid>",
        "oci network vcn list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network route-table list --compartment-id <compartment-ocid> --vcn-id <ocid> --all --region <region>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <region>"
      ],
      "evidence": "Rule OCID, its `compartment-id`, region, the `condition` containing the route-table eventTypes, `is-enabled: true`, and the ONS action (`is-enabled: true`) topic-id with subscription lifecycle-state ACTIVE.",
      "verdict_rule": "PASS when, in every subscribed region, an Events rule whose `compartment-id` equals the tenancy OCID (or, for a deliberately decentralised design, a rule set whose compartments together cover every compartment that can hold a route table) is enabled, matches createroutetable, updateroutetable and deleteroutetable, and delivers through an enabled ONS action to a same-region ONS topic with >=1 ACTIVE subscription.",
      "remediation": "Per region: `oci events rule create --region <r> --compartment-id <tenancy-ocid> --display-name route-table-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.virtualnetwork.createroutetable\",\"com.oraclecloud.virtualnetwork.updateroutetable\",\"com.oraclecloud.virtualnetwork.deleteroutetable\",\"com.oraclecloud.virtualnetwork.changeroutetablecompartment\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. Pair it with a scheduled Logging Search over `search \"<tenancy-ocid>/_Audit_Include_Subcompartment\"` for UpdateRouteTable so the alert can be correlated with the actual route change.",
      "caveat": "Two scoping traps rather than one. Regionally, check every subscribed region, not just the home region or the regions that currently hold VCNs. By compartment, an Events rule only observes its own compartment and that compartment's descendants, so a rule anchored below the root leaves route-table changes in sibling compartments unalerted while satisfying a naive presence check — capture the rule's `compartment-id` as evidence. The update event fires on the table object and carries no before/after route detail, so the alert proves detection exists but never establishes whether the change was dangerous, which is why it must be paired with an audit-subtree query."
    },
    {
      "control_id": "4.10",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Subnet firewall edits reach an alerting channel",
      "objective": "Security lists are the stateful firewall for a subnet, and loosening one to 0.0.0.0/0 is a one-command exposure, so create, update and delete on these objects must be alerted.",
      "procedure": "Loop every subscribed region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>` and, within each, the tenancy root plus every ACTIVE compartment. Run `oci events rule list --compartment-id <ocid> --all --region <r>` then `oci events rule get --rule-id <ocid>`; search `condition` for `com.oraclecloud.virtualnetwork.createsecuritylist`, `.updatesecuritylist` and `.deletesecuritylist`. Record the rule's `compartment-id`, require `is-enabled: true` on the rule and on the ONS action, and require a same-region topic with an ACTIVE subscription. Use `oci network security-list list --compartment-id <ocid> --vcn-id <ocid> --all --region <r>` as inventory context only.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <region>",
        "oci events rule get --rule-id <ocid>",
        "oci network security-list list --compartment-id <compartment-ocid> --vcn-id <ocid> --all --region <region>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <region>"
      ],
      "evidence": "The rule OCID with its `compartment-id` and region, the `condition` JSON with the three securitylist eventTypes, `is-enabled: true`, and an ONS action (`is-enabled: true`) whose topic has a confirmed ACTIVE subscription in the same region.",
      "verdict_rule": "PASS when, in every subscribed region, an enabled Events rule whose `compartment-id` equals the tenancy OCID matches createsecuritylist, updatesecuritylist and deletesecuritylist and publishes through an enabled ONS action to a same-region ONS topic with >=1 ACTIVE subscription. A rule in a child compartment, a disabled ONS action, a cross-region topic, or a region skipped because it holds no VCNs today all FAIL.",
      "remediation": "Per region: `oci events rule create --region <r> --compartment-id <tenancy-ocid> --display-name security-list-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.virtualnetwork.createsecuritylist\",\"com.oraclecloud.virtualnetwork.updatesecuritylist\",\"com.oraclecloud.virtualnetwork.deletesecuritylist\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`",
      "caveat": "Security lists and network security groups are two different enforcement objects; a rule covering only security lists leaves NSG changes (control 4.11) unmonitored, and teams frequently assume one implies the other. Regional scoping must follow the subscription list rather than current inventory, and the rule must sit in the root compartment — an Events rule below the root observes only its own subtree, so record `compartment-id` in evidence. The default security list of a VCN is edited in place, so the event you see is updatesecuritylist with no indication of which rule changed; the notification proves detection exists, not that the change was reviewed."
    },
    {
      "control_id": "4.11",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Security group edits reach an alerting channel",
      "objective": "NSGs attach firewall rules directly to VNICs and are the preferred enforcement point in modern OCI designs, so their creation, modification and deletion must surface as security-relevant events.",
      "procedure": "For each subscribed region and for the tenancy root plus each ACTIVE compartment run `oci events rule list --compartment-id <ocid> --all --region <r>`, then `oci events rule get --rule-id <ocid>` and match `condition` on `com.oraclecloud.virtualnetwork.createnetworksecuritygroup`, `.updatenetworksecuritygroup` and `.deletenetworksecuritygroup`. Record the rule's `compartment-id`, verify `is-enabled: true` on rule and ONS action, and verify the topic is same-region with a confirmed subscription. Then settle the rule-member question deterministically instead of leaving it open: enumerate the virtualnetwork event types the tenancy's realm actually emits (Events service reference plus `oci events rule create --generate-param-json-input condition`, corroborated by a live test that adds and removes a throwaway rule in a scratch NSG and observes what is delivered). If rule-member event types exist, add them to the required condition; if they do not, require the compensating audit query described in the remediation. Enumerate NSGs via `oci network nsg list --compartment-id <ocid> --all --region <r>` and their rules via `oci network nsg rules list --nsg-id <ocid> --all`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <region>",
        "oci events rule get --rule-id <ocid>",
        "oci network nsg list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network nsg rules list --nsg-id <ocid> --all",
        "oci logging-search search-logs --search-query \"search \\\"<tenancy-ocid>/_Audit_Include_Subcompartment\\\"\" --time-start <t0> --time-end <t1> --region <region>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <region>"
      ],
      "evidence": "Rule OCID, `compartment-id` and region, the `condition` listing the three networksecuritygroup eventTypes (plus any rule-member event types the realm emits), `is-enabled: true`, and the ONS action (`is-enabled: true`) topic-id with an ACTIVE subscription. Where rule-member events do not exist, the saved Logging Search query and its most recent result set stand as the compensating evidence.",
      "verdict_rule": "PASS when, in every subscribed region, an enabled Events rule whose `compartment-id` equals the tenancy OCID matches createnetworksecuritygroup, updatenetworksecuritygroup and deletenetworksecuritygroup and delivers through an enabled ONS action to a same-region ONS topic with >=1 ACTIVE subscription AND changes to the rule members of an existing NSG are covered — either by the realm's rule-member event types included in the same condition, or by a scheduled audit query on the NSG security-rule API calls. Object-lifecycle coverage alone, with no rule-member coverage of any kind, FAILs.",
      "remediation": "Per region: `oci events rule create --region <r> --compartment-id <tenancy-ocid> --display-name nsg-changes --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.virtualnetwork.createnetworksecuritygroup\",\"com.oraclecloud.virtualnetwork.updatenetworksecuritygroup\",\"com.oraclecloud.virtualnetwork.deletenetworksecuritygroup\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. Where the realm emits rule-member event types, append them to the same eventType array. Otherwise add the compensating detection: `oci logging-search search-logs --search-query \"search \\\"<tenancy-ocid>/_Audit_Include_Subcompartment\\\" | where data.request.action = 'POST' and (data.eventName = 'AddNetworkSecurityGroupSecurityRules' or data.eventName = 'UpdateNetworkSecurityGroupSecurityRules' or data.eventName = 'RemoveNetworkSecurityGroupSecurityRules')\" --time-start <t0> --time-end <t1> --region <r>` on a schedule that publishes to the same ONS topic.",
      "caveat": "These event types cover the NSG object, not its rule members: adding a 0.0.0.0/0 ingress rule to an existing NSG is the highest-value change and is the one most likely to go unalerted. Do not leave that as an open question for the assessor — establish empirically whether rule-member event types exist in the tenancy's realm and, if they do not, require the audit-log compensating query and mark object-only coverage as a FAIL. Note the audit query must use the subtree pseudo-log `_Audit_Include_Subcompartment`; the plain `_Audit` log returns root-compartment events only and will report zero NSG rule changes in a tenancy whose workloads live in child compartments. Regional and compartment scoping apply exactly as in 4.8-4.10: the rule belongs in the root compartment, one per subscribed region, with the ONS topic co-located."
    },
    {
      "control_id": "4.12",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Attaching or detaching an edge gateway reaches an alerting channel",
      "objective": "Gateways are the doors between a VCN and everything outside it, so attaching an internet or NAT gateway, or standing up a DRG, changes the tenancy's exposure and must trigger a notification.",
      "procedure": "For each region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>` and for the tenancy root plus each ACTIVE compartment, fetch Events rules (`oci events rule list --compartment-id <ocid> --all --region <r>`; `oci events rule get --rule-id <ocid>`) and check `condition` for the create/update/delete triples of `com.oraclecloud.virtualnetwork.*` covering drg, drgattachment, internetgateway, natgateway, servicegateway and localpeeringgateway. Record the rule's `compartment-id`; require `is-enabled: true` on the rule and on its ONS action, and a same-region topic with an ACTIVE subscription. Inventory the gateway families actually present per region for reporting context, but never use that inventory to exempt a region.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <region>",
        "oci events rule get --rule-id <ocid>",
        "oci network internet-gateway list --compartment-id <compartment-ocid> --vcn-id <ocid> --all --region <region>",
        "oci network nat-gateway list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network service-gateway list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network drg list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network drg-attachment list --compartment-id <compartment-ocid> --all --region <region>",
        "oci network local-peering-gateway list --compartment-id <compartment-ocid> --vcn-id <ocid> --all --region <region>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <region>"
      ],
      "evidence": "Rule OCID, `compartment-id` and region; the `condition` enumerating create, update and delete event types for drg, drgattachment, internetgateway, natgateway, servicegateway and localpeeringgateway; `is-enabled: true`; and an ONS action (`is-enabled: true`) whose topic has an ACTIVE subscription in the same region. Record the per-region gateway inventory alongside it.",
      "verdict_rule": "PASS when, for every subscribed region — and at a minimum every region that contains any of VCN, DRG, DRG attachment, internet gateway, NAT gateway, service gateway or local peering gateway — an Events rule in the tenancy root compartment of that region has `is-enabled: true`, matches create, update and delete for all six families, and delivers through an ONS action with `is-enabled: true` to a same-region topic with >=1 ACTIVE subscription. A rule covering only the internet gateway, a rule anchored in a child compartment, or a region excused because it holds no VCN today (a DRG is a standalone regional resource and needs none) all FAIL.",
      "remediation": "Per region create one rule whose condition array includes com.oraclecloud.virtualnetwork.{create,update,delete} for drg, drgattachment, internetgateway, natgateway, servicegateway and localpeeringgateway, with an enabled ONS action: `oci events rule create --region <r> --compartment-id <tenancy-ocid> --display-name gateway-changes --is-enabled true --condition <json-with-all-six-families> --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<same-region-topic-ocid>\"}]}'`",
      "caveat": "This control spans several distinct resource families, and the common partial implementation covers only the internet gateway, which passes a naive 'is there a gateway rule' check while leaving DRG, DRG attachment and NAT unmonitored; match every family explicitly in the condition. A DRG exists independently of any VCN, so scoping coverage to regions that currently contain a VCN exempts precisely the region where a new DRG would be stood up — drive the region loop from the subscription list. Regional co-location of the ONS topic, an enabled ONS action, and root-compartment anchoring apply as with the other networking event controls."
    },
    {
      "control_id": "4.13",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 2,
      "assessment": "Automated",
      "service": "logging",
      "title": "Subnet traffic is recorded so an investigation has data to work from",
      "objective": "Without flow logs there is no record of who talked to what inside the VCN, which makes lateral-movement investigation and exfiltration analysis impossible after the fact.",
      "procedure": "Build the compartment set once with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` (`--include-root` takes no value). For every region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, enumerate subnets with `oci search resource structured-search --query-text \"query subnet resources where lifecycleState = 'AVAILABLE'\" --region <r> --all` (a regional call, repeated per region) or `oci network subnet list --compartment-id <ocid> --vcn-id <ocid> --all --region <r>`. Then enumerate logs across the whole compartment set, not just the subnet's own compartment: `oci logging log-group list --compartment-id <ocid> --all --region <r>` and `oci logging log list --log-group-id <ocid> --all --region <r>`. Match logs where `configuration.source.service` is flowlogs, `configuration.source.category` is all, and `configuration.source.resource` is the subnet OCID or its parent VCN OCID.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci search resource structured-search --query-text \"query subnet resources where lifecycleState = 'AVAILABLE'\" --region <region> --all",
        "oci network subnet list --compartment-id <compartment-ocid> --vcn-id <ocid> --all --region <region>",
        "oci logging log-group list --compartment-id <compartment-ocid> --all --region <region>",
        "oci logging log list --log-group-id <ocid> --all --region <region>",
        "oci logging log get --log-group-id <ocid> --log-id <ocid>"
      ],
      "evidence": "For each subnet OCID (with its VCN OCID, compartment and region): a log object with `lifecycle-state: ACTIVE`, `is-enabled: true`, `configuration.source.service: flowlogs`, `configuration.source.category: all`, and `configuration.source.resource` equal to that subnet OCID or its VCN OCID, plus the log's own OCID, log group and region. Record subnet counts per region so an empty enumeration is visible.",
      "verdict_rule": "PASS when every AVAILABLE subnet in every subscribed region is covered by a Logging log that is simultaneously `lifecycle-state: ACTIVE` and `is-enabled: true`, of service flowlogs and category all, whose `source.resource` is that subnet or its parent VCN and whose region matches the subnet's region. Any uncovered subnet is a FAIL; so is a region whose subnet enumeration returned zero while VCNs exist there, which indicates an enumeration failure rather than compliance.",
      "remediation": "Create a log group then a flow log per subnet or per VCN: `oci logging log-group create --compartment-id <ocid> --display-name flowlogs --region <r>`; `oci logging log create --log-group-id <ocid> --display-name <subnet>-flow --log-type SERVICE --is-enabled true --retention-duration 90 --region <r> --configuration '{\"compartmentId\":\"<ocid>\",\"source\":{\"sourceType\":\"OCISERVICE\",\"service\":\"flowlogs\",\"resource\":\"<subnet-or-vcn-ocid>\",\"category\":\"all\"}}'`. Prefer VCN-level logs so new subnets inherit coverage, and set retention to at least the incident-investigation window.",
      "caveat": "Four false-pass traps. (1) A log object can exist with `is-enabled: false` or lifecycle-state CREATING/FAILED, so presence is not coverage; test both fields. (2) Flow logging can be attached at the VCN level, which covers its subnets, so matching strictly on subnet OCIDs yields false FAILs while matching only VCNs misses subnets in VCNs without a parent-level log — accept either and record which. (3) Logs and subnets are both regional and compartment-scoped, and the log group usually lives in a central logging compartment different from the subnet's compartment, so the two enumerations must be joined tenancy-wide rather than per compartment, and the compartment walk must use `--access-level ANY` or it can silently drop the logging compartment. (4) The resource-search attribute is `lifecycleState`; a misspelt query returns nothing and a zero-subnet result then reads as a clean tenancy. Retention shorter than the investigation window is a real gap even on a pass."
    },
    {
      "control_id": "4.14",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "cloud-guard",
      "title": "Cloud Guard observes the whole tenancy from the root compartment down",
      "objective": "Cloud Guard is OCI's native posture and threat detection service; enabled at the tenancy root with recipes attached, it continuously flags misconfiguration and suspicious activity across every compartment.",
      "procedure": "Run `oci cloud-guard configuration get --compartment-id <tenancy-ocid>` and read `status`, `reporting-region` and `self-manage-resources`. Then prove coverage exists across the whole tree, because targets can be created in child compartments: `oci cloud-guard target list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ACCESSIBLE --all` (the Cloud Guard access-level enum is ACCESSIBLE/RESTRICTED — `ANY` is not valid here). For each returned target run `oci cloud-guard target get --target-id <ocid>` and check that `target-resource-type` is COMPARTMENT, `target-resource-id` equals the tenancy OCID, `lifecycle-state` is ACTIVE, and the detector and responder recipe collections are non-empty. Inspect the target's effective detector rules (`oci cloud-guard target-detector-recipe list --target-id <ocid> --compartment-id <ocid> --all`) for rules that have been disabled or had their risk level lowered.",
      "api": [
        "oci cloud-guard configuration get --compartment-id <tenancy-ocid>",
        "oci cloud-guard target list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ACCESSIBLE --all",
        "oci cloud-guard target get --target-id <ocid>",
        "oci cloud-guard target-detector-recipe list --target-id <ocid> --compartment-id <ocid> --all",
        "oci cloud-guard detector-recipe list --compartment-id <tenancy-ocid> --all",
        "oci cloud-guard problem list --compartment-id <tenancy-ocid> --all"
      ],
      "evidence": "The configuration response with `status: ENABLED` and a populated `reporting-region`; plus a target object with `target-resource-type: COMPARTMENT`, `target-resource-id` equal to the tenancy root OCID, `lifecycle-state: ACTIVE`, the target's own compartment OCID, and its attached detector recipes together with the enabled/disabled state and risk level of their effective rules.",
      "verdict_rule": "PASS when Cloud Guard configuration `status` is ENABLED for the tenancy AND the subtree target listing contains at least one target with `lifecycle-state: ACTIVE` whose `target-resource-id` equals the tenancy root compartment OCID and which has >=1 attached detector recipe containing at least one rule with `is-enabled: true`. Status ENABLED with no target, with only child-compartment-scoped targets, or with a recipe whose entire rule set is disabled, all FAIL.",
      "remediation": "`oci cloud-guard configuration update --compartment-id <tenancy-ocid> --status ENABLED --reporting-region <region>`, then create a root-scoped target: `oci cloud-guard target create --compartment-id <tenancy-ocid> --display-name tenancy-target --target-resource-type COMPARTMENT --target-resource-id <tenancy-ocid>` and attach the Oracle-managed configuration and activity detector recipes plus a responder recipe. Where cloned recipes are used, diff their rules against the Oracle-managed originals and re-enable anything switched off.",
      "caveat": "`status: ENABLED` on its own is the false pass: the service can report enabled while every target has been deleted, or while the only target points at a child compartment, leaving sibling compartments unmonitored. Enumerating targets without `--compartment-id-in-subtree true` is the mirror-image error — it returns only targets whose own compartment is the root and can drive a FAIL on a compliant tenancy or hide the child-compartment targets entirely. Cloned detector recipes are the third trap, since a target can carry a recipe whose individual rules have been switched off or had their risk levels lowered; inspect the effective rules, not just the recipe's presence. Cloud Guard has a single reporting region, so problems are visible only there, and the set of monitored regions must be confirmed separately."
    },
    {
      "control_id": "4.15",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "cloud-guard",
      "title": "Cloud Guard detections reach a person, not only the console",
      "objective": "Cloud Guard findings nobody sees are not a control. Detected problems should be pushed to an alerting channel as they are raised rather than waiting for someone to open the console.",
      "procedure": "Determine the reporting region from `oci cloud-guard configuration get --compartment-id <tenancy-ocid>`. In that region, list Events rules for the tenancy root and every ACTIVE compartment (`oci events rule list --compartment-id <ocid> --all --region <reporting-region>`) and read each with `oci events rule get --rule-id <ocid>`, matching `condition` on `com.oraclecloud.cloudguard.problemdetected`. Read the entire condition, not only the eventType array, so additionalDetails filters are visible. Record the rule's `compartment-id`, verify `is-enabled: true` on the rule and `is-enabled: true` on the ONS action, then resolve the topic with `oci ons topic get --topic-id <ocid>` and `oci ons subscription list --compartment-id <ocid> --all --region <reporting-region>`.",
      "api": [
        "oci cloud-guard configuration get --compartment-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <reporting-region>",
        "oci events rule get --rule-id <ocid>",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <reporting-region>"
      ],
      "evidence": "An Events rule in the reporting region whose `compartment-id` is the tenancy OCID, whose `condition` contains com.oraclecloud.cloudguard.problemdetected together with any additionalDetails filters present verbatim, `is-enabled: true`, and an ONS action (`is-enabled: true`) whose topic has a subscription in lifecycle-state ACTIVE.",
      "verdict_rule": "PASS when an enabled Events rule whose `compartment-id` equals the tenancy OCID exists in the Cloud Guard reporting region, matches com.oraclecloud.cloudguard.problemdetected, and publishes through an enabled ONS action to a topic with >=1 ACTIVE subscription. A rule in another region, a rule in a child compartment, a disabled ONS action, or a condition narrowed by additionalDetails so that lower-risk problems are dropped without documented acceptance, FAILs.",
      "remediation": "`oci events rule create --region <reporting-region> --compartment-id <tenancy-ocid> --display-name cloudguard-problems --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.cloudguard.problemdetected\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. Optionally add a Cloud Guard responder or a second rule dedicated to high-risk problems, but keep an unfiltered rule as the baseline.",
      "caveat": "Cloud Guard emits problem events only in the reporting region, so a rule built in the home region when the reporting region differs never fires while looking entirely correct. Scoping the rule to a child compartment limits it to that subtree, so capture and test the rule's `compartment-id`. Narrowing the condition with additionalDetails filters (for example only CRITICAL risk) reduces coverage in a way a presence check will not notice, so read the whole condition rather than just the eventType array, and check the ONS action's own enabled flag. The control is also meaningless while 4.14 fails, because no targets means no problems to notify on."
    },
    {
      "control_id": "4.16",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "kms-vault",
      "title": "Re-key customer-managed vault keys on a cycle no longer than one year",
      "objective": "Rotating a master encryption key limits how much data a single compromised key version can decrypt and bounds the blast radius of key material exposure.",
      "procedure": "Build the compartment set once: `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all`. Per region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, list vaults across every compartment with `oci kms vault list --compartment-id <ocid> --all --region <r>`. For each vault in lifecycle-state ACTIVE take its `management-endpoint`, then iterate the FULL compartment set against that one endpoint — a key's compartment is independent of its vault's compartment — running `oci kms management key list --compartment-id <c> --endpoint <mgmt-endpoint> --all` for every compartment c. For every key with lifecycle-state ENABLED run `oci kms management key-version list --key-id <ocid> --endpoint <mgmt-endpoint> --all` and compute the newest `time-created`. Compare that timestamp with the assessment date and record `protection-mode`.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci kms vault list --compartment-id <compartment-ocid> --all --region <region>",
        "oci kms vault get --vault-id <ocid>",
        "oci kms management key list --compartment-id <compartment-ocid> --endpoint <vault-management-endpoint> --all",
        "oci kms management key get --key-id <ocid> --endpoint <vault-management-endpoint>",
        "oci kms management key-version list --key-id <ocid> --endpoint <vault-management-endpoint> --all",
        "oci search resource structured-search --query-text \"query vault resources where lifecycleState = 'ACTIVE'\" --region <region> --all"
      ],
      "evidence": "Per key OCID: the (vault OCID, key compartment OCID) pair, the maximum `time-created` across its key-version list, the key's `protection-mode` and `lifecycle-state`, and the owning vault's region and management endpoint. Record the number of compartments queried per vault so an under-scoped enumeration is visible.",
      "verdict_rule": "PASS when, for every ENABLED customer-managed key found by iterating every compartment against every ACTIVE vault's management endpoint in every subscribed region, the newest key version's `time-created` is within 365 days of the assessment date. A vault in lifecycle-state ACTIVE that yields zero keys across all compartments is an enumeration error and must be reported as such, never scored as a pass. Keys with `protection-mode: EXTERNAL` are recorded as documented exceptions rather than automatic FAILs.",
      "remediation": "Rotate each stale key with `oci kms management key-version create --key-id <ocid> --endpoint <vault-management-endpoint>`. Then enable Vault auto-rotation where the key type and protection mode support it, or add a scheduled job, and re-encrypt or re-wrap data that must move to the new version for services that do not do so transparently (for example `oci os bucket reencrypt` for Object Storage).",
      "caveat": "Two enumeration traps precede any evaluation. First, the management commands fail without `--endpoint <vault management-endpoint>` because the KMS management API is per-vault rather than regional, and an assessor who omits it sees an error and may record a vacuous pass. Second, listing keys only in the vault's own compartment misses every key created in a workload compartment inside a centrally owned vault, which is the normal enterprise layout — iterate the full compartment set against each vault endpoint and treat a zero-key ACTIVE vault as a broken enumeration. Only customer-created keys are in scope; Oracle-managed keys must not be flagged. Keys with `protection-mode: EXTERNAL` live in a third-party HSM and cannot be rotated through this API, so they need a documented exception. Creating a new key version does not re-encrypt existing data, so a fresh rotation date does not prove old ciphertext left the old version."
    },
    {
      "control_id": "4.17",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 2,
      "assessment": "Automated",
      "service": "logging",
      "title": "Record object write operations for every Object Storage bucket",
      "objective": "Write-level Object Storage logs are the only record of who created, overwrote or deleted an object. Without them, data tampering and staged exfiltration inside a bucket cannot be reconstructed.",
      "procedure": "Get the tenancy-global namespace with `oci os ns get` (no region argument — the namespace is not regional). Build the compartment set with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all`. For each subscribed region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, enumerate buckets with `oci os bucket list -ns <namespace> --compartment-id <ocid> --all --region <r>` per compartment, or `oci search resource structured-search --query-text \"query bucket resources\" --region <r> --all`. Then enumerate logs across the whole compartment set: `oci logging log-group list --compartment-id <ocid> --all --region <r>` and `oci logging log list --log-group-id <ocid> --all --region <r>`. Match logs where `configuration.source.service` is objectstorage, `configuration.source.category` is write, and `configuration.source.resource` equals the bucket NAME, requiring the log's region to equal the bucket's region.",
      "api": [
        "oci os ns get",
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci os bucket list -ns <namespace> --compartment-id <compartment-ocid> --all --region <region>",
        "oci search resource structured-search --query-text \"query bucket resources\" --region <region> --all",
        "oci logging log-group list --compartment-id <compartment-ocid> --all --region <region>",
        "oci logging log list --log-group-id <ocid> --all --region <region>",
        "oci logging log get --log-group-id <ocid> --log-id <ocid>"
      ],
      "evidence": "For each bucket name plus its compartment and region: a log with `lifecycle-state: ACTIVE`, `is-enabled: true`, `configuration.source.service: objectstorage`, `configuration.source.category: write`, `configuration.source.resource` equal to that bucket's name, and the log's own region matching the bucket's. Record bucket counts per region so a silent enumeration failure is visible.",
      "verdict_rule": "PASS when every bucket in every subscribed region is matched by a Logging log that is both `lifecycle-state: ACTIVE` and `is-enabled: true`, of service objectstorage and category write, whose `source.resource` equals the bucket name AND whose region equals the bucket's region. Any unmatched bucket is a FAIL; a name match across different regions does not count.",
      "remediation": "`oci logging log create --log-group-id <ocid> --display-name <bucket>-write --log-type SERVICE --is-enabled true --retention-duration 90 --region <r> --configuration '{\"compartmentId\":\"<ocid>\",\"source\":{\"sourceType\":\"OCISERVICE\",\"service\":\"objectstorage\",\"resource\":\"<bucket-name>\",\"category\":\"write\"}}'`, repeated for every bucket in every region, and wired into the bucket-provisioning pipeline so new buckets never ship without it.",
      "caveat": "Object Storage log sources are keyed by bucket NAME, not OCID, and bucket names are unique only within a namespace-and-region pair, so matching names without also matching the log's region can pair a bucket in one region with a log in another and produce a false pass. Buckets are namespace-scoped but compartment-assigned and can be moved between compartments, so a root-compartment-only enumeration under-counts, and a compartment walk run with `--access-level ACCESSIBLE` can silently drop compartments along with their buckets. A `read` category log does not satisfy this control, and a log that exists with `is-enabled: false` or in CREATING/FAILED state records nothing. New buckets are the usual gap: this is a point-in-time snapshot unless logging is enforced at provisioning."
    },
    {
      "control_id": "4.18",
      "section": "4",
      "section_name": "Logging and Monitoring",
      "level": 1,
      "assessment": "Automated",
      "service": "events-notifications",
      "title": "Alert on interactive console sign-in by local (non-federated) users",
      "objective": "Local IAM users bypass the enterprise IdP and its conditional-access controls, so every interactive console sign-in by such an account should raise a notification and unexpected break-glass use gets caught.",
      "procedure": "Work in the tenancy home region. List Events rules for the tenancy root and each ACTIVE compartment with `oci events rule list --compartment-id <ocid> --all --region <home>` and read each with `oci events rule get --rule-id <ocid>`; match `condition` on `com.oraclecloud.identitysignon.interactivelogin`. Record the rule's `compartment-id`, confirm `is-enabled: true` on the rule and `is-enabled: true` on the ONS action, then resolve the topic via `oci ons topic get --topic-id <ocid>` and confirm a subscription with lifecycle-state ACTIVE. Cross-reference the local account population across every identity domain, not just legacy IAM: enumerate domains with `oci iam domain list --compartment-id <ocid> --all` (repeat over the compartment tree), read each domain's `url`, and list users with the plural form `oci identity-domains users list --endpoint <domain-url> --all`; supplement with `oci iam user list --compartment-id <tenancy-ocid> --all` for the legacy/default-domain population.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci events rule list --compartment-id <tenancy-ocid> --all --region <home-region>",
        "oci events rule get --rule-id <ocid>",
        "oci iam user list --compartment-id <tenancy-ocid> --all",
        "oci iam domain list --compartment-id <tenancy-ocid> --all",
        "oci identity-domains users list --endpoint <domain-url> --all",
        "oci ons topic get --topic-id <ocid>",
        "oci ons subscription list --compartment-id <ocid> --all --region <home-region>"
      ],
      "evidence": "The rule OCID and its `compartment-id`, the `condition` containing com.oraclecloud.identitysignon.interactivelogin, `is-enabled: true`, and an ONS action (`is-enabled: true`) whose topic-id resolves to an ACTIVE topic with a subscription in lifecycle-state ACTIVE; plus the per-domain local-user inventory used to justify the alert's relevance.",
      "verdict_rule": "PASS when an Events rule whose `compartment-id` equals the tenancy OCID (root compartment) exists in the tenancy home region with `is-enabled: true`, matches com.oraclecloud.identitysignon.interactivelogin, and delivers through an ONS action with `is-enabled: true` to a topic with >=1 ACTIVE subscription. Sign-on events surface at the root, so a rule anchored in a child compartment FAILs.",
      "remediation": "`oci events rule create --region <home> --compartment-id <tenancy-ocid> --display-name local-user-signin --is-enabled true --condition '{\"eventType\":[\"com.oraclecloud.identitysignon.interactivelogin\"]}' --actions '{\"actions\":[{\"actionType\":\"ONS\",\"isEnabled\":true,\"topicId\":\"<ocid>\"}]}'`. Pair it with reducing the local-user population, in every domain, to documented break-glass accounts only, and enable the domain's own sign-on notification where the tenancy uses identity domains.",
      "caveat": "Sign-on events are emitted only in the home region and surface at the root compartment, so a rule elsewhere — by region or by compartment — is inert while looking correct. The event covers interactive console logins by local users; federated logins are authenticated at the IdP and never appear here, so this must not be read as full authentication monitoring. In identity-domain tenancies sign-on telemetry is produced by the domain and the legacy identitysignon event type may not fire, which makes a syntactically correct rule a false pass; the population itself is also under-counted by `oci iam user list`, which returns only the legacy/default-domain users, so enumerate every domain with `oci iam domain list` plus the plural `oci identity-domains users list`. Whether an alert is actually triaged by a human is a process question the API cannot settle."
    },
    {
      "control_id": "5.1.1",
      "section": "5",
      "section_name": "Storage",
      "level": 1,
      "assessment": "Automated",
      "service": "object-storage",
      "title": "Deny anonymous internet read on every Object Storage bucket",
      "objective": "No bucket should serve objects to unauthenticated callers. An OCI bucket set to ObjectRead or ObjectReadWithoutList is reachable over the public Object Storage endpoint with no signed request, so any object in it is world-readable and the only remaining protection is the obscurity of the object name.",
      "procedure": "Get the tenancy-global namespace with `oci os ns get`. Enumerate subscribed regions with `oci iam region-subscription list --tenancy-id <tenancy-ocid>` and every ACTIVE compartment including the tenancy root with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` (`--include-root` is valueless; `--access-level ACCESSIBLE` can silently drop compartments for an auditor principal and hide their buckets). Buckets are regional, so per region run resource search for Bucket to collect names and compartments, then call `oci os bucket get -ns <ns> --name <bucket> --region <r>` on each, because list output and resource search both omit `public-access-type`. Read `public-access-type`. Also list pre-authenticated requests per bucket, since a PAR grants unauthenticated access independently of that field.",
      "api": [
        "oci os ns get",
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci search resource structured-search --query-text \"query bucket resources\" --region <region> --all",
        "oci os bucket get -ns <namespace> --name <bucket> --region <region>",
        "oci os preauth-request list -ns <namespace> --bucket-name <bucket> --all --region <region>",
        "oci cloud-guard detector-recipe-detector-rule list --detector-recipe-id <recipe-ocid> --all",
        "oci cloud-guard detector-recipe-detector-rule get --detector-recipe-id <recipe-ocid> --detector-rule-id BUCKET_IS_PUBLIC"
      ],
      "evidence": "The `public-access-type` field of each `oci os bucket get` response, keyed by namespace + bucket name + compartment OCID + region; plus the full `oci os preauth-request list` array for each bucket with each PAR's `access-type`, `object-name` and `time-expires`; plus the compartment and region counts enumerated.",
      "verdict_rule": "PASS when, for every bucket in every subscribed region and every compartment, `public-access-type` == \"NoPublicAccess\" AND the bucket carries no pre-authenticated request whose `time-expires` is in the future and whose `access-type` confers read (ObjectRead, ObjectReadWrite, AnyObjectRead or AnyObjectReadWrite). Any other `public-access-type` value FAILs, as does any live read-capable PAR. A run that inspected fewer regions or compartments than were enumerated is inconclusive, not a pass.",
      "remediation": "Set the bucket back to private: `oci os bucket update -ns <namespace> --name <bucket> --public-access-type NoPublicAccess`. Delete unnecessary PARs with `oci os preauth-request delete -ns <namespace> --bucket-name <bucket> --par-id <id>`. Prevent recurrence with an IAM policy denying `object-family` public-access changes and enable the Cloud Guard public-bucket detector rule for continuous detection.",
      "caveat": "Four false-pass traps. (1) The Object Storage namespace is tenancy-wide but buckets are per-region: a check that only queries the home region silently passes buckets in every other subscribed region, and `oci iam region-subscription list --all` is not a valid invocation (the operation is not paginated) — use `--tenancy-id <tenancy-ocid>`, or the region loop dies on its first call and the whole sweep never runs. (2) `oci os bucket list` does not return publicAccessType, and neither does resource search, so an assessor who stops at the list sees no evidence of exposure and reports clean — a per-bucket `bucket get` is mandatory. (3) A bucket with NoPublicAccess is still anonymously readable through an unexpired pre-authenticated request; PARs are invisible to the bucket's own access-type field and are the most common way an audited-clean bucket is actually public. (4) Enumerating compartments with `--access-level ACCESSIBLE` drops compartments the auditor cannot directly inspect, and their buckets are then never examined at all. Cloud Guard problem state is a detective signal only and lags configuration, so it must not be used as the primary verdict; confirm the detector rule id for the tenancy with `detector-recipe-detector-rule list` rather than hard-coding a guessed constant, and note the `get` operation is keyed only on recipe id plus rule id."
    },
    {
      "control_id": "5.1.2",
      "section": "5",
      "section_name": "Storage",
      "level": 2,
      "assessment": "Automated",
      "service": "object-storage",
      "title": "Back Object Storage buckets with a tenancy-owned Vault key",
      "objective": "Buckets encrypted with the Oracle-managed default key give the tenancy no ability to rotate, disable, or destroy the encryption material. Binding each bucket to a Vault (KMS) master encryption key the customer owns puts crypto-shredding and rotation under tenancy control and makes key use auditable.",
      "procedure": "Resolve the namespace via `oci os ns get` and the regions via `oci iam region-subscription list --tenancy-id <tenancy-ocid>`. Build the compartment set with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all`. For each subscribed region, enumerate buckets across all compartments (tenancy root included) using resource search for Bucket, then call `oci os bucket get -ns <ns> --name <bucket> --region <r>` per bucket and read `kms-key-id`. A null or absent value means the Oracle-managed key. For every non-null key OCID, verify the key itself with `oci kms management key get --key-id <ocid> --endpoint <vault-management-endpoint>` against that key's own vault management endpoint and confirm `lifecycle-state: ENABLED`, not PENDING_DELETION or DISABLED.",
      "api": [
        "oci os ns get",
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci search resource structured-search --query-text \"query bucket resources\" --region <region> --all",
        "oci os bucket get -ns <namespace> --name <bucket> --region <region>",
        "oci kms management key get --key-id <key-ocid> --endpoint <vault-management-endpoint>",
        "oci kms vault get --vault-id <vault-ocid>",
        "oci cloud-guard detector-recipe-detector-rule list --detector-recipe-id <recipe-ocid> --all",
        "oci cloud-guard detector-recipe-detector-rule get --detector-recipe-id <recipe-ocid> --detector-rule-id <rule-id>"
      ],
      "evidence": "The `kms-key-id` field from `oci os bucket get` for each namespace+bucket+compartment+region, paired with the `lifecycle-state` and `protection-mode` from `oci kms management key get` for the referenced key OCID and the vault OCID/endpoint used to resolve it.",
      "verdict_rule": "PASS when every bucket in every subscribed region and compartment returns a non-null `kms-key-id` whose referenced Vault key resolves and is in `lifecycle-state: ENABLED`. Null, empty, or a key OCID that is DISABLED / PENDING_DELETION / unresolvable FAILs. A region or compartment that was not enumerated cannot be scored as a pass.",
      "remediation": "Create or select a master encryption key in a Vault, grant the Object Storage service access via a policy such as `Allow service objectstorage-<region> to use keys in compartment <c>`, then run `oci os bucket update -ns <ns> --name <bucket> --kms-key-id <key-ocid>`. Re-encrypt pre-existing objects with `oci os bucket reencrypt -ns <ns> --bucket-name <bucket>`.",
      "caveat": "Setting `kms-key-id` on an existing bucket only governs newly written objects — objects already stored keep their previous data-encryption-key wrapping until an explicit re-encrypt completes, so a bucket can show a customer key while its historical objects are still under the Oracle-managed key. A key OCID that merely exists is not sufficient evidence either: the vault or key may be disabled or scheduled for deletion, in which case the field passes a string match but the control's intent (usable customer-controlled rotation) is not met. Buckets are regional — a home-region-only sweep produces a false pass, and `oci iam region-subscription list --all` is invalid, so drive the loop with `--tenancy-id <tenancy-ocid>`. The key OCID may live in a different compartment or region than the bucket; resolve it against its own vault management endpoint rather than assuming locality. Cloud Guard here is corroboration only and never the verdict source: its `detector-recipe-detector-rule get` takes recipe id plus rule id (no `--detector-id`), and the applicable rule id — the Oracle-managed-key bucket rule, e.g. BUCKET_IS_ENCRYPTED_WITH_ORACLE_MANAGED_KEY — must be read from `detector-recipe-detector-rule list` for the tenancy rather than assumed."
    },
    {
      "control_id": "5.1.3",
      "section": "5",
      "section_name": "Storage",
      "level": 2,
      "assessment": "Automated",
      "service": "object-storage",
      "title": "Keep prior object generations recoverable on all buckets",
      "objective": "With versioning off, an overwrite or delete of an object is unrecoverable, so a single compromised credential or a scripted mistake permanently destroys data. Enabling versioning makes Object Storage retain superseded and deleted object generations, which is the tenancy's only native rollback for ransomware or accidental deletion.",
      "procedure": "Resolve the namespace with `oci os ns get`. Build the full compartment set from the tenancy root with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` (the flag takes no value, and ACCESSIBLE would truncate the tree), and the region set from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`. For each region, list buckets per compartment with `oci os bucket list -ns <namespace> --compartment-id <ocid> --all --region <r>`, then call `oci os bucket get -ns <ns> --name <bucket> --region <r>` on each bucket and read the `versioning` field, whose values are Enabled, Suspended, or Disabled. Read the bucket's object-lifecycle policy as well, since a rule deleting previous versions defeats the control. Record the value per bucket, not per compartment.",
      "api": [
        "oci os ns get",
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci os bucket list -ns <namespace> --compartment-id <compartment-ocid> --all --region <region>",
        "oci os bucket get -ns <namespace> --name <bucket> --region <region>",
        "oci os object-lifecycle-policy get -ns <namespace> --bucket-name <bucket> --region <region>"
      ],
      "evidence": "The `versioning` field of each `oci os bucket get` response, recorded against namespace, bucket name, compartment OCID and region, together with the bucket's `oci os object-lifecycle-policy get` rules (target, action, time-amount) and the compartment/region counts enumerated.",
      "verdict_rule": "PASS when every bucket in every subscribed region and compartment returns `versioning` == \"Enabled\" AND no object-lifecycle rule on that bucket deletes previous object versions inside the organisation's recovery window. A value of \"Suspended\" or \"Disabled\" FAILs; a bucket that could not be inspected counts as FAIL, not as absent.",
      "remediation": "Enable retention of prior generations with `oci os bucket update -ns <ns> --name <bucket> --versioning Enabled`. For buckets already carrying versions, review object-lifecycle rules (`oci os object-lifecycle-policy get -ns <ns> --bucket-name <bucket>`) so that a DELETE action on previous versions does not defeat the control, and pair versioning with a retention rule where immutability is required.",
      "caveat": "\"Suspended\" is the principal false pass: a bucket that once had versioning enabled and was later suspended still shows a non-Disabled value and retains old versions, but new overwrites are no longer protected — a naive truthiness check on the field passes it. Second trap: versioning is a per-bucket setting with no compartment or tenancy inheritance, so sampling one bucket says nothing about its siblings, and a walk that omits the tenancy root compartment misses buckets created there — `oci iam compartment list` does not return the root unless the valueless `--include-root` flag is passed (or the root OCID is appended manually), and `--access-level ACCESSIBLE` can drop further compartments silently. Third: versioning does not survive a lifecycle policy that deletes previous versions after N days, so the object-lifecycle-policy read is needed for a truthful verdict. Fourth: the region loop must be driven by `oci iam region-subscription list --tenancy-id <tenancy-ocid>`; the `--all` form is rejected and takes the whole multi-region sweep down with it."
    },
    {
      "control_id": "5.2.1",
      "section": "5",
      "section_name": "Storage",
      "level": 2,
      "assessment": "Automated",
      "service": "block-volume",
      "title": "Attach a customer Vault key to every block volume",
      "objective": "Block volumes are always encrypted at rest, but by default with an Oracle-managed key that the tenancy cannot rotate, disable, or destroy. Assigning a Vault master encryption key owned by the tenancy makes the volume's encryption material subject to customer rotation policy and enables crypto-shredding of a decommissioned volume.",
      "procedure": "Block volumes are regional and compartment-scoped. For each region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, use resource search for volume resources to collect every volume OCID tenancy-wide in one call — `oci search resource structured-search --query-text \"query volume resources where lifecycleState = 'AVAILABLE'\" --region <r> --all` (the searchable attribute is `lifecycleState`; the mis-cased form matches nothing and returns an empty, clean-looking result). Otherwise enumerate compartments with `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` and call `oci bv volume list --compartment-id <ocid> --all --region <r>` per compartment. Then `oci bv volume get --volume-id <ocid> --region <r>` on each and read `kms-key-id`. Restrict scoring to lifecycle-state AVAILABLE volumes, verify each referenced key is ENABLED via its vault management endpoint, and assert a non-zero volume count wherever compute instances exist so a silently empty query cannot be scored as a pass.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci search resource structured-search --query-text \"query volume resources where lifecycleState = 'AVAILABLE'\" --region <region> --all",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci bv volume list --compartment-id <compartment-ocid> --all --region <region>",
        "oci bv volume get --volume-id <volume-ocid> --region <region>",
        "oci bv volume-kms-key get --volume-id <volume-ocid>",
        "oci bv volume-backup list --compartment-id <compartment-ocid> --all --region <region>",
        "oci kms management key get --key-id <key-ocid> --endpoint <vault-management-endpoint>"
      ],
      "evidence": "The `kms-key-id` field of each `oci bv volume get` response, keyed by volume OCID, compartment OCID and region, with the matching `lifecycle-state` (and `protection-mode`) of the key from `oci kms management key get`; plus the per-region volume counts and the corresponding compute-instance counts used as the non-zero sanity check.",
      "verdict_rule": "PASS when every block volume in lifecycle-state AVAILABLE, in every subscribed region and every compartment, has a non-null `kms-key-id` resolving to a Vault key in lifecycle-state ENABLED. Null or absent `kms-key-id`, or a key that is DISABLED/PENDING_DELETION/unresolvable, FAILs. A region whose enumeration returned zero volumes while compute instances exist there is an enumeration error, not a pass. Volumes in PROVISIONING or TERMINATED state are excluded from scoring.",
      "remediation": "A volume's key is assigned through the dedicated KMS-key operation, not through volume update: `oci bv volume-kms-key update --volume-id <ocid> --kms-key-id <key-ocid>` (read the current assignment with `oci bv volume-kms-key get --volume-id <ocid>`, and revert to the Oracle-managed key with `oci bv volume-kms-key delete --volume-id <ocid>`). Grant the Block Volume service key usage first, e.g. `Allow service blockstorage to use keys in compartment <c>`. For volumes that cannot be updated, create a CMK-encrypted clone or restore from a CMK-encrypted backup and swap the attachment. Re-key backups and cross-region replicas as well.",
      "caveat": "The key assignment is not inherited from anywhere — not from the compartment, not from the source image, not from the instance — so it must be read on each volume individually. Three traps produce false passes: (1) a single-region check, since volumes exist independently in each subscribed region and resource search only answers for the region of the endpoint it is called against (and `region-subscription list --all` is an invalid invocation that kills the loop outright); (2) a structured-search query using the non-existent `lifeCycleState` attribute, which returns nothing and reads as a tenancy with no volumes — always assert a non-zero count where instances exist; (3) treating a non-null key OCID as sufficient when the key or its vault is DISABLED or PENDING_DELETION, which leaves the field populated but the control unmet. Also note that volume backups and cross-region replicas carry their own `kms-key-id` and are commonly left on the Oracle-managed key even when the source volume is compliant, so a volume-only sweep overstates coverage. Volumes in PROVISIONING or TERMINATED state should be excluded from scoring rather than counted as passes."
    },
    {
      "control_id": "5.2.2",
      "section": "5",
      "section_name": "Storage",
      "level": 2,
      "assessment": "Automated",
      "service": "block-volume",
      "title": "Boot volumes must reference a tenancy-owned Vault key",
      "objective": "The boot volume holds the operating system, its configuration and often credentials cached by the instance. Left on the Oracle-managed default key, that data cannot be crypto-shredded or re-keyed by the tenancy; binding it to a customer Vault key brings the instance's root disk under the same key lifecycle as other regulated data.",
      "procedure": "For each region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, collect boot volume OCIDs tenancy-wide with `oci search resource structured-search --query-text \"query bootvolume resources where lifecycleState = 'AVAILABLE'\" --region <r> --all` — materially better than the CLI list, because `oci bv boot-volume list` requires both `--compartment-id` and `--availability-domain`, forcing an AD x compartment cross-product. Note the attribute spelling: `lifecycleState`, not `lifeCycleState`, which matches nothing and yields the zero-boot-volumes result this control is written to prevent. Then call `oci bv boot-volume get --boot-volume-id <ocid> --region <r>` per OCID and read `kms-key-id`. Score only AVAILABLE boot volumes, confirm each referenced Vault key is ENABLED, and assert the boot-volume count is non-zero wherever compute instances exist.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci search resource structured-search --query-text \"query bootvolume resources where lifecycleState = 'AVAILABLE'\" --region <region> --all",
        "oci iam availability-domain list --compartment-id <tenancy-ocid> --region <region>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci bv boot-volume list --compartment-id <compartment-ocid> --availability-domain <ad-name> --all --region <region>",
        "oci bv boot-volume get --boot-volume-id <boot-volume-ocid> --region <region>",
        "oci bv boot-volume-kms-key get --boot-volume-id <boot-volume-ocid>",
        "oci bv boot-volume-backup list --compartment-id <compartment-ocid> --all --region <region>",
        "oci kms management key get --key-id <key-ocid> --endpoint <vault-management-endpoint>"
      ],
      "evidence": "The `kms-key-id` field of each `oci bv boot-volume get` response, recorded with the boot volume OCID, its availability domain, compartment OCID and region, plus the key's `lifecycle-state` from `oci kms management key get`; and the per-region boot-volume and instance counts supporting the non-zero assertion.",
      "verdict_rule": "PASS when every boot volume in lifecycle-state AVAILABLE, across all subscribed regions, all compartments and all availability domains, returns a non-null `kms-key-id` that resolves to an ENABLED Vault key. Null or unresolvable FAILs. A region reporting zero boot volumes while compute instances exist there is an enumeration failure and must not be scored as a pass.",
      "remediation": "Re-key through the dedicated KMS-key operation — the boot-volume update operation does not accept a key assignment: `oci bv boot-volume-kms-key update --boot-volume-id <ocid> --kms-key-id <key-ocid>` (with `oci bv boot-volume-kms-key get/delete` to read and remove). Grant `Allow service blockstorage to use keys in compartment <c>` first. For instances that must be rebuilt, launch them with `--source-details` carrying the kmsKeyId so the boot volume is created against the customer key from the start. Re-key boot volume backups too.",
      "caveat": "The dominant trap is enumeration rather than evaluation: `oci bv boot-volume list` mandates an availability domain, so a script that iterates compartments but not ADs quietly returns nothing for multi-AD regions and reports a clean tenancy with zero boot volumes examined — and the resource-search alternative fails the same way if the query uses `lifeCycleState` instead of `lifecycleState`. Prefer resource search, spell the attribute correctly, and assert that the count of boot volumes found is non-zero wherever compute instances exist. Three further traps: `oci iam region list` returns all OCI regions rather than the ones this tenancy is subscribed to, and `oci iam region-subscription list --all` is rejected outright, so `region-subscription list --tenancy-id <tenancy-ocid>` must drive the loop; a boot volume detached from a stopped or terminated instance still exists and still holds OS data, so filtering to attached volumes understates exposure; and boot volume backups hold their own key reference and are frequently non-compliant even when the live volume is."
    },
    {
      "control_id": "5.3.1",
      "section": "5",
      "section_name": "Storage",
      "level": 2,
      "assessment": "Automated",
      "service": "file-storage",
      "title": "File Storage file systems must use a customer-controlled Vault key",
      "objective": "OCI File Storage encrypts every file system at rest, but the default Oracle-managed key leaves key rotation, disablement and destruction outside the tenancy's control. Pointing each file system at a Vault master encryption key the customer owns lets the organisation rotate on its own schedule and render a decommissioned NFS export unreadable by destroying the key.",
      "procedure": "File systems are regional and availability-domain scoped. For each region from `oci iam region-subscription list --tenancy-id <tenancy-ocid>`, use `oci search resource structured-search --query-text \"query filesystem resources where lifecycleState = 'ACTIVE'\" --region <r> --all` to obtain every file system OCID tenancy-wide in one call (the attribute is `lifecycleState`; the mis-cased form returns nothing and, because this query is the primary enumeration, means no file systems are examined at all). The CLI alternative, `oci fs file-system list`, requires both `--compartment-id` and `--availability-domain`, so it needs a compartment x AD cross-product driven by `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all` and `oci iam availability-domain list`. Before accepting an empty result, corroborate against `oci fs mount-target list --compartment-id <ocid> --availability-domain <ad> --all --region <r>`. Call `oci fs file-system get --file-system-id <ocid> --region <r>` per OCID, read `kms-key-id`, and verify each referenced key resolves to an ENABLED Vault key.",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>",
        "oci search resource structured-search --query-text \"query filesystem resources where lifecycleState = 'ACTIVE'\" --region <region> --all",
        "oci iam availability-domain list --compartment-id <tenancy-ocid> --region <region>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --include-root --all",
        "oci fs file-system list --compartment-id <compartment-ocid> --availability-domain <ad-name> --all --region <region>",
        "oci fs mount-target list --compartment-id <compartment-ocid> --availability-domain <ad-name> --all --region <region>",
        "oci fs file-system get --file-system-id <file-system-ocid> --region <region>",
        "oci kms management key get --key-id <key-ocid> --endpoint <vault-management-endpoint>"
      ],
      "evidence": "The `kms-key-id` field of each `oci fs file-system get` response, recorded with file system OCID, availability domain, compartment OCID and region, alongside the key's `lifecycle-state` from `oci kms management key get`, and the per-region/per-AD file-system and mount-target counts used to validate the enumeration.",
      "verdict_rule": "PASS when every file system in lifecycle-state ACTIVE, across all subscribed regions, compartments and availability domains, returns a non-null `kms-key-id` that resolves to a Vault key in lifecycle-state ENABLED. Null, empty or unresolvable FAILs. An empty file-system enumeration in a region that has mount targets is an enumeration error and must be reported as inconclusive rather than as a pass.",
      "remediation": "Assign the key in place with `oci fs file-system update --file-system-id <ocid> --kms-key-id <key-ocid>`, having first granted the service access, e.g. `Allow service FssOc1Prod to use key-delegate in compartment <c>` (the service principal name is realm-specific). Where a file system cannot be updated, create a CMK-backed file system and migrate the data, then delete the original so the old key material is retired. Re-create clones and snapshots taken under the previous key.",
      "caveat": "`oci fs file-system list` will not run without an availability domain, so the classic false pass is an assessor looping compartments only, getting empty result sets and concluding there are no file systems in the tenancy. Drive the enumeration from resource search — with `lifecycleState` spelled correctly, since a malformed query fails the same way silently — and corroborate a zero result against the mount targets that exist per AD. Also note `oci iam region-subscription list --all` is an invalid invocation; use `--tenancy-id <tenancy-ocid>` or the region loop never starts. Two more: the service policy granting FSS use of the key is region- and realm-specific (the service principal name differs between realms), so a file system can carry a key OCID that the service can no longer use, which the `kms-key-id` field alone will not reveal — resolving the key and confirming ENABLED is the minimum corroboration. Finally, `kms-key-id` describes the file system, not the exports or snapshots taken before the key was assigned; clones and snapshots created under the previous key are not re-keyed by this update."
    },
    {
      "control_id": "6.1",
      "section": "6",
      "section_name": "Asset Management",
      "level": 1,
      "assessment": "Automated",
      "service": "compartment",
      "title": "Verify the tenancy is subdivided into operator-defined compartments",
      "objective": "The tenancy root should not be the only container: workloads belong in dedicated compartments so IAM policies, quotas, budgets and audit scoping can be applied per boundary instead of tenancy-wide.",
      "procedure": "Resolve the tenancy OCID (CLI config `tenancy=`, or `oci iam compartment get --compartment-id <tenancy-ocid>`). Compartments are global IAM objects, so one call against the home region suffices: `oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --all`. The subtree flag returns the whole nested tree, so no recursion and no region loop are needed; `--include-root` is deliberately omitted here because the root itself must not count toward segmentation (and note the flag takes no value if you add it for another purpose). Count entries with `lifecycle-state` ACTIVE, excluding Oracle's `ManagedCompartmentForPaaS`.",
      "api": [
        "oci iam compartment get --compartment-id <tenancy-ocid>",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --all",
        "oci iam compartment list --compartment-id <tenancy-ocid> --compartment-id-in-subtree true --access-level ANY --lifecycle-state ACTIVE --all",
        "oci search resource-type list",
        "oci search resource structured-search --query-text \"query compartment resources where lifecycleState = 'ACTIVE'\" --region <home-region> --all",
        "oci iam region-subscription list --tenancy-id <tenancy-ocid>"
      ],
      "evidence": "`data[]` from `oci iam compartment list --compartment-id-in-subtree true`: each entry's `id` (ocid1.compartment...), `name`, and `lifecycle-state`, plus the tenancy OCID used as the root argument and the total entry count returned.",
      "verdict_rule": "PASS if the subtree listing contains at least one compartment whose `lifecycle-state` == ACTIVE and whose `name` != 'ManagedCompartmentForPaaS'. Otherwise FAIL. Compartments in CREATING, DELETING or DELETED state never count, and a listing produced with `--access-level ACCESSIBLE` or under a principal lacking tenancy-wide inspect is inconclusive rather than a pass.",
      "remediation": "Create a compartment structure mirroring ownership/environment boundaries: `oci iam compartment create --compartment-id <parent-ocid> --name <name> --description <text>`. Nest sub-compartments for per-environment isolation, move existing resources out of the tenancy root with each service's change-compartment call, then write IAM policies scoped to the new compartment OCIDs.",
      "caveat": "Three false passes: (1) counting `ManagedCompartmentForPaaS`, which Oracle creates automatically for PaaS services and proves no deliberate segmentation; (2) counting compartments in CREATING/DELETING/DELETED state — always filter on `lifecycle-state`, or pass `--lifecycle-state ACTIVE`; (3) an existing-but-empty compartment satisfies the letter of the check while every real resource still sits at the tenancy level, so this control is only meaningful when evaluated together with 6.2. Operationally, running as a principal without tenancy-wide inspect rights, or passing `--access-level ACCESSIBLE` instead of `ANY`, silently truncates the tree and can turn a populated tenancy into an apparent zero-compartment result — record the returned count as evidence so truncation is visible. Resource Search is an accelerator only: confirm `compartment` is an indexed type with `oci search resource-type list` before relying on it, and treat `oci iam compartment list` as the authoritative enumeration."
    },
    {
      "control_id": "6.2",
      "section": "6",
      "section_name": "Asset Management",
      "level": 1,
      "assessment": "Automated",
      "service": "compartment",
      "title": "Confirm no workload resources are deployed directly at the tenancy level",
      "objective": "Anything created straight under the tenancy inherits the broadest possible policy scope and cannot be isolated, quota-limited or delegated; workload resources therefore belong in child compartments, leaving the tenancy level for identity constructs only.",
      "procedure": "Get the tenancy OCID and every subscribed region: `oci iam region-subscription list --tenancy-id <tenancy-ocid>`. Search is a regional endpoint, so loop regions: `oci search resource structured-search --region <r> --query-text \"query all resources where compartmentId = '<tenancy-ocid>'\" --all`, letting the CLI page rather than hand-rolling `opc-next-page` handling, so a partial first page can never be scored as an empty tenancy root. Confirm which types Search actually indexes with `oci search resource-type list`, then cross-check the unindexed services per region with direct list calls scoped to the tenancy OCID: `oci compute instance list --compartment-id <tenancy-ocid> --region <r> --all`, `oci network vcn list`, `oci bv volume list`, and `oci os bucket list -ns <namespace> --compartment-id <tenancy-ocid> --region <r> --all` (the namespace comes from `oci os ns get`, which is tenancy-global and takes no region).",
      "api": [
        "oci iam region-subscription list --tenancy-id <tenancy-ocid> --query \"data[].\\\"region-name\\\"\"",
        "oci search resource-type list",
        "oci search resource structured-search --region <region> --query-text \"query all resources where compartmentId = '<tenancy-ocid>'\" --all",
        "oci search resource structured-search --region <region> --query-text \"query instance, vcn, volume, bootvolume, bucket, filesystem, autonomousdatabase, dbsystem, loadbalancer resources where compartmentId = '<tenancy-ocid>' && lifecycleState != 'TERMINATED'\" --all",
        "oci compute instance list --compartment-id <tenancy-ocid> --region <region> --all",
        "oci network vcn list --compartment-id <tenancy-ocid> --region <region> --all",
        "oci bv volume list --compartment-id <tenancy-ocid> --region <region> --all",
        "oci os ns get",
        "oci os bucket list -ns <namespace> --compartment-id <tenancy-ocid> --region <region> --all"
      ],
      "evidence": "Per region, the `data.items[]` array from `oci search resource structured-search`: each item's `identifier`, `resource-type`, `display-name`, `lifecycle-state` and `compartment-id` equal to the tenancy OCID; plus the per-service list results for the unindexed types, the region list used, and the resource-type list Search reported as indexed.",
      "verdict_rule": "PASS if, for every subscribed region, the tenancy-OCID-scoped search plus the per-service cross-checks return zero items after excluding TERMINATED/DELETED states and tenancy-only types (compartments, users, groups, policies, dynamic groups, identity domains, tag namespaces and tag defaults, budgets, `ManagedCompartmentForPaaS`). A region that was not queried, or a search truncated by unhandled pagination, makes the result inconclusive rather than a pass.",
      "remediation": "Move each item into a purpose-built compartment with that service's change-compartment call, e.g. `oci compute instance change-compartment --instance-id <ocid> --compartment-id <target>` or, for a bucket, `oci os bucket update -ns <namespace> --name <bucket> --compartment-id <target-compartment-ocid>`. Prevent recurrence by narrowing `manage` policies to child compartments and setting a zero quota at the tenancy level.",
      "caveat": "Two structural false passes. First, querying only the home region — Search is a per-region service, so a stray instance, volume or bucket in another subscribed region is invisible unless every entry from `region-subscription list` is iterated. Second, trusting Search alone: it indexes only resource types registered with it (verify with `oci search resource-type list`), so newly released or unsupported services never appear and must be enumerated with per-service list calls against the tenancy OCID; and a hand-rolled pagination loop that stops after the first page produces the same empty-looking result, so let the CLI page with `--all`. Conversely, expect legitimate tenancy-level residents that must NOT be flagged: IAM users, groups, policies, dynamic groups and compartments themselves, identity domains, tag namespaces and tag defaults, budgets, `ManagedCompartmentForPaaS`, and Oracle-managed objects; in an identity-domains tenancy the users and groups live inside the domain rather than legacy IAM but the domain itself is still a tenancy-level object. Object Storage buckets have namespace-unique names yet a real per-bucket compartment assignment, so they must be listed per region against the tenancy OCID (`-ns`/`--namespace-name` selects the namespace, `--name` selects a bucket on get/update) rather than assumed global — and the namespace itself is tenancy-global, so `oci os ns get` takes no region. Finally, filter out TERMINATED/DELETED items or long-dead resources will produce false FAILs."
    }
  ]
}
